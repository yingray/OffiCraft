# server/ — Go server daemon (ocserverd)

進入 `server/` 時 nested-load。repo-wide 憲章 + 約定見 root `CLAUDE.md`;本檔記 server 專屬。棧:go1.26。

## 命名(root §10)
- **`server/ocserverd/`** — Go server daemon(**production server**;原 Python 實作已退役——歷史 = git tag `py-final`)。folder = `module ocserverd` = binary `ocserverd`。`ocserverd` 由 `go build` fresh 產出、**永不 commit**(OffiCraft policy:零 committed binary,**唯一的例外是 TCC 身分錨點 `dist/officraft/officraft`**,owner 明確核可、見 root `CLAUDE.md` §13；CI step 1 build fresh，迴圈自動納入 `server/*/go.mod`)。⚠️ 別跟 `bin/ocserver`(bash 的 server *installer*)混淆——`d` 是 daemon 本體。
- (已拆除,t-dc68)`server/ocupdaterd/`(發佈/更新伺服器)與 `cli/ocrelease/`(發佈客戶端)整組退役:發佈改走 **GitHub Releases(repo pkyosx/OffiCraft)**——`bin/release <tag>` 從 clean HEAD 打包 `officraft-<tag>-darwin-arm64.tar.gz` + `checksums.txt`(sha256),出貨 = 顯式 `gh release create`。下游更新檢查/升級由 ocserverd 自己對 GitHub API 做(見下方「軟體更新檢查 + 升級」)。邀請碼/publish token/beta-GA promote/portal/fleet 監控等概念隨之作廢(Beta = GitHub prerelease)。歷史實作見 git(t-dc68 前)。

## 範圍(plumbing + REST + SSE + MCP + reconcile producer 全數填肉)
plumbing:oc.toml config reader(`$OC_CONFIG` / `$OC_DATABASE_URL` 解析順序;**B2 config 瘦身後有效 schema 只剩 `[server].port`/`[server].namespace`/`[storage].dsn`**——`[auth]`/`[server].host`/`[sse_context_high]` 退役 key loader warn+ignore、僅供 settings.go 的一次性 oc.toml→DB 遷移消費;host 寫死 loopback;DSN 預設 = 絕對路徑 `~/.officraft{-<ns>}/server/data/officraft.db`)、HS256 JWT mint/verify(與退役 Python 實作 **byte-level 互通**,`jwt_test.go` 用當年 Python 簽的 fixture 鎖住 wire 相容)、RouteSpec 表載體 + fail-closed boot assertions(principal 階梯 machine < agent < admin_agent < owner)、goose migration 底座(embedded FS + modernc.org/sqlite,免 cgo)。auth 全在 DB settings(`settings.go`:argon2id password hash、jwt secret、token_ttl、password_changed_at、一次性 claim token;`ocserverd set-password` 是 harness/操作員救援的 CLI seam——把 owner 密碼 hash 直接落 DB,oc.toml 永不再載明文;`ocserverd claim-token` 是 installer banner 的本機 CLI seam——印出一次性首設啟用碼(密碼已設則 exit 3、不印憑證),token 只走本機 DB 讀,絕不過未認證 HTTP;`ocserverd set-updater` 已隨 ocupdaterd 拆除(t-dc68)——GitHub Releases 檢查免設定,無 URL/邀請碼可預填;installer 不再自生隨機密碼,首設一律走啟用碼流)。**首跑一鍵設密(`browser.go`)**:serve 帶未消耗 claim token 時,自動開瀏覽器到 `http://127.0.0.1:<port>/?code=<claim token>`(FE FirstRunPage 讀 ?code= 預填,owner 只剩選密碼);gating = stdout 是 TTY 且 `OC_NO_OPEN_BROWSER` 空;開失敗/headless 一律默默降級成印完整可點 URL——serve log **不再印裸啟用碼**。B3 owner 面已入(`api_settings.go`):`GET /api/auth/status`(public 首設探針)+ `POST /api/auth/set-password`(public、一次性 claim token 為閘——serve 在無密碼時 mint 並只印到本機 log,用掉即刪)+ `POST /api/auth/change-password`(owner;驗舊密碼、蓋 `auth.password_changed_at`——auth gate 拒收 iat 早於它的 owner token,agent/warden token 不受影響)+ `GET/PATCH /api/settings`(T-6020 起 `requires=admin_agent` + MCP 工具 `get_settings`/`update_settings`;token_ttl 白名單 12h/24h/7d/30d、handover_pct 40..90,DB 寫入 + `settingsMu` 保護的 in-memory snapshot 就地更新 → 即時生效免重啟)。**其餘三條(auth/status、set-password、change-password)仍 mcp_exclude**;change-password 是 owner 個人憑證,T-6020 明確不下放。

M3 REST 子批 A(oapi-codegen 佈線 + SPA 基座)+ 子批 B(50 條 handler 填肉)已入:
- **`ocapi_gen.go`(committed 生成物)**:oapi-codegen(版本 pinned 在 `bin/gen-ocapi`)從 `spec/openapi.json` 生成 wire types + `ServerInterface`/`ServerInterfaceWrapper`。spec 是 OpenAPI 3.1、工具只吃 3.0,`bin/gen-ocapi` 先做確定性降轉(anyOf-null → nullable;bare `type: number` → `format: double`,避免 float32 吃掉 epoch 精度)再生成——**改 wire 一律 spec 先行,然後 `bash bin/gen-ocapi` 重生**(idempotent;生成物 committed 即 drift-diffable);codegen config 在 `ocapi.yaml`。
- **route 表全面(126 rows;數法見下,別憑印象數)**:`jq length conformance/routes_manifest.json` —— 這個數字**每加一條路由就過期一次**,而且沒有任何守衛會叫,所以動這句話前先重數一次。⚠️ 直接數 `routes.go` 的 `Path:` 會少 3:有三列(Web Push)寫成 `Method: …, Path: …` 同一行,行首 grep 掃不到;只數 `"/api` 開頭又會再少 4(`/health`·`/version`·`/in`·`/install.sh`)。表與快照本來就該相等(`server_test.go` 的 spec-surface 測試鎖「表 ≡ spec operations」),兩邊對不起來時先懷疑數法。`routes.go` 現載 spec 的全部 operations,auth/requires/mcp 旗標逐行鏡自凍結快照 `conformance/routes_manifest.json`;`server_test.go` 的 spec-surface 測試鎖「表 ≡ spec operations」。handler 走 wrapper(param 綁定;bind 失敗 → 422 envelope)進 `apiServer`(`api_stub.go` 載體 + `api_*.go` 模組檔)。
- **子批 B handler 已填肉(`api_*.go`)**:`api_auth.go`(login/mint/bootstrap)、`api_members.go`(roster + lifecycle + self-report)、`api_chat.go`(chat/attachments/reads/resume-summary)、`api_roles.go`(global-context/roles/lessons)、`api_monitoring.go`(context gauge / telemetry ingest + fold / monitoring)、`api_machines.go`(machines/installer/binaries/aliases)、`api_infra.go`(minimal SSE + MCP envelope)。行為逐條 port 自退役 Python 實作(tag `py-final`),行為權威 = spec/ + conformance;**response DTO 手寫在 `wire.go`**(Pydantic 宣告序 + null-不省略;生成型別只當 request-body 詞彙——生成 struct 的 omitempty 會丟 `token: null` 這類語意鍵)。`hub.go` 是完整 SSE hub(連線註冊=判定 online、machine claim、dual-SSE 409、connect/disconnect edge hooks、`Publish` 真 fan-out + per-warden command FIFO;見下方 SSE 條)。`assets.go` 的 repo-file assets(repo-root `seeds/*.md` / prebuilt binaries / spec/mcp-catalog.json)**一律 embed-only**(T-e731,owner 拍板):三類全走 **go:embed**、**無磁碟旁路**——CWD 下陳舊的 `seeds/`·`spec/mcp-catalog.json`·`bin/ocwarden` 絕不遮蔽這個 binary 建進去的版本鎖定副本(disk-first 曾釀三連內容面無聲事故:陳舊 boot/worker/role/lessons 種子、陳舊 tools/list catalog、陳舊 bootstrap-here warden;這是把 serveBinary 下載路由早有的 embed-only 姿勢推及全部 asset seam)。seeds 走 `seedsdist/`(`bin/build-seedsdist`),prebuilt binaries + mcp-catalog.json 走 `bindist/`(`bin/build-bindist` build server 同平台的 ocagent、TCC 錨點 `officraft`、ocwarden 三支再連 catalog 一起 staging;**順序是有意義的**——錨點必須先編、staging 進 `cli/ocwarden/anchordist/`,ocwarden 才 embed 得到它——單平台 by design,bootstrap-here 天然同機;cross-arch 遠端走別路)。下載路由直接從 embed bytes serve;exec 路徑(bootstrap/teardown-here)把 embed materialize 成 0755 檔到 `binCacheDir`(SQLite data 檔旁的 `bin/`,cmdServe 蓋章;空值=fallback 關閉答 503)。部署用 `ocserverd`(單檔即可 boot agents + 裝機,~25MB)用 **staged seedsdist + bindist** 建、僅 webdist 仍 pristine;build 前先跑 `bash bin/build-seedsdist && bash bin/build-bindist`(產物**不 commit**——policy 零 committed binary(唯一例外見 root §13 的 TCC 錨點),部署 binary 由發版流程 build fresh、見下方「軟體更新檢查 + 升級」)。dev/CI 從乾淨 worktree 起因此**必先 staging**(`bin/ci.sh` 開頭已自動 stage seedsdist+bindist、`conformance/run.sh` build 前 guarded staging),否則 boot 讀空 embed 直接紅。`dbseed.go` 在 migrate/serve 起站 idempotent seed Mira + server-self warden(無 owner 表)。SQLite 併發姿勢:**WAL + 讀池／寫池分開(T-dd7a,見下方專節)**——`migrate.go openSQLite` 是寫池(上限 1 + `_txlock=immediate` + busy_timeout),`openSQLiteReadPool` 是讀池(8 條、`mode=ro`);**舊敘述「單 pooled conn」已是假的**,那個姿勢讓全行程每個請求在資料庫排一列。
- **go:embed SPA 基座(`spa.go`)**:單一 binary 出貨(owner 定調)。`frontend/dist` 是 gitignored 產物,`bin/build-webdist` 先 npm build 再 staging 進 `webdist/`(gitignored,僅 .gitkeep committed)→ `go build` 內嵌;沒 staging 也能 build,`/` 回 friendly build hint(降級不炸)。fallback 佔 bare `/` pattern,表 row 全贏;template 對中但 method 錯 → 405 envelope、未知 `/api/*` → 404 envelope、asset miss → 404、其餘 → index.html(SPA catch-all)。**帶 SPA 的部署 binary 由 `bin/build-webdist` + `go build` 產(webdist staged 後 go:embed 內嵌),不把 gitignored dist 內容夾帶進 git**;dev/CI 或單檔 boot binary 則用 pristine webdist(僅 hint)build——兩者皆**不 commit**(policy 零 committed binary;唯一例外是 root §13 的 TCC 錨點,與 SPA 無關)。

- **MCP tools/call loopback 已通(步驟⑤,M3 第五批)**:`POST /api/mcp` 全真(`api_infra.go` envelope 面 + `mcp.go` loopback 模組)。tools/call 依 spec/mcp.md §3 拆參(tool name → route 表反查 `mcpToolIndex`;path params 代入模板、缺鍵補空字串;GET 剩餘鍵成 query(nil 丟棄、list doseq 展開);非 GET 剩餘鍵成 JSON body、空剩餘仍送 `{}`),然後**in-process 重入自己的 mux**(`apiServer.loopback`,cmdServe 組完 handler 回寫)、caller Authorization 原樣轉發——同 gate、同 RBAC choke、同 wrapper 綁參、同 handler。result 映射:單一 text content 載 raw body、`isError ≡ status>=400`(route 4xx 是成功 result 帶 isError,不是 RPC error)、body 是 JSON object 才帶 structuredContent(json.Number 保 literal)。tools/list 描述子**留凍結 spec/mcp-catalog.json 直服**(spec §4:對 snapshot byte-equality 才是契約;Go 靜態重derive 全部 inputSchema = 第二份會漂的 DTO schema 清單),tool **名字面**(call 路由 + catalog_hash)則 derive 自表(`RouteSpec.toolName`),conformance 鎖 snapshot ≡ live ≡ 表序防默漂。
- **機器版本欄 + 一鍵升級(T-5f01;`api_machines.go`)**:warden 30s 心跳 telemetry 順帶 `binaries` 內容指紋(live ocwarden/ocagent 的 sha256 12-hex 前綴,in-memory telemetry entry,不落庫);server 起站時 hash 自己 embed 的 bindist 同名 binaries(`assets.go bindistBinaryHashesFrom` → `apiServer.binHashes`),`machineBinStatus` 比對出 `bin_status`(current/stale/nil=unknown——舊 warden 無指紋、部分回報、或 embed 空,一律誠實 unknown 絕不猜)上 machineDTO(list)與 monitoringMachineDTO(monitoring fold)兩路。**一鍵升級 = `POST /api/machines/{member_id}/upgrade`(T-6020 起 `requires=admin_agent` + MCP 工具 `upgrade_warden`;原 owner-only、MCPExclude)**:fire-and-forget push `update` 動詞進 warden 命令 FIFO(`buildTargetFrame` + `enqueueWardenFrame` 同一 reachability gate;離線 = dispatched:false 不寫任何 intent;`--no-reconcile` 一併關掉)——warden 收到即 Kick 既有 self-update reconcile,要確認真的更新了,靠下一次心跳指紋翻 current 觀察,無 receipt。**刻意比對結果、不做逐機版號**(owner 核可;埋版號會造成 update 迴圈，見 cli/ocwarden/selfupdate.go SWAP ORACLE)。**佇列存進資料庫(T-66a2 L3)**:`update` 是唯一會落庫的動詞(`migrations/00034_warden_command_queue.sql` + `hub.BindWardenCommandStore`,newAPIServer 組裝時 rehydrate)——升級端點本身就 re-exec server,而沒有任何東西會重新推導「owner 按過升級」;START/STOP/UNINSTALL 刻意不落庫(reconcile 一個 cadence 內從 presence 重推,且 START frame 帶 member_token,機密不寫進磁碟)。清除點 = **寫進 socket 成功**(本 band 無 ack,這是 server 唯一能觀察到的事件;重啟後復原的意義是「還沒人寫出去」,不是「還沒被執行」)。重複送安全靠 self-update 的 content-hash 冪等;佇列以 (warden,verb,target) 為天然鍵(每機至多一筆)+ 24h 過期兩道界住。


- **telemetry 巢狀契約 + 樣本新鮮度(T-90be / T-b36a;`api_monitoring.go`)**:
  - **巢狀形狀已宣告**:`hardware` / `claude` / `runtimes` 三個 block 在 `spec/openapi.json` 的 `AgentTelemetryIngestDTO` 有真的 `properties`(cpu_pct/ram_pct/battery_pct/ac_power;version/cred_file/sub_readable/keychain;per-runtime installed/logged_in/version),所以 producer 端改名(例:`cpu_pct` → `cpu_percent`)現在會被 CI 抓到。**故意沒有 `additionalProperties: false`**:關掉開放性 = 舊/新 warden 多送一個沒聽過的巢狀鍵就整份 422,那正是 a7fa594 全 fleet telemetry 默默歸零的事故形狀。哨兵 = `TestHandleIngestTelemetry_UndeclaredNestedKeyStillLands`。
  - ⚠️ **宣告形狀連帶把拒絕碼搬了家**:這三個 block 傳**非物件**現在是 decoder 的 **422**(codegen 把欄位型成 `map[string]interface{}`),不是 handler `asObject` 的 flat 400;未宣告的 `binaries`/`rate_limits`/`tokens`/`command_result`/`self_update` 仍是 400。`{"hardware":null}` 與 `{}` 一樣是 all-absent **400**(null 解出來是「欄位缺席」而非型別錯),`{"hardware":{}}` 是 **200**(每個探測都失敗仍是一份樣本)。整張表釘在 `TestHandleIngestTelemetry_WrongTypedBlockStatusTable`,規範性文字在 spec/lifecycle.md §3。
  - **`warden_shape`:回報的、不是算出來的(T-ff5d)**:warden 心跳帶 `anchor`/`legacy`/`unknown`;ingest 照 `runtime` 那條路驗**封閉列舉**(不在三態內 = **flat 400**、非 422),合法值 merge 進 in-memory entry(不帶就沿用舊值——大多數 POST(command_result / agent context)本來就沒有 shape,清掉等於謊報成「這個 build 不回報 shape」),並在 POST 回應 echo。讀回面 `MachineDTO.warden_shape` 與 `MonitoringMachineDTO.warden_shape` **原樣透傳**(`machineWardenShape`)。🔴 **與隔壁的 `bin_status` 對照著看**:`bin_status` 是 server **自己比出來**的(回報指紋 vs 自己 embed 的 hash),`warden_shape` **沒有第二個來源**——只有回報的那個行程讀得到自己的父行程,所以 server 不推導、也不得推導。**缺席永遠留缺席**:`null` = 這台的 warden build 還沒收到 anchor-cutover 發佈,回報的 `unknown` = 新 build 跑了但讀不到父行程,兩者語意不同,任一方向的互推都是造假(spec description 明文寫死)。
  - **per-sample 戳記**:ingest 寫 `entry["hardware"]` / `entry["runtimes"]` 時各自蓋 `hardware_ts` / `runtimes_ts`,**與 entry 自己的 `ts` 分開**——`ts` 每一筆回報都前進,一則不帶硬體的 command_result receipt 會讓一小時前的 CPU 數字看起來像剛到的。fail-closed:帶著資料卻沒有戳記 = 年齡未知 = 不算新鮮(`hardwareStampOf` / `runtimeCapabilitiesStampOf` 讀不到就回 0)。
  - **新鮮度窗 = `telemetryFreshSecs` 90s(唯一常數)**,由 warden 心跳 cadence 推導:`cli/ocwarden/main.go` 的 `reportThrottle = 30 * time.Second`,而被 server **接受**的心跳把 backoff 歸零、下一輪就是那個 cadence,所以健康機器約每 30s 重蓋一次戳記;90s = 三個 cadence,容得下兩次遺失(睡著的筆電、網路抖、server 換版)而不會讓健康機器閃成「無資料」。**刻意不綁 presence**:「SSE 斷了」與「沒人量過這台」是兩件事。
  - **值的型別:不拒收,但要留下痕跡(T-aad2)**。鍵名層自 T-90be 有守衛,**值的型別層完全沒有**——`cpu_pct: "47"` 是 200、原樣存下、讀回來 null,而那個 null **與「這台從來沒有 CPU 探測」的 row 逐 byte 相同**(實測:同一組 `hardware_ts`/`hardware_stale`,四個值同樣是 null)。**沒有改成 400**:對這些 block 做 fail-closed 正是 owner ruling `rc-55861dd893c6` 否決的那件事,而且炸的是整份心跳(hardware+binaries+claude+runtimes 一起歸零 = a7fa594)。改的是**讀取端不再沉默**:`monitoringMachineDTO.HardwareInvalid`(wire `hardware_invalid`,honest-empty 陣列)列出**被服務的那份樣本裡、宣告過但型別讀不懂**的鍵。**per key** 不是 per row(一個壞探測不該連累兄弟欄位)、**只放鍵名不放值**(未受信輸入不進座艙)、**只在 fresh 樣本上算**(stale 的空白已經有 `hardware_stale` 這個公佈過的理由,兩個互相競爭的解釋比一個更糟)。**刻意不算 invalid 的三種**:鍵缺席(`collectHardware` 探測失敗就省略 → 那正是「從沒量到」)、明確的 null(spec 每一欄都宣告 `anyOf: [<type>, null]`)、負數(teleNum 會收回 -1 這個 未量到 sentinel,但它型別沒錯、producer 沒壞)。**未宣告的鍵永遠不被指控**——block 仍是 `additionalProperties: true`,沒宣告就沒有型別可違反,判它等於把被否決的 fail-closed 從 ingest 搬到 read。FE 端 `mon-hardware-bad` chip 與 `mon-hardware-stale` **刻意是兩顆不同的 chip**(要去查的東西不同)。**座艙確實吃得到**(追過,不是假設):`MonitorPage.tsx` 機器表是 JOIN——列是 registry row(`MachineDTO`,身分/online/操作),但**每一格硬體都讀 `hwByHost.get(machineId)`,也就是這個 monitoring fold row**。`MachineDTO` 沒有這個欄位不是漏掉:它從來就沒帶過 cpu/ram/battery/ac,沒有空白硬體格需要解釋(`claude_*` 與 `bin_status` 兩張表都有鏡像,是因為兩張表都渲染它們;非對稱來自誰渲染什麼)。⚠️ **覆蓋範圍(對照 live handler 實測,不是推的;講太窄跟講太寬一樣有害——會讓下一個人去做一個永遠不可能觸發的 marker)**:三個宣告過的 block 由**三種不同機制**守著。`runtimes`——**ingest 就 fail-closed**,而且是既有行為不是這包加的:handler 逐鍵檢查 `installed`/`logged_in`/`version`,型別錯直接 400(`runtimes.codex.installed must be a boolean`),**值根本進不了 store,所以不在洞裡、也不該補讀取端標記**。`hardware`——ingest 不擋(owner ruling),所以由**讀取端**標記承擔,任何 warden 送錯型別座艙都看得到。`claude`——**剩下的那個洞,仍然開著、也仍然沒有任何提示**:`{"claude":{"version":9.9}}` 一樣 200、存下、讀回 null,線上沒有任何欄位講這件事;**只有 CI 守衛擋自家 producer**,舊版或第三方 warden 飄了 runtime 仍然看不見。已知、刻意不在這一包修(owner 裁示另外落票)。哨兵:`TestGetMonitoring_WrongTypedHardwareIsNamedNotSilent` / `_BrokenAndUnmeasuredHardwareAreDistinguishable`(owner 的驗收標準本身)/ `_HealthyHardwareNamesNothing`(誤傷防線)/ `_UndeclaredHardwareKeyIsNeverAccused` / `_StaleSampleAccusesNobody`,FE `MonitorPage.hardware-bad-value.test.tsx`。
  - **CI 端同批補上值型別守衛(T-aad2,`cli/ocwarden/telemetry_wire_test.go`)**:既有 walker 只比對**鍵名**,所以「名字沒變、型別變了」的 producer 回歸從頭到尾看不見。`mistypedPayloadValues` 拿**真 producer 的 payload**(經 `onTheWire` 走一次 encoding/json——producer 回的是 Go 原生型別,`parseBattery` 回 int,要比的是**線上**型別)對凍結 spec 的 `anyOf` 型別比對。**只管我們自己的 producer,對 server 的寬容度零影響**;正向控制 `TestWardenTelemetryValueGuardSeesRetypedValues` 在三個深度各改一個值的型別、要求守衛點名(把比較關掉,四個 subtest 全紅——親驗),另有覆蓋率斷言要求那些葉節點真的宣告了型別(沒宣告的 walker 會無聲放行一切),與 `TestWardenTelemetryValueGuardIgnoresUndeclaredKeys` 守住 open-block 那一半。
  - **兩種樣本、兩種處置,而且理由不同**:`hardware` 過期就**收回數值**(cpu/ram/battery/ac 回 null)但**留戳記**;`runtimes` 過期**不收回**、改**標記**(`runtime_capabilities_stale`)——那份舊 map 是 worker 卡在 `machine_unavailable` 唯一的解釋,刪掉等於用一種無聲換另一種。**stale 裁決一律 server 算**(`hardware_stale` / `runtime_capabilities_stale` 都上 wire),門檻只有一個家、client 不拿自己的時鐘重推。⚠️ **placement 刻意不吃新鮮度**:`machineSupportsRuntime` 若對過期 map 視而不見,一台安靜的機器會被重新分類成「舊 warden」而拿到 claude 工作——新鮮度是「座艙可以宣稱什麼是現況」的問題,不是派工的問題。
  - **machines 區塊的「有哪幾台」由名冊決定、不由 telemetry 決定(T-b89d)**:`GET /api/monitoring` 的 machines 列集合 = **kind=warden ∧ roster=active**(與 `GET /api/machines` / `resolveMachine` 同一個判準,兩個介面因此不可能對「有哪幾台」各說各話);telemetry 只供**那一台的數值**。理由:telemetry 只增不刪(全 repo 唯一 `telemetry.Delete` 是 api_roles.go 的正職 hard-delete),而刪機器(`DELETE /api/machines/{id}`)只把 warden member 翻 roster removed、**完全不動 telemetry**,所以「誰回報過」推出來的列集合只會單調成長——被刪掉的機器會被它跑過的(已釋放)外包**每一次請求重新復活**,owner 永遠看得到一台自己移除過的機器。四個邊界:**離線但在冊 → 照樣出列**(存在是名冊事實、不是 liveness 事實);**已移除 → 不出列**(本票);**已 uninstall 但未刪 → 照樣出列**(uninstall 是一次性 lifecycle 意圖、**從不寫 roster_status**,record 刻意保留可重裝,`GET /api/machines` 也還列著;釘在 `TestGetMonitoring_UninstalledButUndeletedMachineStillListed`,它連「uninstall 不動 roster」這個前提本身都驗);**host 解不出來(`""`) → 不出列**(`""` 是「沒有機器 id」不是一個 id,所以那台掛著帳號的無名列消失;帳號本身**不受影響**——acctByHost 完全沒動,accounts 區塊照樣有那把 key、machine 欄誠實留空)。⚠️ **只窄不寬、且不碰 accounts 區塊**:這是 T-fc2f(owner 親報的 eva-m5-claude bug)的保命論證——列消失了、花費/視窗/機器歸屬一個都沒動,`TestGetMonitoring_RemovedMachineLeavesNoOrphanRow` 在同一個 body 裡同時釘住這兩半。⚠️ **T-fc2f 的零值 mint 已一併刪除(owner 2026-07-27 裁定)**:列集合改由名冊決定後,那三行**構造上就是死碼**——`observedHost` 對 warden **無條件**回自己的 id、非 removed member **無條件** `countsAsPresentAgent`,所以每台在冊機器的 host key 一律由 `++` 建出,mint 只可能多建出隨即被丟掉的 key;過濾後 `keys(hostCounts) ∩ registry ≡ registry`,**刪掉對 wire 零可觀測差異**(已實測)。它想維持的那個「key set = 任何 actor 被觀察到的所有 host」語意**沒有任何消費者**(key set 唯一出口是被交集掉的 `hosts`,值的唯一出口是 `agents`)。留著的形狀最糟:三行證明為死的碼,唯一守衛是一段比它長二十倍的註解,而那段註解已經害人把它當「不可打破的前提」寫進交辦單。`hosts` 現在直接迭代名冊,`hostCounts` 退化成純計數器(查無此 host 讀 0)。

- **T-6020 治理下放(owner 2026-07-26 拍板)**:admin_agent 這一級原本在階梯上高於 agent,實際能做的卻幾乎一樣——**24 條營運端點全部 `requires=owner` + `MCPExclude`**。owner 把其中 **19 條降到 `requires=admin_agent` 並改成真正的 MCP 工具**(catalog 68 → 87 tools;每列的 `MCPTool` 顯式命名,見 `routes.go`):settings 讀寫、release check、station upgrade、webhook /in debug ring、reply-card answer/re-answer/expire、在 server host 上裝/拆 warden、warden upgrade、task terminate/message、outsource worker boot-context/refocus/stop/restart/model、task-manual delete。**5 條刻意保留 `requires=owner` + `MCPExclude`**:`POST /api/mint`(發身分等於可自我提權,開了整條階梯就是裝飾)、`POST /api/auth/change-password`、以及三條 Web Push(owner 個人帳號與個人瀏覽器)。**保留是決定不是遺漏**——理由逐列寫在 `routes.go`,兩半各由 `routes_t6020_governance_test.go`(表層)與 `conformance/test_mcp.py` 的 `test_t6020_*`(線上)釘住,含一條「任何**新的** owner-only 列都要有自己的裁定」的完整性檢查。
  - **工具命名(owner 2026-07-26 二次拍板)**:五個名字被改成講清楚「動到哪個東西」——`upgrade_station`(升級這台 OffiCraft 本體,不是泛指軟體)、`upgrade_warden`、`install_warden_on_server_host` / `uninstall_warden_on_server_host`(原 `*_machine_here` 的「here」在 MCP 這種沒有地點感的介面裡讀不出來)、`get_outsource_worker_boot_context`(與同批其他 `*_outsource_worker` 工具對齊)。其餘 14 個維持原名。
  - **兩個不在路由表上的 in-handler 硬判同批放開**:(a) `set_task_priority` 的 `frozen`——原本 403「frozen is the owner's knob」且**兩側都鎖**,現在放給 **owner / admin_agent / 該任務的 executor**,而且**對稱**(能凍的就能解;碼上就是刪掉那兩個分支、只留單一 `callerMayDriveTask` 閘)。單側放開會生出「自己凍了卻只能等 owner 解」的死角。(b) `callerMaySetAssignee` 門檻 `principalOwner` → `principalAdminAgent`。
  - **`frozen` 開放帶來的語意漂移已一併處理**:凍結原意是「老闆喊停」,開放後 owner 看一張凍結票無法分辨是誰按的,所以新增 durable `task.frozen_by`(`migrations/00037_task_frozen_by.sql`)+ `TaskDTO.frozen_by`——stamp 於進入 frozen 的那次寫入(owner scope = `wireOwnerID` 字面,其餘 = member / ow- id)、離開 frozen 時清空(不留過期的名字在跑著的票上)。**這是可從外部讀到的欄位、不是註解裡的宣稱**,釘在 `TestSetTaskPriorityFrozenByNamesWhoFroze` 與 conformance `test_executor_freezes_and_unfreezes_symmetrically`。seeds(`system_interaction.md` §10.6 / `role_def_assistant.md`)同批教:owner 凍的票技術上解得開、但不要自己解。⚠️ `frozen_by` **只在詳細 DTO 上,輕量摘要表(`GET /api/tasks`)不帶**——與既有輕量化設計一致,但 FE 想在列表顯示「誰凍的」時要先擴欄。

- **T-5336 權限邊界整併(owner 2026-07-27 拍板)**:三個爭議點逐一裁定,**只有第一點動了行為**。
  - **(1) webhook 設定 CRUD 四條全部抬到 `requires=admin_agent`**(GET list / POST create / PATCH / DELETE,`routes.go`)。**讀取面一起抬,不是順手**:四條的回應都是 `WebhookEndpointDTO`,而該 DTO 帶**明文 `token`**——公開 `/in` 入口的全部憑證。所以 LIST 一呼就交出該成員全部入口密鑰,比任一寫入動詞更危險;拿到 token 的人可以往那個成員注入合成 chat。⚠️ **`MCPExclude` 從來不是邊界**:它只把這四列擋在 MCP 工具目錄外(一個 client 的可發現性),REST 一直是開的,舊的 `principalMachine` 地板讓**任何** agent token 通過。同批修掉 `spec/openapi.json` 的 `WebhookEndpointDTO.description` 與 `wire.go` 那句「is NEVER on any public or agent-facing wire」——在舊地板上那句是**假的**,不是過時。生成物照 wire-freeze 流程走:改 `spec/openapi.json` → `bash bin/gen-ocapi` 重生 `ocapi_gen.go` → `npx openapi-typescript` 重生 FE schema,三份逐字一致。`conformance/routes_manifest.json` 與 `test_auth_matrix.py` 的 requires 同步(matrix 的 PATCH/DELETE `overrides` 從「全 identity 404」窄成「只有 admin/owner 404」,否則 below-floor 的 403 會被覆蓋值遮掉)。**既有呼叫者盤點**:座艙(`frontend/src/api/http.ts` 走 `ownerToken()`)是 owner scope、不受影響;`cli/ocagent` 與 `cli/ocwarden` 完全沒有呼叫這四條;沒有 MCP 工具(MCPExclude)⇒ 沒有 agent 走這條路。三臂測試 `routes_t5336_webhook_authz_test.go`(反事實實測:四列改回 `principalMachine` → 臂 (c) 四個子測全紅、臂 (b) 仍綠)。
  - **(2) `PATCH /api/members/{member_id}`(update_member)維持 `principalMachine` 地板——刻意,不是遺漏。** 前提是**成員之間互相信任**:改同事的顯示名/model/effort 是庶務不是治理。與同一組 `{member_id}` 的 DELETE(`dismiss_member` = `admin_agent`)不對稱(「改掉他」比「解僱他」容易)是**已知且被接受**的:解僱不可逆且把人移出辦公室,編輯下一個人就能改回來。理由寫在 `routes.go` 該列上方,**下一個做權限盤點的人不必再查一遍**;要抬需要新的 owner 裁定,不是整理型 commit。
  - **(3) 讀取面權限地板這一輪一行不改。** owner 明確否決了「把改帳號顯示名抬到 admin 以補齊不對稱」的提議;書面裁定就是全部產出,碼上不留註解(免得把一個「暫不處理」寫成一個看起來像設計的宣稱)。

- **T-5336 結構性防復發:路由表以外的授權必須被列舉(`authz_surface_gate_test.go`)**。⚠️ 這張票真正的產出不是那四條 webhook,是**這份清單**。T-6020 一次重評 24 條營運端點,而它能重評的**只有它列舉得到的東西 —— routes.go 的 `Requires` 欄**;授權判斷寫在別處的(handler 內部硬判、或寫入動詞停在 machine 地板而實際上沒有任何守衛),對那次重評**結構上不可見**,所以被漏掉。owner 這次撞到的就是漏掉的那條。**凡授權寫在路由表之外,就會週期性地漏掉下一次治理變更**——治這個形狀,不治那一條。
  - **gate (1) `TestAuthzOutsideTheRouteTableIsEnumerated`**:AST 掃(`go/parser`,**不是 grep** —— 註解與字串字面值根本不是運算式節點,所以「匹配到自己的說明文字」這種失效模式由構造排除)兩類函式:(i) **看得到 caller 的**(參數或 **receiver** 的型別在 `callerContextTypes`:`http.Request` / `taskCaller` / `outsourceGateRequest`,比對前先剝掉指標星號),以及 (ii) **自己推導出 principal class 的**(呼叫 `classifyMember`/`principalAtLeast`/`isOutsourceMember`,或提及 `principal*` 常數)—— 第二類是必要的,因為 `resolveDispatchInitiator` 只收一個裸 `actorID string`,純看簽章的掃描**看不到它**。找出每一條讀 caller 身分的判斷:seam 呼叫(`currentScope`/`currentActor`/`principalOfRequest`/`principalAtLeast`/`classifyMember`/`isOutsourceMember`)、特權常數比對(`principal*`/`adminRoleKey`/`machineKind`/`Kind*`)、以及**直接比對 `.Kind` / `.RoleKey`**。⚠️ **最後那一種是重點**:只認 `currentScope` 的掃描會從 `caller.RoleKey != adminRoleKey` 旁邊走過去,而那正是上一條藏起來的方式(實測 mutant A2)。掃到的 **36 條**都必須列在 `authzOutsideRouteTable` 並**寫明理由**(≥40 字元,`TestAuthzInventoryReasonsAreRealReasons` 擋「legacy」「ok」這種一個字的敷衍)。掃到但沒列 → 紅;**列了但掃不到(stale)也 → 紅**。⚠️ **這兩道閘擋不住「一句話的敷衍」**:key 寫對、理由寫成一段語意通順但空洞的話,兩道閘都會綠(T-5336 獨立審查實測過)。所以這份清單的價值**不是「不能被灌水」——它可以**;而是**灌水必須顯式出現在 diff 上**:要在路由表外加一條授權,就得同一個 commit 改這個檔,reviewer 看得到。閘把「默默新增」變成「可見新增」,真正擋下壞條目的是**讀 diff 的人**——所以看到這裡的新增項,要當成「待查證的主張」,不是「已經核准的決定」。
  - **gate (2) `TestMachineFloorWriteRoutesAreEachARuling`**:每一條 **POST/PATCH/PUT/DELETE 且 `Requires=principalMachine`** 的路由,都必須在 `machineFloorWriteRulings` 有一筆**追溯得到裁定**的紀錄(`Ruling` 必須含票號 / owner 日期 / 憲章節次,`Why` ≥40 字元)。machine 地板**不掛任何 class choke**(authz.go),所以「寫入停在地板」是一個**主張**、不是預設值。豁免清單因此不是白名單:`update_member` 在裡面是因為 **T-5336 裁定 2**,`PATCH /api/accounts/{account_id}` 與 `PATCH /api/machines/{machine_id}` 在裡面是因為 **T-5336 裁定 3**(owner 明確否決了把改顯示名抬到 admin)。
  - **鑑別力已實測(2026-07-27,每顆 mutant 前 `go clean -testcache`、事後從 scratchpad 備份還原)**:(A) 在 `HandleUpdateMemberApiMembersMemberIdPatch` 加一條 `currentScope(r)=="agent"` 硬判 → gate (1) 紅,兩條新判斷都被點名;(A2) 同樣的形狀改寫成 `caller.RoleKey != adminRoleKey`(完全不碰 seam 函式)→ gate (1) 仍紅;(B) 把 `DELETE /api/members/{member_id}` 從 admin_agent 降回 `principalMachine` → gate (2) 紅;(C) 把一筆豁免的 `Ruling` 改成 `"known-good"` → gate (2) 紅(追溯性不是裝飾)。
  - **反恆真三道**:掃描語料在被審判**之前**先自證非空(sites/files/functions 各有下限,掃描壞掉是紅不是綠);路由表先自證同時存在寫入列與地板列;兩份清單的 stale 項目都是 finding。⚠️ 這只治**這兩道閘自己**,**不代表那個 bug 類別在 repo 其他地方已經全域解決**——別讀成那樣。
  - ⚠️ **`TestCallerContextTypesAllExist`——下限檢查證明不了的那一半**。`callerContextTypes` 是一串**字串比對的型別名**:名字打錯不會紅,只會**默默縮小掃描範圍**,而 `authzFuncFloor` 光靠 `*http.Request` 的函式就滿足了。**下限只證明「掃描器活著」,不證明「掃描器看得到它宣稱看得到的每一類入口」——這是兩件事。** 這不是假設:這道閘第一版寫的是 `outsourceSpawnRequest`,一個**全樹不存在**的型別(真名 `outsourceGateRequest`),於是**發包授權 choke(`outsourceSpawnGate` 的 admit/deny + `resolveDispatchInitiator` 的分類器)從第一天就漏在清單外**,由 T-5336 獨立審查抓到。現在每個型別名都必須在本 package 至少匹配到一個函式,否則紅(mutant 實測:把名字改回原本的錯字 → 紅)。這道檢查上線第一跑就又抓到兩個不存在的指標型別 `*taskCaller` / `*outsourceGateRequest`,已改成比對前剝除指標。

- **機器名冊 = 機器憑證的權威(T-9cf8;`authz.go revocationRefusal`,掛在 `server.go requireAuth`)**:刪掉一台機器,它的 token 下一個請求就失效。⚠️ **實查後的現況(改動前)**:刪除只寫 `RosterStatusRemoved`,而驗證是無狀態 HS256、**machine floor 那 51 條路由連 `requirePrincipalClass` 都不掛**(`buildHandler` 顯式 skip),所以被刪機器的 token 照樣 200:`/api/monitoring/telemetry`(心跳——「孤兒機器列」的**其中一個**上游來源,不是唯一成因:上面 T-b89d 段講的是另一個(已釋放外包 worker 的請求每次把該列復活,那一路確實是殘留資料)。⚠️ **合併後兩路都不再能產生該列**——T-b89d 已把列集合改由名冊決定;這條留在這裡是因為撤銷 token 治的是「被刪的機器還在說話」本身,不是那一列的顯示)、`/api/chat`、`/api/mcp`、`/api/members`、`/api/machines`、`/api/global-context`。當時**已經**擋住的只有兩處:`GET /api/events`(上面那條 zombie gate 的 roster-removed 臂)與 `POST /api/self/*`(`api_helpers.go` 的 active-member 解析回 404)。
  - **為什麼放在每次請求、不是簽發時綁**:機器 token TTL 以天計(`defaultMachineTTLDays`)、成員上看 400 天,而且早就在 server 連不到的主機上了——**沒有東西可以「取消簽名」**。已發出的 bearer token 要撤銷,本質上就是使用時查一次;唯一的選擇是查在哪,答案是既有的撤銷 seam(`requireAuth`,改密碼的 owner iat floor 隔壁)。代價誠實寫下:每個 gated 請求多一次 member 讀(warden 一次、agent 兩次)。
  - **兩條臂,因為機器的憑證有兩個持有者**:(a) 機器自己——warden token 的 `machine_id` 是**空的**(onboard 明寫「a warden carries NO self-binding」),所以這臂只能認 `sub`;(b) 跑在那台機器上的 agent/worker——boot token 的 `machine_id` 就是開機主機。(b) 有一道**誤傷守衛**:caller 自己的 row 若已指向**別台**機器(名冊已經把它搬走了),就不撤銷——只是舊 token 還指著屍體。
  - 🔴 **`kind=="warden"` 這個限制是承重牆,不是偷懶**:`RosterStatusRemoved` **也**是「外包 worker 已釋放」(`dal_tasks.go ReleaseWorkersForTask`)與「成員已解僱」的寫法,而 §6.3 close-out 是**刻意**留著已釋放 worker 的 session 讓它回寫 learnings + `report_task_closeout`。只認 roster status 不看 kind = 全 fleet 的 close-out 一起默默死掉。哨兵 `TestMachineRevocationSparesEveryLegitimateCaller` 用一個**真的已釋放 worker 打真的 closeout 端點**釘住這件事(mutant:拿掉 kind 判斷 → 該臂紅)。
  - **查不到不等於被撤銷**:lookup 出錯一律**不**撤銷(與 `resolvePrincipal` 的 deny-by-default 刻意相反)——拒發能力查失敗只賠一個 403,撤銷查失敗會把一次 DB 抖動變成全 fleet 憑證斷線,那正是本 repo 記過的事故形狀。
  - 🔴 **server-self 的 teardown-here 補上與 delete 同一道 409(同批)**:`DELETE /api/machines` 早就對 `ServerSelfHost` 409,`POST /api/machines/{id}/teardown-here` **沒有**,而它 exit 0 就寫 removed —— 本次改動把那個結果從「機器離線」升級成「憑證死掉」,連帶撤銷所有 `desired_machine_id == ServerSelfHost` 的 agent(dbseed 的**預設** placement)。**提高後果的人負責把守衛補齊**,否則代價留給下一個按下去的人。訊息抽成共用常數 `serverSelfUndeletableMsg`,兩個動詞不可能再各說各話;守衛在 subprocess **之前**(先拆掉 daemon 再回 409 比沒守衛更糟)。**查證過這不是擋掉正當流程**:座艙沒有任何元件呼叫 `teardownOnServer`(只有 MCP 工具驅動);teardown-here 對**任何**機器本來就是單程票(寫 removed 後 `resolveMachine` 只認 active ⇒ `bootstrap-here`/`boot-command` 一律 404、`GET /api/machines` 也濾掉該列),所以「拆掉再重建 server host」根本不存在回程,不可能是既有的遷移流程;重啟也救不回(`seedOutOfBox` 只在 `GetMember == nil` 時補種,soft-deleted 列不是 nil);實例除役 / e2e 重建走的是 **shell 的 `ocwarden teardown`**(`oc_teardown_bounded`),不經這條端點,namespaced 實例清理不受影響;conformance 只對未知 machine id 打這條(404)。
  - 🔴 **T-42a0:`teardown-here` 的 `{machine_id}` 從來就不是目標選擇器,現在指名別台一律 409**(owner 2026-07-27 選項 [0]「行為跟名字對齊」)。`runWardenTeardownHere` 組出來的 argv+env **只**被 HOME / uid / `OC_NAMESPACE` 定址——這條路上根本沒有機器選擇器,handler 也從沒傳過。所以 `POST /api/machines/m-box/teardown-here` 實際拆的是**本機這顆 warden**,然後把 `RosterStatusRemoved` 寫到 **m-box** 頭上:一次點擊,本機 daemon 被 launchd bootout、一台從沒被連絡過的機器掉出名冊(T-9cf8 之後還連帶撤銷它的憑證)。名字承諾了一個實作碰不到的目標。拒絕訊息 = `teardownHereForeignTargetMsg`,**明講兩條正路**(別台退役走 `uninstall_machine` → `delete_machine`;修本機 warden 走 `install_warden_on_server_host` 的 `install --force`),**刻意不提供任何旁路**——沒有 flag、沒有 query 參數能讓這個動詞跨主機。守衛在 subprocess **之前**、在 `resolveMachine` **之後**(未知 id 仍是 404,conformance `test_auth_matrix.py` 的 `m-conf-missing` 只打這一面,不受影響)。
    - ⚠️ **後果寫下來,不要等下一個人發現**:加上上一條的 server-self 409,**這條端點現在對任何目標都拒絕**,handler 裡的 subprocess + CONFIRM-THEN-REMOVE 因此**經 HTTP 不可達**。刻意留著而不是連端點一起退役:在凍結 wire 上退役一條路由是 owner 裁定,不是修缺陷的副作用;`runWardenTeardownHere` 自己仍有直測(T-2257 的 `--canonical`/`OC_NAMESPACE` argv 契約)。**要不要退役這條端點是待 owner 裁定的獨立問題。**
    - 🔴 **兩支主線測試原本把這個錯誤行為當契約釘住,已改**:`TestHandleTeardownHere_ExplicitTargetContract`(明文斷言「argv 不帶 machine id」**且**「被指名的 m-box 要被標成 removed」——兩半都是真的、兩半合起來就是缺陷)搬成 `TestTeardownHere_CoreStillSpellsItsOwnTarget`,直接驅動 core;`TestTeardownHereStillWorksForAnOrdinaryMachine` 的理由寫的是「這是把機器移出這台主機的**唯一可用途徑**」——**那個前提是假的,它守的正是缺陷本身**,已反向改寫成 `TestTeardownHereRefusesAnOrdinaryMachineToo`。
    - 🔴 **測試不得靠它正在測的守衛保護自己**:驗證守衛的唯一誠實方法就是把它拿掉再跑,而那一刻 handler 會走到 subprocess。所以 `execOcwarden` 這個系統操作在**測試 binary 裡根本沒接上**——`TestMain`(update_check_test.go)把 `runOcwarden` 預設換成 `refuseToExecOcwarden`(跑什麼都不做、回 exit 126 + 讀得懂的訊息),個別測試再各自 bind `withRecordedOcwarden` 讓「零次呼叫」可觀測。**測試檔對 `execOcwarden` 的直接呼叫次數 = 0**(全樹只有 `api_machines.go` 提到它)。哨兵 `TestTeardownHere_TestBinaryNeverWiresTheRealExec`(mutant:拿掉 TestMain 那一行 → 紅,實測)。
    - 🔴 **兩道拒絕整併成一個函式 `teardownHereRefusal`,因為原本那個寫法有一個「可證恆真的死條件」**(獨立審查 M2 抓到):兩道連續 `if` 之下,第二個 `machine.ID != ServerSelfHost` 在**第一個已經 return** 之後**必然為真**——審查者把它改成 `if true` **全套件全綠**,沒有任何測試分辨得出來。**一個沒有鑑別力的條件比沒有條件更糟**:它讀起來像第二層保護,而那層保護不存在。現在 handler 只有**一個**閘(`if refusal := teardownHereRefusal(machine.ID); refusal != ""`),函式內那個 `if` 是**真的二選一**(給哪一句),兩個方向各自有測試釘住。⚠️ **為什麼不乾脆寫成無條件 `writeError`+`return`**:那會讓底下的 subprocess 段變成 `go vet` 的 `unreachable code`(已實測 vet 會紅),CI 直接擋。`""` 回傳值刻意留著並寫明理由:owner 哪天裁定端點去留,這個函式就是唯一要改的地方。
    - **mutant 實測(2026-07-28 重測,每顆前 `go clean -testcache`)**:`teardownHereRefusal` 的非 self 臂改成回 `""` → **3 條紅**(`TestTeardownHere_NamingAnotherMachineIsRefused`、`TestTeardownHere_RefusalPointsAtTheRealPathAndOffersNoBypass`、`TestTeardownHereRefusesAnOrdinaryMachineToo`),`TestTeardownHere_ServerLocalRefusalIsUnchanged` 與 T-2257 core 兩臂維持綠;把內層條件**恆真**(自我永遠拿到 foreign 訊息)→ 紅;**恆假**(別台拿到 self 訊息)→ 紅。**死條件已不存在**。
    - **MCP 工具說明同批修正**(§9c:動到 agent 互動面就要同批更新 seeds):`spec/mcp-catalog.json` 的 `uninstall_warden_on_server_host` 原文是「tear **this machine's** warden down on the host」——**照字面用就會踩到這個缺陷**,已改成講明 machine_id 不是目標、兩種目標都會 409、以及兩條正路;`seeds/role_def_assistant.md` 那句「拆機這個動詞是給**別台**機器退役用的」是同一個誤解的另一份拷貝,已改寫。
    - **同一句誤導文字的全部副本都已消滅(獨立審查 B1/B3/B4 必修,已補)**——這條原本寫的是「`spec/openapi.json` 的 operation description 仍是舊敘述(未改),列為未完項」,而**那句話在同一批的第二顆 commit 之後就是假的**(審查者當場推翻)。這一批對自己的狀態說謊,正是它自己在修的那種「錯的敘述會長腳」。現況(逐份查證):`spec/mcp-catalog.json` description ✅、`spec/openapi.json` 的 **description ✅ 與 `.summary` ✅**、`server/ocserverd/routes.go` 的 `Summary` ✅(該欄無其他消費者,只餵 route 表文件面 + 生成碼註解)、`cli/CLAUDE.md` 的兩處 ✅、`seeds/role_def_assistant.md` ✅。**成本評估也更正**:改 description **只**需重生 FE `schema.ts`(`ocapi_gen.go` 不內嵌 operation description);改 **`.summary`** 才會連帶動到 `ocapi_gen.go`(它進生成碼註解)。兩份生成物都由產生器重跑,無手改。
  - ⚠️ **解除安裝(`POST /api/machines/{id}/uninstall`)不撤銷任何東西**:它**不寫** roster removed(只翻 desired_state,warden 斷線後由 reconcile 消化回 offline),機器仍在名冊上、可再安裝。**teardown-here 確認成功(exit 0)才寫 removed**,所以那條會撤銷。座艙文案照這個差異分開寫(`frontend/src/i18n/locales/*` 的 `monitor.machine.deleteConfirmBody` 講出憑證失效/不可復原,解除安裝文案不動——加一句它做不到的承諾就是同一種說謊)。
- **殭屍 SSE gate(zombie 防線 B,api_infra.go `sseStopGateRefusal`)**:`GET /api/events` 在 hub 註冊前多一道 pre-stream 409 擋門——member「stop 生效中」(desired_state=offline ∧ stopping_since/stopped_since 有錨,deactivate / force-stop 必stamp)或 roster removed 時,拒絕重連;殭屍 listener 因此永遠無法把死 agent 重新算成 online(reconcile 不再被假 online 鎖死)。窄化設計:剛 hire 未 activate(desired offline、無錨)照常連;warden 豁免 desired-offline 臂(warden 預設就 offline,removal 走 uninstall 一次性意圖);既存活連線不受影響(winddown nudge 仍騎原連線);activate 同一筆寫清錨+翻 desired,gate 原子解除。選「拒絕」而非「連上但不算 online」:餓死殭屍 + 給 ocagent 一個權威訊號做 fail-closed 自殺(cli/ocagent listen)。行為釘在 `api_infra_test.go` + conformance test_sse.py zombie-gate 系列。
- **SSE 面已通(步驟④,M3 第四批)**:`GET /api/events` 全真——delta frame 流(`hub.go` `Publish`:五鍵 envelope、單一 process-local seq/epoch counter、closed topic gate at the seam(現 13 topics,見 spec/sse.md §3.1)、per-listener buffer 保發佈序)+ 三條定向帶(`sse_bands.go`:context-high WARN 只發該 agent 連線、warden-command FIFO 只 drain 到該 warden 連線、task-close nudge 只發該任務 executor 連線(sse.md §8,M3 Phase 6C:任務進終態(done/terminated 皆推)且有 type 時,沿 executor 自己的 SSE 推一則 write_task_learnings 提醒;ad-hoc 無手冊不推、無 executor 不推;`hub.PushDirected` best-effort at-most-once,離線即丟、不排隊);event-driven dispatch 見下方 reconcile 條——第六批把舊 `dispatchStartNow` 併入 `reconcile.go` 的共 store driver)。context-high knob 從 DB settings 讀(`ctx.*`,settings.go;oc.toml 的 `[sse_context_high]` 已退役——loader warn+ignore,首啟一次性把明寫值搬進 DB)。alias 寫入不 fan delta(spec closed set 無 alias topic)。

- **ocwarden 子進程 env = fail-closed allowlist(T-5047;`api_machines.go`)**:`bootstrap-here` / `teardown-here` 把 `ocwarden` 當**子進程**跑,而子進程的 env 就是「裝/拆哪一個實例」的決定權(root、launchd label、tokfile、身分,甚至做不做事)。原本是把 `os.Environ()` **整包**傳下去、只刮掉一個 `OC_ID`——長度為 1 的 denylist。serve 進程身上任何雜散的 `OC_NAMESPACE` 會讓 install(或更糟,teardown)打到**別的實例的 warden**;`WARDEN_INSTALL_DRYRUN=1` 會讓 server 回報「安裝成功」但什麼都沒裝;`OC_AGENT_BIN` 會把裝進去的 ocagent 換成任意本機檔案。現改為 **allowlist `ocwardenChildEnvAllowlist`**(`HOME`/`PATH`/`OC_CLAUDE_BIN`/`OC_CODEX_BIN`/`OC_CLAUDE_CRED_CHECK`,每條都有寫明的中繼理由),過濾函數 `ocwardenChildEnv` 兩個動詞共用;server 自己要設的 `OC_BASE`/`OC_TOKEN`/`OC_NAMESPACE` 在過濾**之後**才 append,所以永遠不會被繼承來的同名值遮蔽。⚠️ **選 allowlist 而非 denylist 的理由**:denylist 只對「有人想到的變數」正確,而 child(cli/ocwarden)與 parent(這裡)是**兩個沒有編譯器相連的 module**——那份單鍵 denylist 寫下時 `OC_NAMESPACE` 還不存在,namespacing 上線那天它就已經過期而沒有任何人會發現。allowlist 的失效方向相反:child 新依賴的變數是**缺席**,第一次跑就吵著失敗,而不是安靜地裝到別的地方去。
  - **接線本身也有測試**:`runOcwarden` 是可注入的 seam(production 綁 `execOcwarden`),`api_machines_childenv_wiring_test.go` 直接驅動兩個動詞、從 seam 上讀真正會交給 `exec.Cmd.Env` 的那個 `[]string`。這條是**刻意補的**:原本 allowlist 只被當純函數測,把兩個 handler 的呼叫改回 `env := os.Environ()`(原始 defect 一字不差)整套 server 測試依然全綠——**沒有人證明有人呼叫的過濾函數不是防線**。teardown 那半併抽成 `runWardenTeardownHere`,與 `runWardenInstallHere` 對稱,兩者都不必經 HTTP recorder / embed bindist 就能被驅動。
- 🔴 **first-run onboarding 的「認領要同步」是與座艙的成對契約(T-8115)**:`kickFirstRunOnboardingWith` 在**開 goroutine 之前**就把 `running` 報告寫進 DB(`putOnboardingReport`),所以那一列在 set-password handler **return 之前**就存在——而 net/http 對這種小回應是 handler return 才 flush,因此**任何拿到那個 200 的 client,再讀 settings 一定看得到報告**。它其餘四條 early return(`dal==nil` / `OC_NO_ONBOARDING=1` / `GetSetting` 出錯 / 認領寫入失敗)代表這次 onboarding **根本不會跑**,而且**不會重試**(set-password 是一次性的)。兩者合起來 = **client 看得到的 `onboarding: null` 永遠是「從來沒跑過」那一種**。座艙就是靠這條把 null 當終態、不再輪詢(`frontend/src/components/OnboardingBanner.tsx` 的 `isTerminal`);把認領搬進 goroutine、或搬到 handler return 之後,**座艙那條規則就變成謊話,而首次安裝那則 ~30s 的失敗結論從此沒人看得到**。護欄:`onboarding_contract_test.go` 的 `TestOnboardingClaimIsPersistedBeforeKickReturns`(擋住認領非同步化)與 `TestSetPasswordLeavesNoNullOnboardingWindow`(擋住 set-password 不再 kick——實測**整套 ocserverd 只有這一條**會為此變紅)。**改任一側都要看另一側。**
- **namespace 導出是跨 module 手抄鏡像**:`onboarding.go` 的 `wardenLaunchdLabel`/`officraftRootPath`/`wardenTokfilePath` 是 `cli/ocwarden/namespace.go` 的手抄複本(兩個 go module,無 import、無編譯器)。它算出來的 label 就是「本機到底有沒有 warden」這道安全連鎖的答案,差一個字元 = 對有 warden 的機器回答「沒有」= 在活的 job 上再裝一個。兩側各自對同一張表 `bin/tests/fixtures/namespace-axes.tsv` 對質(`onboarding_mirror_test.go`),漂掉時紅的是漂掉那一側。
- **reconcile producer 已入(步驟⑥,M3 第六批;`reconcile.go`)**:對 spec/lifecycle.md §4——30s cadence tick(sleep-then-tick,unless `--no-reconcile`)+ 純決策核 `reconcileDecide`(desired×observed→START/STOP/UNINSTALL;timers 照 §4.4:start_timeout 90s / stop_grace 120s / stop_retry 90s / recycle_grace 120s / backoff 5·300s / circuit 5·120s;silent-timeout 只 backoff 不累 breaker)+ in-memory reconcile store(restart amnesia=契約,單一 mutex 序列化 cadence 與 event-driven tick,冪等防重複 dispatch)+ 四個 pre-decide roster pass(context-high auto-recycle stamp / recycle loop-break / stale-stopping clear / offline-warden uninstall 意圖消化)+ dispatch 紀律 §4.6(fail-closed START fold+mint、target-reachability gate、不 accepted 就保留 prior state 下 tick 重試)。event-driven seam:activate/deactivate/machine-uninstall → `reconcileMemberNow`(共 store,cadence 是冪等 backstop;舊 `dispatchStartNow` 已併入退役);force-stop / 首次 stopped-report(refocus-marked)→ `dispatchRobustStopNow`(raw dispatch,繞過 grace 與 cadence)。**`--no-reconcile` 是真 kill-switch**(lifecycle.md 附錄 B #1 的 shadow 部署必備):cadence + 全部 event-driven warden-command dispatch 一併關掉,server 其餘照常;conformance/測試隔離可用(harness 現況跑 always-on)。**uninstall 意圖一次性(owner 定案 2026-07-11,lifecycle.md §4.3)**:warden 觀察到真斷線即消化 `desired_state="uninstall"`→offline(SSE disconnect edge `consumeUninstallOnDisconnect` 事件驅動 + tick pass backstop);重裝路徑(boot-command 重取 / bootstrap-here)先歸零殘留意圖再裝(`clearResidualUninstall`);機器 uninstall / delete 的 409 擋門只計實際線上 agent(`hub.AgentsOnMachine`),離線綁定不擋。
- **migrations/ 已載真 schema**:`00001_schema.sql`(9 表,single-owner 化:無 owner 表/owner_id/scoped key/schema_version 欄)+ `00002_settings.sql`(key-value settings)+ `00003_reply_card.sql`(回覆卡)+ `00004_tasks.sql`(M3 任務五表 + reply_card gate 連結欄)+ `00005_task_closeout.sql`(task.closeout_ts——§6.3 close-out 回報戳,0.0=未回報)+ `00006_last_op_reason.sql`(member.last_op_reason——warden command_result 的結構化失敗原因 `<code>: <detail>` 一行摘要,與自由文 last_op_log 分開落庫;''=receipt 未帶原因(舊 warden/成功 op),FE 照舊只顯狀態。fold 在 api_monitoring.go `foldCommandResult`,wire 露出 = MemberDTO.last_op_reason;warden 端 closed code set 見 cli/ocwarden/spawn.go SpawnOutcome.Reason),`dal.go` / `dal_tasks.go` 是存取層;migrate 目前 sqlite-only(serve 亦只吃 sqlite DSN)。
- **回覆卡(等我回覆卡)面已入(M2 reply-card B1;`api_replycards.go`)**:agent 開卡請示 owner(kind=decision|action、options 1..4 且 [0]=AI 建議),開卡同時發一則普通 chat 訊息(initiator→owner,`meta.reply_card_id`)讓請示騎在聊天流上(未讀紅點/永久歷史免費拿),卡的 `chat_message_id` 反向連回(跳原訊息錨點)。狀態機閉死(T-1aa4 起三態、閉集只在 code——migration 00013 比照 00011 rebuild 掉 DB CHECK、加 `expired_ts`):waiting --(POST answer,唯一**正面**關卡途徑)--> answered --(PUT answer=重新決定,換答案不變態)--> answered;waiting --(POST `/api/reply-cards/{id}/expire`,治理層標為過期,**不是回答**、無 body、終態不可復原)--> expired;**無 close/skip/reopen 路由 = by construction**,兩個出口都是治理層動作(owner 或 admin 助理;一般 agent 仍無任何自關卡的路)。expire 走與首答同一個 hold 釋放 seam(`releaseCardHold`,原 resumeTaskStepAfterCardAnswered 抽共用):綁定節點/任務退回 in_progress、防呆全繼承;**task 已終態的 orphan 卡(T-f571 的死角:answer 409)唯一出口就是 expire**——關閉任務不動、不 bump UpdatedTS。answer/PUT 對 expired 卡 409;list 面加 `?status=expired` pane(24h 窗 keyed expired_ts、新在前——ocagent drain 的離線補課 pane)、count DTO 加 `expired`。answer/expire 三條路由 T-6020 起 `requires=admin_agent` + MCP 工具(`answer_reply_card`/`reanswer_reply_card`/`expire_reply_card`)——owner 與 admin 助理能回覆/標過期,**開卡的一般 agent 仍關不了自己的卡**;答案附件重用 chat attachment 機制的 decode/上限/blob store/serve 路由——⚠️ **但不是「整套」:答案面只吃 inline `data_b64`,從來沒有解析過 `{id}` ref**(T-e2b2 實測)。T-e2b2 起這件事至少不再是無聲的:答案面帶 `{id}` 的項目回 400 並講明這一面不收 ref(要不要補上 ref 支援是另一個 owner 問題)。SSE:`reply_card` topic(closed set 第 9 個;create/answer/revision 全 op patch,payload {id,from,status} 只帶部分欄位)——agent 自己的 SSE 連線收到 delta 後用 `get_reply_card` 拉全卡脈絡+答案。近期已回覆 pane = answered_ts 24h 窗(revision 重新入窗);徽章 = waiting 數(`GET /api/reply-cards/count`,mcp_exclude)。**list 面輕量化(T-3f31 owner 定案:卡只需要 title+決策)**:`GET /api/reply-cards` 出 `replyCardListItemDTO` 輕量摘要——summary+狀態+時間戳+task ref+決策 digest(`replyCardAnswerBriefDTO`:option_idx+選中選項**原文**、answer text 截 200 rune 預覽、附件為 COUNT),**不帶 body/options 全文/chat_message_id/附件 refs**(全卡走 `GET /api/reply-cards/{id}`);`?limit=N` 在 pane 排序後截前 N(0/負數/缺省=全量)。**`?view=full`(T-a3e4,owner 2026-08-02 核准)= 同一個 pane、同一批列、同一個順序,但回全卡**:由**同一支** `replyCardDTOOf`(`GET /api/reply-cards/{card_id}` 用的那支)逐列建出,所以一列**逐位元組**等於那張卡自己的單卡回應。理由:座艙的 pane 與聊天串內嵌卡都渲染全卡,輕量列因此逼 http adapter 在 list 之後**逐列**再打一次 `GET /api/reply-cards/{id}` ⇒ 開一個 pane 要付**一張 waiting 卡一個往返**。🔴 **價值在往返次數、不在位元組**(真 ocserverd 實測 waiting pane:26 請求 / 49,970 B → 1 請求 / 44,183 B = 少 25 個 RTT、位元組只少 11.6%),**別講成省流量**。**預設(不帶參數)與 `?view=light` 逐位元組等於改動前**;不認得的值回 **400 並點名兩個合法值**——默默落回 light 會讓一個打錯的字**無聲**恢復逐列 fan-out(刻意偏離 `?view=list` / `?fields=light` 兩個先例的靜默落回,owner 知情未反對)。🔴 **`view` 刻意不在 `list_reply_cards` 的 MCP inputSchema 上**:登記在 `spec_catalog_conformance_test.go` 的 **`deliberatelyOffMCP`**(**不是** `knownCatalogDrift`——那份是「追溯過、確認漏掉」的債,填錯欄會招來下一個人把它廣告出去)。輕量列就是 owner 裁定的 agent 契約(T-3f31),給 agent 一個一次拉整個 pane 全卡的把手等於把 T-3f31 縮掉的還回去;agent 拿全卡的路仍是 `get_reply_card`,一次一張、刻意選擇。**seeds 因此不需要同批改**(它教的「列表只給標題＋決策要點、全文 `get_reply_card`」對 agent 仍逐字為真)。哨兵 `api_replycards_viewfull_test.go`:full 列 ≡ 單卡回應(⚠️ **語料裡必須有一張綁任務的卡**——`replyCardDTOOf` 相對於裸 `newReplyCardDTO` 唯一多做的就是 task join,全 unbound 的語料下把 builder 換掉**一條都不會紅**,實測過)、light 列仍不帶 body/options、pane 順序與 `?limit=` 不變、預設投影不變、未知值 400 帶陽性對照;conformance `test_view_full_serves_the_same_pane_as_whole_cards` / `test_view_defaults_to_light_and_rejects_anything_else` / `test_view_is_not_advertised_to_agents`(對**線上** tools/list,不是對凍結檔)。**回應 schema 因此是聯集**(`ReplyCardListItemDTO[]` | `ReplyCardDTO[]`,light 排第一),代價是 `ocapi_gen.go` 多 88 行**零 caller** 的 union wrapper(該檔第一個 union 構造;handler 一律手寫 DTO 走 `writeJSON`,所以那組型別是死的)——owner 在知情下選了「規格誠實」這一邊,對照選項是照 `?view=list` 的先例**完全不宣告**輕量形狀(那條至今如此,`TaskManualListItemDTO` 在 openapi.json 裡根本不存在)。🔴 **同一個「整張卡」承諾現在也涵蓋三個寫入動詞,而且有客戶端後果**:answer / reanswer / expire 都以 `writeReplyCard` 收尾,所以回應**逐位元組**等於那張卡自己的 `GET /api/reply-cards/{card_id}`。座艙的動作路徑**採用這個回應**當唯一的自我對帳(`frontend/src/hooks/useReplyCards.tsx` 的 `adoptWrite`——`reply_card` SSE frame 斷掉或漏掉時,那張卡仍要離開等待面),所以**把任一個寫入動詞改成回輕量列,會讓座艙把畫面上的全卡換成半張卡**,而 client 側測不出來(`api/mock` 構造上就回全卡)。哨兵 `api_replycards_writeecho_test.go`(三個動詞各一條)。🔴 **兩半各自抓不同的東西,別把功勞算錯(本檔上一版算反了)**:單卡 GET **也**以同一支 `writeReplyCard` 收尾,所以動那個共用 tail 會讓 echo 與 GET **一起移動**、`got != want` **永遠不可能響**——「讓 tail 對終態卡回輕量列 → 3 條全紅」那顆 mutant 是靠**反恆真的欄位檢查**(body + options + 綁任務的卡)抓到的,實測把那三行刪掉、同一顆 mutant **完全變綠**。identity 的射程是**另一種**形狀:**寫入側單方面漂掉**(只把三個動詞換成裸 `newReplyCardDTO`、GET 不動 → answer 與 reanswer 兩條的 `got != want` 紅),那才是現實中會發生的「有人順手優化其中一個動詞」。**兩半都要,而且語料必須有 body + options + 綁任務的卡**,否則兩個投影長得一樣、比較等於沒比。
- **任務系統 server 面已入(M3 任務批 Phase 1)**:`migrations/00004_tasks.sql`(task / task_dep / task_step / outsource_worker / task_manual 五表 + reply_card ALTER 加 task_id/task_step_id gate 連結)、`dal_tasks.go`(五表顯式 CRUD;ReplaceTaskPlan 保留終態節點(done/superseded)+ 依 handler 名單凍結/續用已答卡節點(T-1aea)、ReleaseWorkersForTask 終態解僱)、`domain.go` 任務段(六態/四級/step 五態閉集(+`superseded` 終態,T-1aea:submit_plan 唯一寫入點,`StepIsTerminal` helper)、agent 回報狀態機轉換表(非法→409)、task_no `T-<id[2:6]>` 推導、codename O/S/H+MAX+1 派生(Phase 2 排程器消費)、dedupe key 派生)、handlers `api_tasks.go`(C.1 讀面 + C.2 治理層動作(terminate/message 代發 chat 帶 task meta——T-6020 起 `requires=admin_agent` + MCP 工具 `terminate_task`/`post_task_message`;priority 仍 `requires=agent` 走執行者守衛)+ C.3 agent 寫面:create 去重(非終態撞鍵 200+deduped:true、終態撞鍵造新)、submit_plan 整份替換未保留節點(done 照舊保留;**T-1aea**:最新綁卡已 answered/expired 的節點凍結成 `superseded` 終態(finished_ts=凍結時刻、卡指標留存),新 plan 同名重列則原樣續用不產生副本;waiting 卡節點照舊移除、releaseCardHold guard 使後續答卡安全 no-op;superseded 不計 TaskProgress/AllTaskStepProgress、不可 open_gate 重綁(409)、非 agent 可報(400);migration 00016 rebuild 掉 task_step status CHECK,閉集移入 code)、update status/step 走狀態機、open_gate = 重用 reply-card create 機制 + step/task 綁定(waiting_owner 唯一入口;答卡後伺服器**不**代推——agent 回報原則)、set_task_deps 全量替換、get_my_task 外包領任務(identity-locked,首呼標 active))/`api_taskmanuals.go`(**手冊作者權分面(owner 定案 2026-07-13)**:內容欄 agent 級可寫——create 空手冊 + partial edit purpose/fields/sop_md/learnings(MCP `create_task_manual`/`update_task_manual`);**assignee 面是治理面**——T-6020(owner 2026-07-26)把 in-handler gate 的門檻從 owner 降到 `principalAdminAgent`(`callerMaySetAssignee`),所以 owner 與 admin 助理帶 `assignee` 都會被驗證後套用、**一般 agent 帶了才是 403**(常數改名 `assigneeGovernanceMsg`,免得訊息與門檻兩份說法);delete 同批降到 `requires=admin_agent`(MCP 工具 `delete_task_manual`);agent learnings whole-doc replace 照舊、刪除防呼:有非終態 task → 409)/`api_outsource.go`(外包面板 read)。**Requires: agent 首用**(routes.go 20 新列;warden 被 agent 級擋在外;執行者守衛 = caller==executor、admin capability 豁免——§14)。外包 worker **不建 member row**(未知 sub 保底 agent 級);聊天沿用既有 chat 面零改動(worker id 即 chat peer)。settings 加 `task.outsource_max_parallel`(int 預設 3,PATCH -1..20;-1=無限、0=暫停指派)。SSE 加 `task`/`outsource_worker`/`task_manual` 三 topic(sse.md §3.1 13-topic 閉集)。**§6.2/§6.3 批已入**:(a) resume-summary 帶任務段(`api_tasks.go` resumeTasksFor:caller 為 executor 的非終態任務,最多 5 張、依 updated_ts 新→舊;**T-3f31 輕量化(owner 定案:任務不該包含細節)**:每張只出 task_no/標題/type/狀態/優先權/waiting_reason/當前節點 id+**名稱**(第一個非終態 step,superseded 紀錄跳過)/進度/`detail_chars`(省略掉的計畫文字 rune 數 = Σ step name+DoD)/updated_ts,**不帶 steps/DoD 全文、gate 狀態、executor/dedupe 欄**(細節 get_task 拉);回應另帶 `overview` size/概要塊(`resumeOverviewDTO`:chat_count、tasks_returned/tasks_open_total(`CountOpenTasksByExecutor`)/tasks_detail_chars、caller 自己的 cards_waiting/cards_answered_recent 24h 窗、T-1b09 roster_chars/machines_chars)——peek-then-decide:agent 先看大小再決定 get_task 自己拉還是丟 sub-agent;wake payload 有界,其餘 list_tasks/get_task 翻頁);(b) `POST /api/tasks/{id}/closeout`(MCP `report_task_closeout`,requires=agent+執行者守衛):負責人回報「收尾事項已處理完」——僅終態(開放中 409)、冪等(首報 stamp closeout_ts+發 task delta,重報 200 no-op);**外包解僱掛鉤已接**(首報成功路徑呼 `dismissOutsourceWorkersForTask`——Phase 6 hook,見下方喚醒/解僱條);task-close nudge(sse_bands.go)文字同步教三步收尾(learnings→清暫存→report_task_closeout)。
- **外包排程器已入(M3 任務批 Phase 2;`outsource_sched.go`)**:第二個背景 producer,同構 reconcile——30s cadence(sleep-then-tick)+ 事件驅動單筆 tick(create_task 落庫外包任務即 `outsourceTickNow`,不等 cadence)+ `--no-outsource` serve flag(比照 --no-reconcile 的 wholesale kill-switch:cadence 與事件 tick 一併關)。純決策核 `outsourceDecide`(候選佇列×手冊 assignee spec×live worker 計數×全域上限 → 指派清單;priority high→mid→low + created_ts FIFO、per-type copies 上限(H6:N 個並行任務、一 worker 一任務;**copies=0 = 無限**,不設 per-type 上限——spec TaskManualDTO,2026-07-13)、全域 `task.outsource_max_parallel`(**-1=無限、0=暫停指派**;成員任務不計——H7)、frozen 跳過、同 call 冪等)。manual 外包 assignee 另帶 `machine`(**具體 machine id**)placement 偏好——排程器只解析進 spec(outsourceTypeSpec.Machine)、**admission 不消費**;實際 placement(**無自動挑機、無離線 fallback**)由 worker_spawn.go 在 spawn-time 消費(見下條)。**`"auto"` 已廢(owner 2026-07-25)**:它從來不是 warden id,`IsOnline("auto")` 恆 false,所以 auto placement 是個 reconcile 永遠治不好的死路——現在**每個寫入面同一條規則:非空的 machine id 一律要解析到真機器**(成員 relocate、成員 **activate**——同一呼叫把 desired_state 翻 online,不擋就等於造出一個永遠派不出去的成員、外包 worker relocate、手冊 assignee(shape 檢查 + 真 resolve)、create/reassign 的 `target.machine`),`""` 才是「這行沒選機器」;migration 00035 把 member.desired_machine_id / task.outsource_machine / task_manual assignee `$.machine` 的庫存 `'auto'` 正規化成 `''`。**發包 target 的缺席欄 = 繼承**(`api_tasks.go inheritDispatchSpec`,owner 2026-07-25 立、**2026-07-26 補齊**):create_task / reassign_task 帶 `target.kind=outsource` 時,**沒寫的** runtime/model/effort/machine 從**單一**來源補。owner 2026-07-26 原話:「如果是有手冊的話看手冊上指定的,如果是自由指派那種不是由手冊來的,但是發派對象是外包的話,那就以該 agent 本身配置一樣下去開外包。」⚠️ **這是兩個 case、不是一條優先序**——兩者永遠不會同時競爭同一筆:**(A) 有手冊(typed)→ 走手冊 assignee**(手冊是該任務類型的 owner 設定,誰按 發包 都不該覆蓋它);**(B) 沒手冊(ad-hoc/自由代辦)且發派對象是外包 → 走發包 agent 自己那行**(它的 runtime/model/effort + 它 pin 的機器,「發一個像我這樣的」;T-0d32 的「自由代辦外包完整繼承委派者設定」是同一契約的獨立表述)。碼上 `manualSpec` **只有**在 task 帶 type_key、該手冊存在、且 assignee kind=outsource 時才非 nil(`outsourceSpecOf` 其餘一律回 nil),所以 switch 的第二臂**恰好**就是「沒有手冊可讀」的情形——這就是 (A)/(B) 本身,不是誰壓過誰。⚠️ **把它讀成優先序已經出過一次事**:`fb8981c` 想讓 ad-hoc 繼承發包者(**碼本來就是這樣**),卻寫成全域反轉——對 ad-hoc 零改變、把每一筆 typed 發包弄壞,留下兩個紅測試,而且沒同批更新本檔(§8)。教訓:動任一臂前先確認另一臂缺席時它是不是已經對了;兩臂**分開釘**(`TestInheritDispatchSpec_TypedTakesTheManual` / `_AdHocTakesTheDispatcher` / `_SharedRules` + 端到端 `TestCreateTypedTaskDispatchInheritsTheManual` / `TestCreateAdHocTaskDispatchInheritsTheDispatcher`),一個 mutant 只會點名一臂。共通規則(不屬於任一臂):runtime+model **綁在一起繼承**(來源沒明講 runtime、或講的不是這次要派的 runtime,就不吃它的 model、改用該 runtime 預設,免得 codex model 餵進 claude boot)、effort 沒填就預設 `medium`、**machine 沒有預設值可退**(沒人指名 = 沒 placement = 不啟動)。解析結果**落在 task 行**(`outsource_runtime/model/effort/machine`),handover / 重生時重讀、不重推導。

  🟥 **「手冊機器欄空白」的唯一表述(owner 2026-07-26 深夜拍板,採讀法 A;本段是這條規則的唯一版本,別再從 7/25 與 7/26 兩次裁定各讀一份)**:手冊 assignee 的機器欄**空白 = 沒有陳述**(不是「刻意不指定」),所以往下由**建立者自己 pin 的那台機器**補。⚠️ **同時,7/25 那條絕對規則一字未改**:**沒人指名機器 = 沒 placement = 不啟動,沒有最終 fallback**。兩者**不衝突**,理由要記住:**建立者自己 pin 的機器本來就是一個明確的人為 placement 決定**(某個人替那個 agent 選過那台機器),所以消費它並沒有「啟動一台沒人選過的機器」——被禁止的是**伺服器自己憑空挑一台**,不是沿用一個已經有人做過的決定。空白手冊欄之所以能往下走,正因為下一層不是猜測、而是另一個人為決定;真的**沒有任何人**指名過(建立者也沒 pin)時,machine 依舊留空、依舊不啟動。⚠️ **這條規則曾被讀成兩種意思,而且「讀成優先序」已經出過一次事故**——就是上一段的 `fb8981c`(想讓 ad-hoc 繼承發包者、碼本來就是那樣,卻寫成全域反轉:對 ad-hoc 零改變、把每一筆 typed 發包弄壞,留下兩個紅測試且沒同批更新本檔)。**那段歷史刻意留著:它就是本段文字存在的理由。** 動這裡之前先把它讀完。⚠️ **T-8a67 兩處補正(2026-07-26,同一條「沒指定就照建立者」裁示的兩半)**:(a) **手冊的沉默不是決定**——兩臂改成**兩道 pass**(`fillDispatchSpecFrom` × 2 + `defaultedDispatchSpec` 收尾),手冊講了的欄照舊由手冊定(所以 (A) 沒變:dispatcher pass 跑到時那些欄已經滿了、永不競爭),**手冊沒講的欄落到建立者身上**。修的是:typed 發包碰上 assignee 沒填 machine 時,以前解析出「完全沒有機器」,而沒人會再指名 ⇒ 該任務建出來的 worker **終身開不起來**。(b) **手冊派工(manual-driven,根本沒帶 `target`)以前完全不繼承、task 行四欄全空**——現在也走同一條解析並落庫。擋住這件事的原本是**「四欄非空 ⇒ 這是顯式發包」這個推論**(寫了就會讓手冊派工冒充發包、繞掉排程器那道 by-creator 的 spawn gate),因此 **migration 00036 加 `task.outsource_dispatched` 把判別寫明**(backfill 完全複製舊推論,in-flight 列分類不變),`hasExplicitTarget()` 改讀該欄。兩種語意從此分開:**dispatched=1** = 顯式 target、**權威**、蓋過手冊、跳 gate;**dispatched=0** = 建立者**快照**、**只補**活手冊沒講的欄(`fillTypeSpecFromSnapshot`,copies 永遠只聽手冊)、gate 照跑——所以手冊**沒有被 create 時凍住**,owner 事後改手冊下一 tick 就贏。⚠️ **殘留缺口(必讀,別把上面讀成全好了):T-8a67 只修好 claude 建立者的 placement;codex 建立者的 typed 手冊派工任務仍然會失敗,只是拒絕代碼從 `no_machine_selected` 換成 `machine_unavailable`。** 機制:`outsourceSpecOf` 在讀任何 assignee key **之前**就填好 `Runtime=claude`/`Effort=medium`(`outsource_sched.go`),所以 typed 手冊的**沉默與明講 claude 無法區分**,四欄裡真正會被快照補到的只有 model 與 machine;codex 建立者於是拿到 runtime=claude(其 codex model 被 runtime 耦合守衛正確丟掉)**但 machine 那一臂沒有 runtime 耦合,仍然指向他那台 codex 機器**,`resolveWorkerPlacement` 撞上 `machineSupportsRuntime`(回報過的 capability map 沒提 claude = 沒有,不是未知)就拒派。非回歸(原本也起不來),真正的修法要動 `outsourceSpecOf` 的預設值、而它另有呼叫者 ⇒ **另票 T-cd21**(待 owner 確認、未開工)。釘住它的是 `TestCreateTypedManualDrivenCodexCreatorStillFailsClosed`——這個缺口只被誠實標示,沒有被偷偷留著。附帶條件講清楚:那台機器**若沒回報 capability map、或 map 裡也有 claude**,worker 會**起得來**——只是起成一個 claude worker 待在 codex 開發者的機器上,不是「發一個像我這樣的」,只是沒被拒絕而已。**無 in-memory 帳**(有別於 reconcile store):outsource_worker 行即帳本,每 tick 從 DB 重數,restart 零遺忘、cadence 是事件 tick 的冪等 backstop。指派=建立 worker(codename 走 domain.go MAX+1 派生、永不複用;model/effort 取手冊 assignee)→ 寫 task.executor_id → 發 task+outsource_worker delta。
- **外包 worker 喚醒/解僱已入(M3 Phase 6;`worker_spawn.go`,owner 拍板「乾淨新建:warden 臨時 session 形態」)**:`notifyWorkerSpawn` 填肉——組 worker 專屬 boot context(`buildWorkerBootContext`:seeds/worker_context.md + 身分(ow-id/代號/model/effort)+ 綁定任務全文 + 手冊 Q1/Q2/Q3+學習經驗;**刻意不走成員 buildBootContext**,worker 無 role/lessons)→ **server mint** worker token(`sub=ow-id`,agent scope;authz 未知 sub 保底 agent 級——契約 §H;token 只上 directed frame→warden 落 0600 檔,絕不進 log/chat)→ push 成員 `start` frame(P5b 統一:member_id=ow-id、role="outsource-worker"、session member-<ow-id>)到**線上 warden** 的命令 FIFO(**placement = 明確決策,owner 2026-07-25 裁示**:`pickWorkerWarden`/`resolveWorkerPlacement` 只認**一台當下真的可派工的具體機器**——kind=warden、roster active、online、未因該 worker 前次 boot 失敗被 bench、且具該 worker 的 runtime;**任何一條不過就不派**,舊的挑最閒/健康度評分(RAM 上限、壞憑證降權、agent 數 tie-break)**整段已刪**、離線也**不換別台**。machine 來源順位(**T-98f4 起在 pin 與其餘之間插入了「上一輪實際落點」一臂,見下方獨立小節「外包 worker 的落點黏性」——本句其餘不變**):worker 自身 pin(`desired_machine_id`,改機器 relocate)→ **上一輪實際落點(`last_machine_id`,軟偏好)** → 轉派對話框的 in-memory pick(`workerMachinePref`)→ 然後**兩個 durable 的 task 側來源,順序看誰真的做了決定(T-8a67)**:**dispatched=1**(顯式發包)時 **task 行 `outsource_machine` 先於手冊**(dispatcher 為這一筆指名的;restart 後 in-memory pick 沒了,靠它才不會永久卡住),**dispatched=0**(手冊派工)時**活手冊 assignee 先於 task 行**——後者只是建立者快照,是兩者中較弱的主張,必須輸給手冊。**沒有最終 fallback**:沒人指名機器 = 不啟動(但 T-8a67 後「沒人指名」罕見得多:手冊派工時建立者自己 pin 的那台已經寫在 task 行上,即這條順位的**第 (4) 臂**——(3) 只給 dispatched=1 的列)。不派時把原因 stamp 回 worker 行的 `last_op/last_op_reason`(`stampWorkerPlacementBlocked`),**只在原因改變時寫**(30s cadence 會一直重試,無條件寫會每 tick 噴 SSE delta)、start 真的派出去就清(`clearWorkerPlacementBlock`,連 server 自己寫的 `last_op_ok` 一併歸 nil——留著 false 又沒 reason 就是另一種空白);沒有線上的 warden = fail-closed 不派、cadence 重試。**T-e0e3:「不派一定留收據」已推及 `notifyWorkerSpawn` 的每一條 refusal**,code 閉集在 `spawnBlockedReasonCodes`(`no_machine_selected` / `machine_unavailable` / `no_live_task` / `boot_context_failed` / `no_signing_secret` / `token_mint_failed` / `frame_build_failed` / `warden_unreachable` / `respawn_deferred`;`isPlacementBlockedReason` 認這一份,加 code 沒登記進去 = 該 stamp 永不被清)——先前這些臂只寫 stderr,於是「起不來」與「正在起」在座艙上長得一模一樣。唯一刻意不出聲的 = 開頭的 `workerSpawnRetrySecs` pacing 早退(那是節流、不是失敗)。同批修掉 relocate 的整段吞掉:`relocateWorkerNow` 不再照抄 `respawnWorkerNow` 的 O-28 deferral(active 但無 kill target 就整個不動),而是**照樣試 start**——`old==""` 代表 spawn 記憶與 live SSE claim 皆空,即該 worker 沒連線。⚠️ **安全論證已於 T-e0e3 第五輪改寫(舊的寫錯了)**:擋這件事的**不是** warden clobber-guard——那道閘是 **per-machine local**,而 relocate 的定義就是換到另一台,舊 session 在 A、新 START 送 B,B 的 guard 看不到 A,**這條路上根本沒有它**。真正在這條路上的是 `identitySweepOnConnect`(正身在目標機連上即掃殺他機同 id session);更決定性的是**這個 fall-through 沒有引入新的危險類別**:outsource cadence tick 本來就會在同一個狀態(ACTIVE、desired 非 offline、無 live SSE)下 `reconcileWorkerLiveness → START`,fall-through 只是把 30s 空窗拿掉。兩個已記錄的不對稱(各自另票):cadence 那條有 `RefocusSince == 0.0` 遮罩、這條沒有;丟 pacing stamp 繞過 `workerSpawnRetrySecs` 與 FSM backoff(owner-explicit,由人手速節流);deferral 本身也改成留收據(`respawn_deferred`)。**T-e0e3 第二輪(真正的 X-46 根因)**:`reconcileWorkerLiveness` **從來沒讀 `decision.StartTimedOut`** —— 成員側自 T-ba62 就用它寫 `wake_timeout` 收據(`stampWakeObservability` arm (b)),worker 側整條漏接,所以「START 派出去了、warden 收了、但 agent 沒 boot 起來」是 worker row **唯一沒有任何持久記錄**的失敗;配上「spawn 觀測帳 in-memory 是契約」,server 一 re-exec 連 machine 欄都歸空 ⇒ 座艙顯示「尚未分配機器 + last_op 全空」,而實際上 server 已經派了 6 次(X-46 逐字重現)。現已補上 stamp(位置在 switch **之後**,讓同一 decision 可能帶的 re-START 先跑完成功清理);`wake_timeout` 與 `held_down` **刻意不列入 `spawnBlockedReasonCodes`** —— 前者之後必然跟一次 retry,被清掉就是 31751ae 剛修好的 regression 重演,後者是 owner 自己的「停止」standing、不因任何 dispatch 失效。**三個治理層動詞(owner 或 admin 助理——T-6020 起全部 `requires=admin_agent`;relocate 改機器 / restart 重啟 / 換 runtime-model)統一到單一 `respawnWorkerForOwnerOp`**(**T-98f4 起在這之後多一道「要不要給收尾機會」的分流,見下方獨立小節「所有 owner 動詞都給收尾機會」——本句其餘不變**):第一個分流點 = `desired_state == offline`?是 → 只存 placement/model、**不啟動**、留 `held_down` 收據(owner 裁示:placement ≠ 啟動,與 tick 既有 invariant `TestStoppedWorker_TickNeverRevives` 對齊,relocate 曾是唯一偷偷推翻 owner「停止」的動詞);否 → 共用 `respawnWorkerNow` + fall-through 真的試 start。restart 先翻 desired_state=online 才進來,**by construction 到不了 held_down 臂**(intent 住在 state 裡、不是各入口各抄一份);換 model 的 handler 因此**刪掉重複的 `desired != offline` 判斷**,只留 `Status==Active && hub.IsOnline`(那是 model 專屬的「只有活 session 才需就地重啟」)。**T-e0e3 第三輪(dispatch ≠ delivery)**:⚠️ **`enqueueToWarden` 回 true 只代表 frame 被 append 進 `hub.wardenCmds` 這個 map,不代表任何人讀了它** —— 整條 server→warden 路上**沒有 ack**(`write` = `w.Write`+`Flush`,只是塞進 socket buffer;T-7e07 那段 write-deadline 註解自己承認 consumer 可能 stuck/zero-window),而且 `DrainWardenCommands` 是**先 `delete` 整個 FIFO 再逐格 write**,任一格 write 失敗即 return ⇒ **已 pop 的 frame 全部無聲丟棄、不 re-enqueue、無 log、無收據**(`api_infra.go:215-222` + `hub.go:530`)。其餘無聲丟失的窗口:restart 丟光 process-local FIFO、同 id takeover 竊取(重複 ocwarden 進程持槽即 drain 走)、以及 loop 裡 entity-delta band 先行 `continue` 造成 warden-command band 被餓(`api_infra.go:191-195`)。**因此 `notifyWorkerSpawn` 的回傳值與測試裡的 frame 計數對「有人收 vs 沒人收」零鑑別力**(測試呼 `DrainWardenCommands` 就是在扮演 warden,恰好把 bug 演掉)。唯一 in-process 鑑別量 = **`hub.PendingWardenCommandsFor(wardenID, subject)`**(唯讀不 pop):**該 worker 自己那格**仍在隊列 ⇒ 沒人 collect ⇒ 收據寫 **`never_collected`**(「還排在隊列裡,**還沒有任何東西試著開機**,檢查該機 ocwarden 是否在跑」),空 ⇒ `wake_timeout` 且措辭改為誠實的「**collected by** machine X but never came online」。這兩者對 owner 意義完全不同(該去查的機器不同),混成一碼會把人指去一台從沒被要求開機的機器 —— X-46 就是這樣讓人先去查 codex 的(實測:warden log 對 X-46 的 id 0 命中,而 warden 在 dispatch **之前**就記 receipt(`cli/ocwarden/transport.go:351`,註解明寫「provable from the warden log ALONE」),同機 X-41/X-43 各 8/4 命中 ⇒ 那 6 個 frame 從未抵達)。`never_collected` **刻意不列入 `spawnBlockedReasonCodes`**:clear 是每次成功 **dispatch** 時跑的,而這個 code 的意義正是「dispatch ≠ delivery」,列進去等於每次 retry 都擦掉它。**T-e0e3 第五輪(獨立 review C.1/C.2 兩處必修)**:(a) **per-warden 深度 ≠ per-worker 事實**——`hub.wardenCmds` 是**一台機器一條隊列、全機所有 member/worker 共用**,所以隊列深度只回答「這台機器欠不欠任何人一格 frame」。先前拿它去斷言「**這個 worker 的** start frame 還在排隊」,於是只要同機**任何別人**有 frame 進行中,一台剛剛才證明自己在 drain 的健康 warden 就會被收據指控「ocwarden 沒在跑」,而且訊息還明確把真正該查的 runtime 排除掉——這是系統性偏誤不是 race(reconcile 與 outsource 兩條 cadence 同為 30s、開機相差微秒,相位鎖定)。修法 = **frame 可歸屬**:FIFO 值改成 `wardenCmd{Subject, Frame}`,`enqueueToWarden(memberID, …)` 一律走 `EnqueueWardenCommandFor` 帶上 subject,判別改讀 `PendingWardenCommandsFor(target, w.ID)`;`PendingWardenCommands` 保留為整機深度(語意不同,別再混用)。哨兵 `TestReconcileWorkerLiveness_OtherMembersBacklogIsNotOurs`。(b) **`target == ""` 第三臂**——兩個既有臂都會**指名一台機器並對它下斷言**,target 空時就會寫出「collected by machine ''」這種零證據的肯定句、還叫人去看一台沒有名字的機器的 log;現在空 target 走**只講已知事實**的第三種訊息(start window 過了、server 已不記得送去哪、請重按改機器)。誠實標註:這個狀態由測試強制構造,**生產可達性未經證實**(re-exec 會同時清掉 `workerSpawnTarget` 與 `workerReconcileStates`),保留它是因為錯訊息的代價遠大於一個分支。哨兵 `TestReconcileWorkerLiveness_UnknownTargetNamesNoMachine`。**T-e0e3 第四輪(O1 已修,rebase 後由 T-66a2 取代實作)**:warden band 的 write 失敗不再無聲吞掉 frame。⚠️ **本輪原本的 `hub.RequeueWardenCommands`(全部依序放回隊首)已刪除**——main 的 T-66a2 獨立修了同一個 bug,rebase 時採 main 的版本:`hub.ReturnUndeliveredCommands(wardenID, undelivered)`,handler 端 `if !write(cmd.Frame) { ReturnUndeliveredCommands(wardenID, pending[i:]); return }`(`pending[i:]` **含失敗那格**,它沒送出去)。**契約與舊版相反,不是全部放回**:只有 **`update`** 會被放回隊首(它是唯一沒有 re-decision 路徑的動詞;30s reconcile 會從 presence 重新推導 start/stop/uninstall,但沒有任何東西會重新推導「owner 要求這台機器升級」);**其餘動詞一律 DROPPED**——把一個過時的 START 放回一個 reconcile 已經重新決定過的佇列,比丟掉它更糟。差別在於 drop 現在**很大聲**:每格一行 stderr,外加一則 `cmdUndelivered` 註記(per-ADDRESSED-MEMBER,`UndeliveredCommandSince` 讀,dispatch 時清)。**head 不是 tail**(對放回的那些):這些 frame 的 enqueue 早於「doomed write 進行中才到的」frame,而 warden 命令**序敏感**(`stop` 超車自己的 `start` 會收掉那個 start 要建的 session);此性質由 `TestReturnUndeliveredCommands_RequeuedFrameLeadsFramesThatArrivedLater` 釘住(舊的 RequeueWardenCommands 測試曾釘住它,函式刪掉後一度沒有任何東西在執行這條註解寫明的規則——實測把 append 反過來全套件照樣全綠,trunk 亦然)。哨兵 `TestEventsHandler_SuccessfulWardenDrainClearsTheQueue` 釘住反向過度修正(全成功仍須清空,否則無限重送)。**worker 側收據必須接上這條**:START 被 drain 取走後遺失時 backlog 是**空的**,與「收到了但開機失敗」長得一模一樣,`reconcileWorkerLiveness` 因此在 `default` 之前加一臂讀 `UndeliveredCommandSince(w.ID, spawnAt)` → 收據 `never_collected`「從未抵達該機、連線才是嫌疑犯」;少了這臂就會掉進 default,自信地叫 owner 去查一台從沒收到過命令的機器的 runtime(釘住:`TestReconcileWorkerLiveness_DroppedStartIsNotBlamedOnTheRuntime`)。**殘留(未修,已登記成票)**:frame 被 pop **且 write 回報成功之後**仍可能丟(半開 socket / buffer),所以「FIFO 空」只證明 collected、**不證明 delivered** —— 要補需要 ack(動協定)。restart 丟光 FIFO、takeover 竊取、delta band 餓死 warden band 三格亦未修。⚠️ **收據是單槽不是歷史**(owner 裁示維持單槽,結構修法另票):三個 code 共用同一組 `last_op*`,新的蓋舊的 —— 最傷情境是 `never_collected` 被後來的 `wake_timeout` 蓋掉、「那台從沒收到過」的線索消失;碼上 `spawnReasonHeldDown` 旁有 KNOWN LIMITATION 註解寫明這不是設計終點。)喚醒觀察訊號 = 它首呼 get_my_task 翻 active。**救援(A案 P6)= 共用成員 reconcile FSM**(`reconcileWorkerLiveness`→`reconcileDecide`:start_timeout/backoff/zombie-takeover;clobber receipt(session_already_exists)→ robust stop 清殭屍 + bench 該 (worker, machine) 對(`workerSpawnCooldownSecs` 3×90s;**不是換機輪替**——沒有別台可換,冷卻期間該 worker 就是 `machine_unavailable` 停著,冷卻過了再回同一台);一次性 `recoverStuckWorker` 已退役);active-掉線 worker 同走 FSM 重生;identity sweep 不再排除 outsource(正身連上其派工機即掃殺他機分身——seth-m1 洞)。wire liveness = 成員 presence 詞彙(`workerPresence`/`deriveLiveness`,spawn_state 已廢)。**收場(§6.3 後半)**:task 終態時 closeTask 只標 released(面板消失)、**刻意不收 session**(留給 worker 做 close-out 後續:回寫 learnings、清暫存、回報);**解僱 hook = `dismissOutsourceWorkersForTask`——已接上 close-out 回報 handler**(POST /api/tasks/{id}/closeout 首報 stamp closeout_ts 後即呼;冪等,成員任務無 worker row = no-op)→ push 成員 `stop`(EXACT:warden 殺 member-&lt;ow-id&gt;,並過渡掃殺 legacy worker-&lt;ow-id&gt;);沒回報時的保底 = tick 的 reclaim backstop(released 後 `workerReclaimGraceSecs` 120s 強制收)。spawn 帳全 in-memory(spawnAt/target/reclaimed,restart amnesia 無害:多發的 start 被 clobber-guard 擋、多發的 stop 對不存在 session 是乾淨 no-op)。frame 契約見 spec/sse.md §7(P5b:外包共用 start/stop;worker_* 動詞退役、僅 warden 端留 legacy kill 過渡)。
- **軟體更新檢查 + 升級(GitHub Releases 版,t-dc68 改造)**:來源 = **GitHub Releases API(repo `pkyosx/OffiCraft`,匿名、免 token、零設定)**;無 updater URL/邀請碼概念。設定只剩兩個 bool toggle(DB `updater.receive_beta` / `updater.auto_update`,**皆預設 OFF**;SettingsDTO/SettingsUpdateDTO 欄):receive_beta = 檢查是否也納 **GitHub prerelease**(關 = 只吃正式 release);auto_update = 背景自動升級。**檢查(`update_check.go`)**:`/api/version` 的 `update_available`/`latest_version` 讀 cached 背景檢查(TTL 5min、timeout 8s,**永不在 request path 等網路**——stale 就 kick goroutine、先答 last-known;失敗優雅降級:保留 last-known + stamp checkedAt 防 hammering;channel 翻轉即重置 cache + PATCH kick)。比對規則:最新 admissible release tag ≠ 執行中 `appVersion`(官方包由 `bin/release` 以 `OC_APP_VERSION=<tag>` stamp;self-build 誠實 "0.0.0")。**顯式檢查 `GET /api/release/check`(T-6020 起 `requires=admin_agent` + MCP 工具 `check_release`)**:同步問 GitHub(30s button-TTL 吸連點),答 `up_to_date`/`update_available`(tag + release 連結)/`unknown`(GitHub 不通——誠實降級,仍 200)。**升級(`upgrade.go`,`POST /api/update/upgrade`,T-6020 起 `requires=admin_agent` + MCP 工具 `upgrade_station`——admin 助理跑升級,**一般 agent 仍不能自升級 server**)**:trigger-time 重新 pin release(不信 cache)→ 下載 `officraft-<tag>-darwin-arm64.tar.gz` 到 exe 同目錄暫存(bounded 2min、cap 256MB、邊流邊 hash)→ **sha256 對 release 自帶 `checksums.txt` 驗證**(不符=中止+502、磁碟零變動)→ 從 tarball 抽 `ocserverd` member → `--help` smoke test → 備份成 `<exe>.bak`(只留一份、成功覆寫、永不自動刪——手動退路)→ 原子 rename swap(失敗自動還原 .bak)→ 200 `{status:"restarting"}` 後 750ms `syscall.Exec` 原地 re-exec(同 PID,launchd/前景跑皆對;exec 失敗=舊進程照常服務,loud log)。TryLock 同時只允許一個升級(撞了 409)。**auto-update cadence(`auto_update.go`)**:serve 恆掛 1min tick(toggle 閘動作、不閘 loop——PATCH 開關即生效免重啟);armed tick 見新版即跑與手動端點**同一個** `runUpgrade` 本體。自動 = 「owner 有 arm」,永不是 server 自作主張。**測試 seam**:per-server `releaseAPIBase` + 全域 `releaseAPIDefault`(單測 TestMain 指到不可達 loopback,絕不打真 GitHub);serve 讀 `$OC_RELEASE_API_BASE`(conformance/e2e 隔離用)。舊 DB 的 `updater.url`/`updater.invite_code` rows 不再被讀(key 集封閉即忽略);兩個 toggle 的 DB key 沿用不變(armed 安裝跨遷移仍 armed,`settings_test.go TestSetUpdaterRetired` 釘住)。
- **domain 業務規則層已入(M3 第二批)**:`domain.go`(presence tri-state 衍生 / kind 閉集 + 空值→assistant 折疊 / member_no·名池 / 實體不變量 / unread 反相 / role_def·lessons·user_context·alias fold)+ `pacing.go`(rate-limit pacing,honest-null)。純函數、無 http/SQL;online 一律顯式參數(Go member 無 online 欄——observed 不落 DB,見 state-model.md)。

## 外包 worker 的落點黏性(T-98f4,owner 2026-07-27)

owner 原話兩句:「換手應該在原地,除非我有特別指定要換去別處」「手冊上的機器只對 outsource worker **第一次起來**生效,一旦我手動改到其他電腦上,再次換手應該活在其他電腦上」。

**一句話:手冊只管出生地;之後住哪由最後一次實際落點決定,而 owner 的手動搬遷就是在改那個落點。**

- **決策三層**(`worker_spawn.go resolveStickyWorkerPlacement`,spawn 路徑上 `resolveWorkerPlacement` 的唯一呼叫者):
  1. `desired_machine_id`(owner 的改機器)——**硬 pin**。那台不可派工就**停住留收據**,不試別的。owner 的指令不是建議。
  2. `last_machine_id`(上一輪實際落點,migrations/00039)——**軟偏好**。那台不可派工就**往下掉到第 3 層**,不停住。
  3. 原本那條 configured 鏈(in-memory 發包 pick → task 行 / 活手冊)——**維持硬語意、一字未改**。`last_machine_id == ""`(從沒落地過)時本函數**等同改動前**,這就是「手冊只管出生地」的實作。
- ⚠️ **本功能的 migration 編號是 `00039`;`00038` 在 `origin/main` 上永久空著,別「順手補回去」**:PR #12(member 自訂頭像)最終落的是 `00040_member_avatar.sql`(2026-07-27 已 merge 進 `origin/main`),所以 `00038` 這個號碼**不屬於任何人、也不會再有人來填**——它就是一個永久的跳號,**不要新增 `00038_*.sql`**,新的 migration 一律取當前最大編號 +1。`migrate.go` 呼叫的是 bare `goose.Up`(無 `WithAllowMissing`),兩種失敗方向都要看清楚:**跳號**的代價是「已在較高版本的 DB 遇到後來才出現的低編號 migration 會 migrate 失敗、server 拒絕啟動」——**很吵、擋在起站前、看得見**;**撞號**(兩個分支各有一個同號檔)的代價是 goose 記到那個版本就認為做過了,**後 merge 的那份 DDL 永遠不會被執行、沒有任何錯誤**——安靜的 schema 缺漏。撞號嚴格更糟,所以寧可留跳號也不要搶號:**空號留著就好,永遠往上取新號**。
- 🔴 **第 2 層必須是軟的,這是本功能的安全論證**:黏性若做成 pin,一台闔上的筆電就能把 worker 永久釘死在上面。**「owner 主動搬」與「機器下線」兩種合法搬遷必須照樣搬得動**(owner 點名要驗的那一臂)。往下掉也**不是**被禁止的自動挑機——第 3 層本身就是人為決定過的來源,伺服器從沒憑空挑過機器(2026-07-25 裁示未動)。
- **落點取「實際觀測到的主機」,不是「派工目標」**:server→warden **無 ack**,dispatch ≠ delivery,所以拿 `workerSpawnTarget` 當落點會把**一次失敗的派工黏成永久住址**。寫入點是 SSE first-connect(`api_infra.go stampLandedMachine`),值來自該 session token 的 `machine_id` claim(`notifyWorkerSpawn` 把解析出來的 warden 燒進 token)——**只有真的連上來才算落地**,而且 durable(不像 in-memory 的 `workerSpawnTarget`、也不像只在 session 活著時存在的 `hub.MachineOf`)。哨兵 `TestSticky_LandingIsTheObservedHostNotTheDispatchTarget`。
- 🔴 **只戳「正身」的落點,不是「任何連上來的」**(獨立審查第二輪必修):戳記是 durable 的、而且會左右之後每一次重生,所以判準必須是「**連上了 而且 確實是派到這裡的**」,不是「連上了」。用的就是**下一行** `identitySweepOnConnect` 已經算好的同一個 predicate(抽成 `reconcile.go connectionIsTheGenuineArticle`:pin 具體時比 pin,outsource 且 pin 空時比 `workerSpawnTarget`;任一邊為 `""` = 無法佐證 = false)——遊走者的宣稱在掃殺面不具權威,在落點面同樣不具。**沒有這道閘的具體後果**:sticky worker 常態是 `DesiredMachineID==""`,server re-exec 後 `workerSpawnTarget` 也空 ⇒ 舊主機上的殘留 ocagent **不會被 sweep**,它那一次連線卻會**持久改寫** `last_machine_id`,下一次重生就跟著幽靈走。⚠️ **兩個方向都被釘住,而且各自需要自己的測試**:(a) **誤擋**——pin 空、spawnTarget 有值的**合法首次落點必須照樣戳得上**(把 spawnTarget 那一臂拿掉 → 10 條紅);(b) **放寬**——把判準鬆成「驗不了就算正身」(`expected == "" || expected == machineClaim`)會**原封不動復活**本缺陷,而 🔴 **第三輪之前整個 sticky 檔對這個方向完全瞎**(實測:全檔 10 條測試無一紅,只有另一個檔的 identity-sweep 測試紅——維護者盯著本檔會什麼都看不到)。根因是原本的幽靈哨兵只走 `expected != ""`(spawnTarget 不一致)那條腿,從沒碰到 fail-safe 那條腿,**而 `expected == ""` 正是這道閘要治的生產形狀本身**(server re-exec 後 spawnTarget 空、sticky worker 的 pin 也空)。補上的哨兵 `TestSticky_UnverifiableConnectionAfterReExecNeverRewritesTheLanding`。
- **所有 kind 都寫(T-7f28 放寬)**:原本只寫 `kind=outsource`,理由是「正職有 durable 的 `desired_machine_id` 釘著」——但 pin 是**意圖**,owner 一改 pin 它就不再描述現況,離線的正職於是沒有任何東西可以拿來比對,「已經搬過去」與「還沒搬」在畫面上一樣。放寬後對正職**純觀測**(`MemberDTO.actual_machine`),**不進 placement 決策**——`last_machine_id` 的唯一讀者仍是 `worker_spawn.go`。warden 不 claim 自己的機器。**空的 machine claim 一律不寫**——`""` 是「不知道」不是「哪裡都不是」,擦掉已知落點等於把 worker 悄悄交還給手冊(同一個 bug 往下一層)。write-on-change,重連同機零寫入零 delta。
- ✅ **上一版的「未決 owner 問題」已裁定(owner 2026-07-27:搬遷一定要帶機器)**:規則第 1 條說「被手動搬過就不被任何設定拉回去」,而 relocate 端點的 `machine_id` 從前預設 `""` 且**無條件寫入**,所以 body 不帶欄位就會**清掉既有 pin**——**把最有破壞性的形式,做成「你忘了填一個欄位」時拿到的那一個**。裁定:**relocate 一定要指名機器**,這條路由上**不再有解除固定這個動詞**。三個入口(成員 `api_members.go`、worker `api_outsource.go`、共用核心 `relocateWorkerByID` 的 fail-closed 保底)一致:**欄位缺席 → 422**(`decodeJSONBodyRequired`,凍結的 missing-required-field 面)、**明確 null 或 `""` → 400**(語意拒絕,與 `decodeJSONBodyStrict` 的既有分工一致);訊息只有一份(`relocateNeedsMachineMsg`),兩個面不可能各說各話。**被拒的請求一律不碰 pin**——半執行的 400 比不擋更糟。spec 同批改(`spec/openapi.json` 兩個 DTO 的 `required` + `spec/mcp-catalog.json` 的 `relocate_member`,連帶 `ocapi_gen.go`/FE `schema.ts` 重生:`MachineId` 由 `*string` 變 `string`)。**座艙不用改,已親驗**:`MachinePicker` 的 select 只由 `online` 機器填、`selected` 預設 `online[0]?.machineId ?? ""`、confirm 被 `disabled={!selected}` 擋住,`onConfirm` 唯一出口就是 `useRelocateMachine` 的 `run`——沒有任何入口送得出空機器 id。哨兵 `TestRelocateMember_MachineIsMandatory` / `TestRelocateOutsourceWorker_MachineIsMandatory`(各含三種形狀 + 一條「指名機器照樣搬得動」的陽性對照);conformance `test_relocate_requires_a_machine_that_resolves`。
- ⚠️ **一個既有測試的前提被這條裁定刪掉了,誠實記下**:`TestRelocateMintedOfflineWorker_ClearedPinNeverStarts` 整條的前提就是「relocate `""` 清 pin ⇒ 沒有 placement ⇒ 不啟動」,而那個狀態**經由這條路由已經不可達**,再斷言它等於釘住一份 server 不再擁有的契約。改寫成 `_BlankMachineRefusedAndStartsNothing`:保留它真正在守的那一半(**沒指名目的地的 owner 動作不能把 worker 放到任何地方**),只是拒絕點從排程器搬到了路由本身,並額外釘住「被拒的請求沒有半執行」。它原本順帶驗的 `no_machine_selected` 收據**沒有失去覆蓋**,那條不變式在自己的源頭仍被 `TestNotifyWorkerSpawn_StampsNoMachineSelectedReason` 與 `reconcile_test.go` 的成員孿生釘著。
- ⚠️ **代價,誠實寫下**:黏性讓「停滯」更容易重複發生在**同一台**。落點機器掛掉時第 2 層會往下掉(所以不會卡死),但**被 bench**(前次 boot 失敗)期間若 configured 鏈也指向同一台,worker 就是停著等冷卻——那是既有的 `machine_unavailable` 行為,不是本次新增的。
- 哨兵:`worker_sticky_placement_t98f4_test.go`,刻意分成「保護沒生效」與「保護誤擋」兩半。⚠️ **「一顆 mutant 只會點名其中一半」這句話是錯的,而且錯的方向很傷**(第二輪獨立審查抓到、本輪逐顆重測,每顆前 `go clean -testcache`,整套件計數):**拿掉第 2 層 → 5 條紅、橫跨兩半**(`_RebirthStaysPutWhenTheManualMoved`、`_RebirthStaysPutWhenTheTaskRowMoved` 屬保護那半;`_OwnerRelocateStillMoves`、`_OfflineLastMachineFallsThroughToTheConfiguredOne`、`_UnreachableLastLandingWithNoAlternativeNamesItself` 屬誤擋那半——那三條會紅是因為它們的**前置**就是先落地一次,第 2 層不存在時連 fixture 想表達的狀態都構造不出來)。其餘三顆重測**與原記錄相符**:把第 2 層做成硬 pin → **2 條紅**(`_BenchedLastMachineFallsThrough`、`_OfflineLastMachineFallsThrough`,都在誤擋那半);讓第 2 層蓋過 pin → **1 條紅**(`_OwnerRelocateStillMoves`);拆掉 SSE 接線(`api_infra.go` 那一行 `stampLandedMachine`)→ **2 條紅**(`_ConnectStampsTheLandingFromTheTokenClaim`、`_GhostConnectionNeverRewritesTheLanding`,兩條接線測試);把 正身 閘的 `workerSpawnTarget` 那一臂拿掉(過度嚴格方向)→ **10 條紅**;把 正身 閘**放寬**成「驗不了就算正身」→ **2 條紅**(`_UnverifiableConnectionAfterReExecNeverRewritesTheLanding` + 另一檔的 `TestIdentitySweepReapsOutsourceResidual_AutoPlacement`)——⚠️ 第三輪補上前者之前,這個方向在本檔是 **0 條紅**。**要拿哨兵當單一診斷訊號的人請注意第一顆是跨組的**——看到誤擋那半紅,不能直接推論「黏性被做成 pin 了」。


## 所有 owner 動詞都給收尾機會(T-98f4 規則二,owner 2026-07-27)

owner 原話:「我建議所有換手都可以給他機會收尾」。**relocate(改機器)/ restart(重啟)/ 換 model 三個動詞原本是直接砍掉重起**——無 `refocus_since` stamp、無 SOP 預告、無寬限,前一輪還在進行中的東西沒有機會存下來;而 **換手(refocus)自 T-ea82 起就有完整的 預告→寬限→收口**。從 worker 的角度這四件事是同一件事(這個 session 結束、下一個接著做同一個任務),沒有理由厚此薄彼。

- **改動點只有一處**:`respawnWorkerForOwnerOp`(三個動詞唯一的漏斗)在既有的 `desired_state==offline → held_down` 分流**之後**多一道分流,走 `openOwnerOpHandover`(stamp 新的 refocus epoch + `openWorkerHandoverGrace` 預告,**不砍**)。收口沿用既有的兩個 driver:worker 自己的 `report_stopped`,或 `autoHandoverWorker` in-flight 臂的 `StoppingTimeoutSecs` 天花板。**新的 pin / model 在開窗前就已經寫進 row**,所以收口那次 respawn 自然帶上新值。原本的立即路徑原封不動搬進 `respawnWorkerForOwnerOpNow`。
- 🔴 **「有東西要存才等,沒有就立刻走」的判準(owner 在意體感:不要改個模型乾等 120 秒)**。server 能**證明**的「沒東西要存」有下列幾種,每一種都是結構事實不是猜測(前兩條是原始的兩種,3./4. 是兩輪獨立審查各補的一種,5. 是 deny-list 例外):
  1. **沒有活 session**(`!hub.IsOnline`)——沒有東西聽得到預告、也沒有東西能存。這是 `openWorkerHandoverGrace` 早就有的 D6 規則。
  2. **從沒認領任務**(`Status != active`,即 `activated_ts == 0`)——assigned→active 的翻轉**就是** `get_my_task` 那一次認領,所以非 active 的 session **可證明**從沒拿到任務內容,沒有任務狀態可回寫。
  3. **這個 epoch 的收尾已經收攏**(`refocus_since > 0` **且** `stopped_since > 0`)——收口 latch 代表 kill+start 已經用**當時**的 model/machine 派出去了,舊 session 只是還沒被 warden 收屍,所以沒有東西還要 flush,動詞必須**立刻**派。🔴 **兩個條件缺一不可,而且是兩輪獨立審查各抓一次的同一個判斷式**:少了 `stopped_since` 那半 = 第一輪的 HIGH(收攏窗內的動詞被默默丟棄);少了 `refocus_since` 那半 = 第二輪的 HIGH(見下條)。
  4. 🔴 **`stopped_since` 有兩個 latch 點,只有一個是換手——所以判準一定要 epoch-scoped**:`collectWorkerHandover` latch 它當作某個 refocus epoch 的收口,但 `workerReportStopped` 的 **else 臂**也會 latch 它,那是**換手之外**的一次普通「停止 → worker 回報自己收工了」。**沒有任何東西會清掉後者**——`clearWorkerRefocus` 只在 `refocus_since > 0` 時可達,重啟 handler 只寫 `desired_state`——所以那個 latch 會活過整個 停止→重啟 循環。拿**全域**的 `stopped_since` 去讀,它就永遠在說「已經收攏了」:那個 worker **從此每一次** 改機器／換 model 都被當場砍掉,無預告、無寬限、未存的東西全丟,而且**一直重複**。這比第一輪那個窗口洞更廣也更日常。配上 `refocus_since > 0` 才是原本要問的問題(**這個 epoch** 收攏了嗎)——收口只可能屬於某個 epoch。殘留 latch 隨後自癒:下一次開窗(`openOwnerOpHandover`)會把它歸零。
  5. **另外一個 deny-list 例外:`restart`**(`ownerOpRevivesStoppedWorker`)——重啟只會在 `desired_state` 剛從 offline 翻回 online 時走到這裡,它要取代的那個 session **是 停止 已經下過殺令的**;對它 fan 預告然後等滿一輪,等的是一個永遠不會來的回答。**刻意做成 deny-list 而非 allow-list**:之後新增的動詞預設**有**收尾,「不給」才需要被論證。
- ⚠️ **active + online 那一格是誠實的退路,不是正向偵測**:server 對 agent 的 transcript **零可見度**,唯一知道「有沒有東西還沒存」的是 agent 自己。再細的 server 端判準(context pct、開機多久、訊息數)都是**把猜測包裝成判準**,猜錯的代價是那一輪的 learnings 無聲全掉。所以這一格走 **「照舊等滿但可提早結束」**:`StoppingTimeoutSecs` 只是**天花板不是時長**,收口在 worker 回 `report_stopped` 的那一刻就發生——沒東西要存的 session 幾秒就結束。
- **agent 互動面零改動**(§9c 檢查過):fan 出去的還是同一則 member-topic delta,ocagent 的 recycleHook refetch 後印同一份五步 SOP。沒有新工具、沒有新步驟,所以 `seeds/` 不需要同批改。
- **可觀察的代價,誠實寫下**:改機器 / 換 model 之後,worker 最長會在**舊機器 / 舊 model** 上多活一個寬限窗;座艙這段期間顯示「換手中」(`refocus_since>0`,與 refocus 同一個顯示狀態)。這是 owner 要的交換。
- 哨兵 `worker_ownerop_winddown_t98f4_test.go` + `TestSetWorkerModel_ActiveWindsDownThenRespawns`(**契約變更**:該測試原本斷言 handler 當場 2 frames,已改成斷言開窗、再由 stopped-report 收口且新 model 有帶上)。⚠️ **原本記的第二顆數字與測試名都是錯的**(第二輪獨立審查抓到;本輪逐顆重測,每顆前 `go clean -testcache`,整套件計數):
  - 拿掉整條規則(那一段 `if` 整個刪掉,永遠走立即路徑)→ **5 條紅**:`TestOwnerOp_RelocateWindsDownInsteadOfKilling`、`TestOwnerOp_WindDownEndsOnTheDeadlineToo`、`TestOwnerOp_OrdinaryStopRestartStillWindsDownLater`、`TestOwnerOp_SecondVerbDuringAnOpenWindowReStampsAndStillCollects`、`TestSetWorkerModel_ActiveWindsDownThenRespawns`。
  - 拿掉全部 fast path(`if true`,永遠開窗)→ **9 條紅,不是五條,而且不全在本檔**:本檔 5 條(`_NoLiveSessionTakesEffectImmediately`、`_NeverClaimedItsTaskTakesEffectImmediately`、`_RestartNeverWindsDown`、`_VerbAfterTheCollectIsNotSwallowed`、`_OrdinaryStopRestartStillWindsDownLater`),另外 4 條是既有生命週期測試(`TestRelocateMintedOfflineWorker`、`TestRelocateMintedOfflineWorker_UnreachablePinLeavesReceipt`、`TestRestartWorker_ClearsAndRedispatches`、`TestRestartWorker_NoKillTarget_StillAttemptsStart`)。**`TestOwnerOp_StoppedWorkerStillOnlyGetsAReceipt` 保持綠**——它守的是更前面那道 `desired_state==offline` 分流,這顆 mutant 動不到它,寫成「五條立刻走」會讓人以為它在其中。
  - 把 restart 的 deny-list 拿掉(`ownerOpRevivesStoppedWorker` 恆 false)→ **3 條紅**:`TestOwnerOp_RestartNeverWindsDown`、`TestRestartWorker_ClearsAndRedispatches`、`TestOwnerOp_OrdinaryStopRestartStillWindsDownLater`。
  - **「已收攏」那個判斷式的三顆 mutant——三個 conjunct 各自被恰好一條測試釘住**(第三輪重測):整臂拿掉(`collectedThisEpoch := false`)→ **1 條紅** `_VerbAfterTheCollectIsNotSwallowed`(第一輪的 HIGH);退回**全域** `stopped_since` 讀法 → **1 條紅** `_OrdinaryStopRestartStillWindsDownLater`(第二輪的 HIGH);只讀 `refocus_since`(把任何開著的窗都當成已收攏)→ **1 條紅** `_SecondVerbDuringAnOpenWindowReStampsAndStillCollects`(決策表 cell 3)。
  - ⚠️ **上面三行以外的數字在第三輪全部重測過,而且都變了**(新增三條哨兵所致,舊數字已就地更新;這正是為什麼「加測試但不重測 mutant 紀錄」會讓這一節再次過期):拿掉整條規則 **3 → 5 條紅**(多了 `_OrdinaryStopRestartStillWindsDownLater`、`_SecondVerbDuringAnOpenWindowReStampsAndStillCollects`);`if true` **8 → 9 條紅**(多了 `_OrdinaryStopRestartStillWindsDownLater`——它的 fixture 需要重啟**不**開窗才構造得出殘留 latch);restart deny-list **2 → 3 條紅**(同一個 fixture 理由)。
  - 🔴 **判斷輸入的完整決策表在 `worker_ownerop_winddown_t98f4_test.go` 檔頭**(active × online × refocus × stopped 十六格逐格期望 + 兩道上游閘)。**兩個 HIGH 都出在這同一個判斷式**——第一次漏掉一個狀態、第二次把範圍畫太寬——所以表是刻意的:動這個函式前先在表上找到你的改動,**找不到對應格子 = 表過期了,或第三個洞就在那裡**。cell 1..4 各有自己的哨兵(上面三顆 mutant 就是在證明這件事),cell 5-8 / 9-16 因為前面的 conjunct 短路而各只需一條。


## 分享連結的 mint seam 也是 agent 面(反轉一個既有決定)

`GET /api/chat/attachments/{attachment_id}/share-link` 一直存在,但那一列原本帶著 `MCPExclude: true, // a UI convenience seam, not an agent tool` —— **那是刻意的決定,不是遺漏**。現在它反轉成 MCP 工具 `get_chat_attachment_share_link`。

- **為什麼反轉**:agent 交付檔案的完整路徑是 upload → pin,而收件者要拿到 bytes,**唯一**的路是自己登入座艙、手動複製連結。也就是說 agent 產得出檔案、卻永遠產不出一條能交出去的連結——而那正是它唯一還缺的東西。
- 🔴 **授權邊界一個字沒動**:`Requires` 仍是 `principalMachine`,任何已認證 principal 本來就打得到這條 REST。**但「可發現性提高」不等於「風險中性」**——工具進了 `tools/list` 之後 mint 會變得頻繁得多,而每一條 mint 出來的連結都是**永久、不可撤銷、免憑證**的(`sharesig.go`)。這兩句要一起讀:少讀哪一句都會得到一個過度樂觀或過度恐慌的結論。
- **兩個既存事實**(解釋為何這不是新增讀取能力):(a) blob GET 同樣是 machine floor、同樣沒有 ownership 檢查,agent 拿自己的 token 本來就讀得到任何 blob;(b) `get_chat_attachments?with=<member_id>` 早就是 MCP 工具,列舉別人對話的 attachment id 早在 agent 面上。**沒有 ownership 檢查這件事因此是既有缺口、不是本次引入的**——要補是另一個 owner 決定(會改到既有端點行為)。
- 🔴 **真正新增的是「把讀取權轉授給未認證第三方」的能力,(a)(b) 兩個事實講不到這一點**——本次之後,**組合**起來的這條路第一次整條都在 agent 工具面上:`get_chat_attachments(with=<任何成員>)` 拿到別人私訊裡的 attachment id → `get_chat_attachment_share_link(id)` → 一條永久、免憑證、撤不回的公開網址。本次之前第 2 步需要 agent 知道一條沒出現在 `tools/list` 的 REST 路徑;現在兩步都被 catalog 廣告、seed 還教了範例。**唯一的護欄是 seed 裡一句沒有任何強制力的規範**(「只為你本來就要交出去的東西簽」)。寫在這裡是為了讓下一個 reviewer 直接秤這個**組合**,不必自己重推一遍。
- **blob GET 本身仍 `MCPExclude`**:bytes 走 streaming seam(`ocagent download`),永遠不進 JSON tool result。
- ⚠️ **本次發現但**刻意未修**的 doc↔碼 矛盾(留給維護者裁,見憲章 §8 護欄)**:上面事實 (a) 說 blob GET 沒有 ownership 檢查(碼上確實如此——`api_chat.go` 的 handler 就是一個裸 `GetChatAttachment`,沒有任何 caller 比對),但**兩份文件說相反的話**:`routes.go` 該列的 `Summary` 寫 `owner-gated`,`spec/openapi.json` 該 operation 的 description 更寫明「scoped to that owner … **a caller can not fetch another owner's attachment**」。那是**退役 Python 多 owner 時代**的語言,描述一個 Go handler 裡已經不存在的 `owner` 依賴。單 owner 化之後那句「不能拿到**別的 owner** 的附件」變成空真,但 `owner-gated` 這個詞任何人都會讀成「只有 owner 拿得到」,而這條路由的門檻是 `principalMachine`。**沒有在本 PR 一併改**,因為 (i) 動 `spec/openapi.json` 是凍結 wire 面、依 §13 要 spec-first 過 owner,(ii) 依「兩份權威打架不自裁」原則,該由維護者定哪邊算數。**這是 T-5336 §(1) 處理過的同一種形狀**(description 承諾了地板守不住的事)——那次的裁定是「同批修掉那句話」。
- **回的是 server-relative path**:server 的 host 寫死 loopback,它不知道自己的對外 origin(座艙是在瀏覽器端用 `window.location.origin` 絕對化的)。agent 端由 seed 教它自己前綴——**刻意不讓 server 猜**,猜就得新增一個會過期的設定項。
- 同批更新(憲章 §9c):`spec/mcp-catalog.json`(手維護)、`conformance/routes_manifest.json` 的 `mcp_tool` 欄、`seeds/system_interaction.md` §4。哨兵 `api_chat_share_link_tool_test.go`:工具在面上**且 floor 沒動**、mint 出來的就是 `shareSigFor` 那一份(沒有第二條簽名路徑)、免憑證可讀、竄改/他檔/空 sig/裸請求/sig 打別條 route 全 401、未知 blob id 回 not_found(不 mint 進虛空)。**刻意沒有「過期」測試**——share sig 依設計就沒有效期,測一個不存在的機制等於釘一份 repo 沒有的契約。

## 正職成員的 owner 動詞也給收尾機會(T-b6d9,T-98f4 規則二的正職半邊)

T-98f4 只做了外包 worker。正職這邊三個動詞各走各的,實測(2026-07-28 追碼 + 陽性對照)結果三種待遇:

| 動詞 | 改動前 | 改動後 |
|---|---|---|
| **重啟**(`refocus_member` / `restart_self`) | 蓋 `refocus_since`、**不派任何東西**,由 `decideUp` 的 recycle 臂等 `report_stopped` 或 `RecycleGrace`(120s) | **一字未改**(哨兵 `TestRelocateMember_RestartIsUntouched`) |
| **改機器**(relocate) | **零預告零寬限**:handler 只寫 pin,`reconcileMemberNow` 走 relocate 臂當場 `stop` → tmux kill-session。連 `stopping_since` 都不蓋,座艙上無聲消失 | 蓋 `refocus_since` → 走 **既有的** recycle 臂,agent 收到同一份五步 SOP,收口後下一 tick 的 START 自然帶新 pin |
| **換模型 / runtime / effort**(`update_member`) | **完全不重啟**,回 200,新值等下次 spawn 才被讀 | 三欄**真的變了**才開窗;收口後以新值重生 |

- 🔴 **判別式只有一個欄位**:五步 SOP 由 `cli/ocagent/listen_hooks.go` 的 `recycleHook.maybeRecycle` 印出,閘寫死 `desired_state==online ∧ refocus_since>0`。**沒蓋戳 = agent 從頭到尾看不到 SOP**,沒有「比較短的預告」這種中間狀態。
- **漏斗 = `server/ocserverd/member_ownerop_winddown.go`**(`memberHasStateToFlush` / `armMemberOwnerOpHandover`),形狀比照外包的 `respawnWorkerForOwnerOp`。**caller 只做一次 `putMember`**:新 pin/model 與 refocus epoch 同一筆寫入,agent 醒來看到的那一則 delta 就已經帶著新值。
- 🔴 **`memberHasStateToFlush` 是把 `workerHasStateToFlush` 照抄過來的,不是重寫的**——那張表**被兩輪獨立審查各抓過一個 HIGH**。逐格對照與每一格的理由寫在該檔檔頭;決策表在 `member_ownerop_winddown_tb6d9_test.go` 檔頭(kind × desired × online × refocus × stopped,七列各有自己的哨兵)。三處差異都寫明了:(a) 多一條 `kind==assistant`(warden 沒有 ocagent 讀得到這個戳);(b) worker 的「從沒認領任務」(`Status != active`)**正職沒有對應物**,刻意不發明——省略的方向是「多開窗」,是安全那邊;(c) worker 的 restart deny-list 正職 **N/A**(重啟本身就是換手,不進這個漏斗)。**epoch-scoped 的 `refocus_since>0 ∧ stopped_since>0` 一字照抄**——正職的 `HandleReportStoppedApiSelfStoppedPost` 同樣有第二個 latch 點(平常的 停止 也會蓋 `stopped_since`),讀成全域就會讓那個成員**從此每一次**改機器/換模型都被當場砍掉。
- 🔴 **收口的 kill 改成打「session 真的在哪台」**(`memberKillTargetWarden`,以及 recycle 臂的 `DispatchWarden: obs.RunningMachine`)。原本兩條都用 `wardenTargetOf`(**desired** machine),而 改機器 的 pin 在開窗那一刻就已經指向**目的地**——不改的話收口會叫目的地的 warden 去殺一個它根本沒有的 session,舊 session 永遠活著。對「坐在自己 pin 上」的成員兩者同一台,所以 重新聚焦 觀察不到差異;`hub.MachineOf` 為空時退回 `wardenTargetOf`(即原行為)。
- **`relocation_pending` 多了第二個成因**(並在 T-927a 補上可分辨的第二個訊號 `relocation_deferred`:純加欄、只在 relocate 回應、只在開窗時 true,座艙據此只對「真的派不出去」告警):原本只代表「STOP 派不出去」,現在「開了窗、還沒收口」同樣是 `pending=true`——那正是欄位的字面意思(move scheduled, not yet landed)。`api_members_relocate_pending_test.go` 那對紅綠測試的 fixture 因此**被改到「本 epoch 已收攏」那一格**(那格仍會當場派 STOP),鑑別力保住、只是換了到達路徑。
- ⚠️ **`decideUp` 的 relocate 臂沒有刪,但語意降級成 backstop**:正常的 改機器 現在由上游的 recycle 臂接手。它原本那句註解說 relocate「recycled exactly like refocus」是**假的**(refocus 會等、relocate 一秒不等),已修——那句話害人對 owner 講錯過一次現況。
- ⚠️ **既有測試的契約變更(誠實記下,兩條都是「原測試把直接砍掉釘成契約」)**:`TestRelocateMember_MigratesLiveMember` 原本斷言 handler 當場在舊機器 warden 上放**恰好 1 個 stop frame**,改寫成「開窗 + 零 frame,收口後才有那 1 個 stop」,並額外釘住 pin 活過收口;`TestReconcileDecide` 的「refocus 壓過 relocate」子測原本斷言 `DispatchWarden == ""`,改成斷言它等於 RunningMachine(釘住的是 Reason 那個優先序,不是位址)。
- **座艙**:窗期間 `refocus_since>0`,與 重新聚焦 同一個顯示狀態(所以看得到「換手中」而不是無聲消失)。FE 那行 `mp.modelEffortNextWakeNote`「變更於下次喚醒／換手生效」在這之後是假話,同批改成「線上會自動換手後套用;離線則下次喚醒生效」;**key 名保留歷史拼法**(theme wording overlay 以 key 為契約)。⚠️ **T-927a 之後那行字在成員面板已經看不到了**(面板改唯讀、設定收進喚醒/更改對話框),它現在只剩外包詳情那個面板在用;成員面板的等價說明改寫在 `docs/guide/members.md` 的動作清單裡。
- **agent 互動面零改動**(root §9c):同一則 member delta、同一次 refetch、同一份五步 SOP,無新工具無新步驟 ⇒ `seeds/` 不需要同批改。
- **代價**:改機器/換模型之後,成員最多在**舊機器/舊模型**上多活一個 `RecycleGrace`(120s 天花板,不是時長——agent 一回 `report_stopped` 就收口)。這是 owner 要的交換。
- **mutant 實測(2026-07-28,每顆前 `go clean -testcache`,還原一律用 scratchpad 備份,整個 package 計數)**:
  - relocate 那道分流拿掉(`windDown := false`)→ **3 條紅**:`TestRelocateMember_MigratesLiveMember`、`TestRelocateMember_WindDownIsAlsoPending`、`TestMemberOwnerOp_SecondVerbDuringAnOpenWindowReStamps`。
  - update_member 那道分流拿掉(`if false && launchIntentChanged`)→ **2 條紅**:`TestMemberOwnerOp_ModelChangeWindsDownThenRespawns`、`TestMemberOwnerOp_OrdinaryStopRestartStillWindsDownLater`。
  - 讓 `refocus_member` 當場派 robust stop(破壞哨兵)→ **1 條紅**:`TestRelocateMember_RestartIsUntouched`。
  - 已收攏判斷退回**全域** `stopped_since` → **1 條紅**:`TestMemberOwnerOp_OrdinaryStopRestartStillWindsDownLater`。
  - 已收攏判斷只讀 `refocus_since` → **1 條紅**:`TestMemberOwnerOp_SecondVerbDuringAnOpenWindowReStamps`。
  - kill 位址退回 `wardenTargetOf`(兩處一起)→ **4 條紅**:`TestRelocateMember_MigratesLiveMember`、`TestMemberOwnerOp_SecondVerbDuringAnOpenWindowReStamps`、`TestRelocateMember_LandedNoPending`、`TestReconcileDecide`。⚠️ **這顆是跨組的**——看到 `_LandedNoPending` 紅不要直接推論 pending 邏輯壞了。

## 聊天附件:一則訊息/一張卡與它的 blob 是同一筆寫入(T-e2b2)

- **拒絕在儲存之前,寫入是全有全無。** 帶附件的四個面(`post_chat`、`post_task_message`、開請示卡、回答請示卡)先把**每一項**解析完才寫任何東西;**既無 `id` 也無 `data_b64` 的項目一律 400**(owner 2026-07-27 rc-3a589dfec503 拍板;此前聊天與任務訊息兩面是默默丟棄、照樣回 200——寄件者以為檔案送到了)。
- **三個原子入口(DAL)**:`PutChatWithAttachments`(blob+訊息)、`PutReplyCardWithChat`(blob+companion 訊息+卡)、`PutReplyCardWithAttachments`(blob+卡,回答面)。共用 `sqlExecer` seam,單筆與交易兩種形式跑同一條 SQL,所以原子版本不會跟單寫版本漂開。**帶附件的路徑不要直接呼叫 `PutChat`/`PutChatAttachment`/`PutReplyCard`**——先寫 blob 再寫紀錄,失敗就留下一個沒有任何紀錄指得到的 blob,而唯一的回收串接(`DeleteChatInvolving`)是從紀錄的 refs 走的,永遠掃不到它。
- 🔴 **守衛在哪、以及哪一條才算數**:`api_chat_orphan_blob_test.go`(`TestAttachmentWritesAreAllOrNothing`)是真正的防線——用 SQLite trigger 讓**某一張表的 INSERT 與 UPDATE 一併**失敗(回答面寫卡是 upsert,只擋 INSERT 會漏掉它)(SELECT 仍可用,這點是關鍵:改用改表名會連路由自己的查詢一起打爛,那一版的回答面守衛因此**永遠不可能紅**),四個面各驗**兩個方向**(紀錄寫失敗 / blob 寫失敗)。**判準不是數列數**——數列數看不見兩種真缺陷:回答面是 upsert,列數在正確與壞掉的世界裡一樣;開卡面一次寫兩筆紀錄(companion 訊息＋卡),盯一張表就漏掉另一筆。改成掃三條不變式:每個 blob 都有紀錄指得到、每個紀錄指的 blob 都存在、每則訊息指的卡都存在(懸空請示)。⚠️ **不變式只在它讀得到的欄位範圍內成立**——目前讀 `chat_message.meta.attachments`、`reply_card.attachments`、`reply_card.answer_attachments`、`task_artifact.attachment_id`;漏一欄就是把盲點往上搬一層(審查實測:少讀 task_artifact 會對一個合法被引用的 blob 誤報孤兒)。**每個面**跑一次陽性對照,而且每個探針都送**兩個**附件:不注入故障時必須兩個都寫進去、也都被紀錄指到。**一致 ≠ 沒漏**——只送一個附件時,「只保留第一個附件」這種缺陷是對稱的資料遺失,三條不變式全部乾淨、守衛看不見(審查實測整套照樣綠),而那正是本票的立案缺陷本身。沒有陽性對照的話,「有錯誤發生、而且沒看到壞東西」還會被一個根本沒走到附件的請求滿足。
  - **它證明的範圍就到這裡,別放大**:證的是「同一筆交易內,後續 INSERT 失敗會回捲先前的 INSERT」,四個面各自實測。**沒有**證:語句中途的部分回捲、真正的行程崩潰或磁碟滿的持久性、WAL/fsync、併發。
  - `api_chat_attachment_wiring_test.go` 的 AST 掃描只是「早一步指出行號」的便利品:獨立審查用三種寫法(抽成 helper、直接用 `putChatAttachmentOn` seam、取 method value)把它全綠繞過去——**別拿它的綠當證據**(這正是任務手冊 T-5047「釘名字不釘結構等於沒釘」那條的再現)。

- 🔴 **同一份四欄清單現在有兩個家,兩邊都要改(T-62a8)**:上面那份是**測試側**的世界模型(`referencedAttachmentIDs`),判的是**寫入面**;`dal.go` 的 `collectSurvivingBlobRefs` 是**生產側**的存活判定,判的是**刪除面**——`DeleteChatInvolving` 是**全域唯一**刪 `chat_attachment` 的敘述,所以那個掃描就是 blob store 的全部存活裁決。**新增第五個引用欄位時兩邊都要加,而且同一個 commit**;只加測試側那份,生產的刪除路徑依舊看不見它。
  - ⚠️ **別把測試側那份當成「已經有守衛在守生產」**——T-e2b2 把 `task_artifact.attachment_id` 納入的是不變式掃描,`DeleteChatInvolving` 的存活判定當時一行都沒動,於是一個被釘成任務交付物的 blob 會隨它所在的聊天訊息一起被刪掉(任務一進終態產物集雙向凍結 ⇒ **不可復原**)。T-62a8 補的是生產那半。兩個方向各有哨兵(`dal_blob_liveness_test.go`):被引用的不准刪、**沒人引用的仍然要刪**——只釘前者會把資料遺失換成沒人會發現的磁碟洩漏。
  - **誠實邊界**:它**不是通用 GC**,只看「剛被刪的那些訊息引用過的 blob」、之後不重訪。所以被 artifact 保住的 blob 若日後被 un-pin 就永久收不回(`DeleteTaskArtifact` 依明文契約不刪 blob)。這是有界洩漏,換掉的是不可復原的刪除——要改等於改 `DeleteTaskArtifact` 的契約,屬 owner 裁定面。

## 文件版本歷史（T-7d33）：三類文件的還原行為為什麼不一樣

`document_history` 對四類可覆寫長文各留最近三版（migration 00043）。**還原的前置條件三類不同，那是刻意的，不是漏寫**——加第四類文件的人先讀這一段：

**T-1f39 之後手冊不是一個 kind 而是兩條獨立序列**：`task_manual_sop`（`{sop_md}`）與 `task_manual_learnings`（`{learnings}`），key 都是 `type_key`，各自留三版、各自修剪；`purpose`／識別鍵／`display_name`／`assignee` **完全不留版**（owner 裁定）。還原時的 rune 上限（`doc.cap_chars.manual`，數字見 `domain.go`）只檢查該序列真的寫回的那一欄。舊的 `task_manual` 四欄整包**已退場**：owner 2026-07-31 裁定「不需要管舊歷史，舊歷史資料直接刪除就好，不要留技術債」，migration `00045_drop_legacy_task_manual_history.sql` 刪掉全部 9 列（不備份，owner 在知情下選的；Down 是**明示的 no-op**，救不回來）。`documentHistoryAllowed` 現在對 `task_manual` **回 400 並指名兩個新 kind**——回空清單的話「沒有歷史」與「你用錯 kind」會長得一模一樣。常數 `docKindTaskManual` 只為了認出這個舊名字而留著，不再有任何讀寫路徑。

- **`role_definition` / `task_manual_sop` / `task_manual_learnings`：文件必須還存在，否則 404。** 還原是「把這份文件退回舊狀態」，不是「用一份歷史把已刪的文件生回來」。刪掉的角色／手冊要回來，走建立流程。
- **`lessons`：折疊不要求角色存在**（`foldLessonsDTO` 只讀 overlay ⊕ seed，從不查 `role_def`），所以在角色已刪的情況下，**admin 以上**仍可把它的 lessons 還原成一列活的 overlay。一般 agent 過不了「只能寫自己角色」那道閘（已刪角色不再是任何人的角色），但那是**授權**擋下來的，不是這條路徑不存在。
- **`global_context` 沒有刪除入口**，不會有這個問題。

⇒ **刪除文件時必須在同一個交易裡刪掉它的歷史**（角色：自己的 `role_definition` 歷史 ＋ 每一份 `<role>::*` 的 lessons 歷史；手冊：`task_manual_sop`、`task_manual_learnings` **兩條序列全清**；退場的那個舊 kind 不在其中，00045 之後它一列都不會再存在）。少了這一步，刪掉的文件會留下**任何持 token 的 caller 都讀得到**的殘影（`GET /api/document-history/{kind}/{key}` 是 machine 地板），而且 `docs/guide/members.md` 那句「永久移除」會變成假話。串接補上之後，上面 `lessons` 那條路徑就沒有素材可用了，**但不對稱本身還在**——所以寫在這裡。

🔴 **日後要盤點孤兒歷史列時的誤報坑**：判「這個 `document_key` 的文件還在不在」**不能只看 `role_def` 有沒有那一列**——**seed 角色在沒被自訂過之前根本沒有 `role_def` 列**（overlay 模型：沒有 overlay ＝ 用 seed），於是 seed 角色的 lessons 歷史會被**整批誤判成孤兒**。判準要併上 seed 角色清單，來源是 `assets.go` 的 `seedRoleKeys()`（不要手抄一份會過期的名單）。**一個會把正常資料標成待刪的盤點查詢，比沒有盤點查詢危險。**

## 累積型 context 文件的上限(T-3351 + T-3aeb,`domain.go DocCapBlocked`)

owner 2026-07-27 兩句話 + 一個數字:「更新的時候不能塞超過這個大小」「已經超出的我們不 truncate 但是下次更新他只能縮小」,上限當時是 **10,000**(那個數字後來變成設定、又被調過,見下)。

🔴 **T-3aeb(owner 2026-07-31):那個數字不再是常數,是設定(預設 = `contextDocMaxCharsDefault`,範圍是「下限＝預設、上限 100000」)。** 三行語意一個字沒動,動的只有「上限從哪裡來」:

🔴 **T-ae38(owner 2026-08-03):一個設定變成四個,而且舊鍵被改名。** owner 原話(**史料,不是現況的數字**——他當天稍後就把 insight/learning 改掉了,而且三者本來就是可調設定):「我預期 duty 1000 / insight 10000 / learning 10000 但是三者都可以調整」。三行語意**仍然一個字沒動**,動的還是只有「上限從哪裡來」。⚠️ **本檔以下一律不複述那四個數字**——它們是設定值,複述一次就多一句必然過期的話;要看數字請讀 `domain.go` 的 `dutyCapCharsDefault` / `contextDocMaxCharsDefault`:
- **四個鍵,每個都有後綴**:`doc.cap_chars.duty`(預設 = `dutyCapCharsDefault`)/ `.insight` / `.learning` / `.manual`(三者共用 `contextDocMaxCharsDefault`)。**四個的範圍都是「下限＝該段自己的預設、上限 100000」**。舊的 `doc.cap_chars` **被 migration `00048` 改名成 `.manual`**(值原樣搬過去),**DB 裡不會同時有兩個**。
- 🔴 **為什麼改名而不是把舊鍵留給手冊**:agent 在 `get_settings` 的回應裡**只看得到鍵名、看不到 description**。`doc.cap_chars` 擺在三個有後綴的鍵旁邊,讀起來就是「全域預設」——想調手冊上限的人會去動它、**以為自己調到了全部,而不會有任何東西叫**。「留原名可以零遷移成本」這個理由不成立:值本來就得搬到 per-segment 語意,migration 一定要寫。
- **四個 accessor,沒有 `docCap(segment)`**:`dutyCap()` / `insightCap()` / `learningCap()` / `manualCap()`。段別是**寫入 seam 的性質、不是 runtime 參數**,做成參數就會讓某個呼叫點傳錯一個而照樣編譯得過;現在 reviewer 只需要看名字。
- 🔴 **Duty 的上限要補兩處,不是一處**:`api_roles.go` 的 `HandleUpdateRoleApiRolesRolePost`,**以及 `api_document_history.go` 的 `case "role_definition"`**。只補編輯門等於形同虛設——**編到 999 再還原一個 4,000 字的舊版就繞過去了**。lessons 與 insight 在同一個 switch 裡本來就有檢查,**只有 role_definition 沒有**,那正是這張票要補的洞。
- **`RoleDefDTO` 補 `size_chars` / `cap_chars`**。一手證據:Mira 整理完自己的 Duty **根本不知道自己剩幾字,得回頭請別人用尺量一次**。少了這兩欄,每個 agent 收斂 Duty 都得找一個「有尺的人」,而通常沒有那個人。
- 🔴 **出廠 seed 是構造上的例外,但今天沒有實例**:`reset_role` 寫的是 tombstone、fold 回檔案 seed,**那條路上沒有任何 cap 檢查,構造上攔不到出廠內容**。所以「Duty ≤ 1000」的實際語意是「**手寫的 ≤ 1000,出廠 seed 構造上豁免**」——這句在構造上仍然對(那條路上還是沒有 cap 檢查)。⚠️ **但現在的出廠 Duty `seeds/role_def_assistant.md` 遠低於 1,000 這個預設上限**,所以那個豁免**當下沒有任何東西在行使**。(**這裡刻意不寫它幾個字**——seed 改動的頻率遠高於這份文件,寫死一個字數只是預約下一句假話;要知道多少就去讀那個檔。)**不要照著舊的 4,594 這個數字去推論或去修任何東西,也不要去找一份「超標的 seed」來修。**
- **界線**:`global_context` **今天仍然完全沒有上限**(`task.description` 是刻意沒有),本票一個字都沒動它;`docs/guide/settings.md` 那句對 owner 承諾「全域情境與角色定義沒有上限」已在同一批改成「角色定義現在有上限、全域情境仍然沒有」。手冊那兩份長文的**行為**一個字沒動,只換了它們吃哪一個鍵——它們的鍵是 `type_key`,是任務**類型**的資產、不是角色誌,所以歸 `.manual`。
- `contextDocMaxChars` 這個常數**已經不存在**,取而代之的是 `contextDocMaxCharsDefault`(只是預設值)。`DocCapBlocked` 與 `docCapRefusal` 現在**第一個參數就是 cap**——刻意不做成 package 變數或 `*apiServer` 方法:參數化之後,**漏改一個呼叫點是編譯錯誤**,而那正是這種「同一個值散在 9 個地方」的改動唯一可靠的守衛。
- 每個呼叫點在 **request time** 讀 `s.docCap()`(RLock),所以 PATCH 完下一次寫入就生效、免重啟;不快取、沒有第二份會漂的副本。`update_task_manual` 那個 handler **一次讀、兩個欄位共用**——兩次讀可能跨過一次並行 PATCH,讓 sop_md 與 learnings 被兩個不同的上限審判。
- 🔴 **範圍的下限等於預設值,這是設計不是巧合**:owner 明示這個旋鈕只能**調高**。調低會讓今天合法的文件立刻變成 shrink-only(立案時實測:9 份手冊裡 2 份已超過一萬字、3 份在 8.6k~10k),所以 `minDocCapChars = contextDocMaxCharsDefault`,別「順手整理」成一個更小的數字。⚠️ **T-ae38 之後這是 per-segment 的**:`minDutyCapChars = dutyCapCharsDefault`,**不要把它「統一」成 `minDocCapChars`** ——那會讓 owner 拍板的 Duty 預設值從設定面**構造上不可達**。
- **回應同時報「現在多少字／上限多少」(owner 2026-07-31 第二次裁定,卡 `rc-3800e090f5e1`)**:`size` 這個不帶單位的名字**已移除**,改名 `size_chars`,並在兩個讀取面與兩張回條都加上 `cap_chars`。理由是 owner 要「寫之前就知道自己多長」——設定面是 admin-only,**一般 agent 讀不到上限**,所以上限必須跟著文件本身送出去,否則唯一的學習途徑就是被拒絕。
  - **手冊報兩個尺寸(`learnings_chars` / `sop_md_chars`)不是一個總和**:cap 是逐份套用的,一個合計數兩個問題都答不了。
  - 🔴 **`?view=list` 這條輕量投影量的是「資料列」不是「被清空的 wire 欄位」**:它刻意不送 sop_md/learnings 的內文,若順手拿被清空的欄位去數就會回 0——**一個看起來像量測結果的 0,比誠實省略更糟**,而「哪一份手冊快到上限」正是列表視圖會問的問題。哨兵 `TestListViewOmitsTheTextButNotItsSize`(mutant:把兩個尺寸寫死 0 → 紅)。
  - `newTaskManualDTO` / `newTaskManualListItemDTO` 改成**收 cap 參數**(同一個理由:漏改的呼叫點是編譯錯誤);列表 handler **整批只讀一次 cap**,免得同一份清單報出兩個不同的上限。
- **前端那份鏡像也跟著吃設定**:`docCap.ts` 匯出的是 `DOC_CAP_CHARS_DEFAULT`,`docCapBlocked` / `docCapBlockedFields` 都收 cap;cap 還沒載到時**棄權不標記**(與 `current` 未載入同一個姿勢)。理由是方向性的:cap 只會被調高,拿預設值去判只可能**把 server 會接受的版本標成不可還原**——正是那個模組自己說「更糟」的那一種錯。

- **單位是 rune,不是 byte**(`utf8.RuneCountInString`,對齊 `chatBodyMaxChars` 的口徑)。owner 挑那個數字時看的分佈是 SQLite `length()`=**字元**;這些文件多為中文散文(2.2–3 bytes/字),拿 `len()` 當上限實際只剩約 3,300–4,500 個中文字,**比 owner 拍板的數字嚴格一倍以上**。⚠️ **T-3aeb(owner 2026-07-31)已把這條反轉:patch receipt 的 `size` 欄現在也是 rune**。舊文寫「凍結 wire 欄,不動,兩者單位不同是刻意的」——owner 明示同一件事不該有兩個單位,親自推翻了那個決定(卡 `rc-33b88ed80212`,選項 ①)。動它之前先確認:實查當時全 repo **沒有任何** UI／CLI／conformance 依賴舊單位,而且**所有既有測試在兩種單位下都會過**(夾具全是 ASCII),所以那次改動本身在 CI 上是隱形的——鑑別力靠新加的多位元組夾具,不是靠既有測試。
- **三行語意(邊界含在內)**:新內容 ≤ L → 過;> L 且 **< 舊內容** → 過(允許超標者繼續往下縮);> L 且 **≥ 舊內容** → 拒,**含等長**(沒變短就不算收斂,不然超標文件可以永遠整份重寫)。**既有超標內容一律不動、不截斷**——這條規則只擋 WRITE。第一次寫(無舊內容)只看 ≤ L。
- **比的是「caller 讀到、正在編輯的那份」**:lessons 用 `foldLessonsDTO` 的 overlay ⊕ seed 折疊結果,手冊用存的欄位。**刻意與 `LessonsShrinkBlocked` 用同一個 `before`**,兩道閘因此不可能對「現在的文件是什麼」各說各話。(`seeds/lessons.md` 實測 **40 bytes = 14 runes**——這張票的主旨就是 rune≠byte,所以兩個單位都寫明、不含糊講「字元」;不論用哪個尺都遠小於 L,所以 seed role 的第一次寫實務上就是「只看 ≤ L」,fold-vs-overlay 在現行 seed 大小下行為不可區分。)
- **五個寫入面全覆蓋**:`replace_lessons` / `patch_lessons` / `write_task_learnings` / `patch_task_learnings` / **`update_task_manual`**。最後一個非補不可:它是 `sop_md` 的**唯一**寫入面,也是 learnings 的**第二個**寫入面(欄名 `learnings`,與 `write_task_learnings` 的 `text` 是同一份文件)——只擋前四個等於留一扇沒上鎖的門。**patch 面受檢的是「改完之後的結果」,不是 patch 自己的大小**(小 patch 疊上大文件正是把它撐大的東西)。
- **與 `allow_shrink` 的關係:兩道閘方向相反、並存、互不代勞**。`allow_shrink` 擋的是「縮太多」(整份清空 / 縮到不到十分之一),這道擋的是「太大且沒變短」。**`allow_shrink=true` 不是本閘的旁路,本閘無條件檢查**;拒絕訊息也**刻意不提任何參數**——這是 fail-closed 閘,教人繞過等於把 owner 拍板的上限做成裝飾。⚠️ 兩者**不是「由構造互斥」**(此處原本這麼寫,是錯的):`LessonsShrinkBlocked` 的 `TrimSpace(after)==""` 那一臂**沒有長度上界**,所以 `before` = 20,000 個 `a`、`after` = 25,000 個空白會被**兩道閘同時擋**(實測 2026-07-27:cap=true、shrink=true)。**但不會讓任何人無路可走**:重疊區**只有「新內容 trim 後全為空白」這一種**,而那本來就該被擋——它不是「往下收斂」,是清空。**after trim 後非空白時,兩閘確實互斥,而且可證**:shrink guard 要 `len(after)*10 < len(before)`(bytes),本閘要 `runes(after) ≥ runes(before)`;UTF-8 下 `runes ≤ bytes ≤ 4×runes`,由前式得 `runes(after) ≤ bytes(after) < bytes(before)/10 ≤ 0.4×runes(before) < runes(before)`,與後式矛盾。單位不同也不破(兩閘一個是 byte 一個是 rune)。實測掃過 100 組隨機非空白配對 + ~19 萬組網格配對,零反例。(43k → 4k 走 `allow_shrink`;43k → 40k 兩閘都不擋。)
- 🔴 **「超標但變短」這條路一定會被用到**:立案時正式站有 **2 份 lessons**(43,029 / 12,132)與 **4 份 manual learnings**(19,336 / 14,691 / 11,031 / 10,557)已超標(sop_md 全數在界內,最大 3,546)。掉了這條,那六份文件**永遠改不了,連想變短都不行**。
- **代價是具體的**:這是 agent 換手前把該輪教訓寫回去的那條縫,誤擋一次 = 那一輪的經驗無聲全掉。所以拒絕訊息(`docCapRefusal`,五個面共用一份、不可能漂成五種說法)一定講出**三個數字**(這次多長 / 上限 / 現在多長)與**唯一合法出路**(把這次寫的弄短:同一次寫入裡先刪過時內容)。seeds 同批教(`system_interaction.md` §9a / `worker_context.md`)——agent 只知道 seed 教的做法。
- 哨兵 `api_context_cap_t3351_test.go`:(a) 超標被擋 ×6 面、(b) 合法寫入不被誤擋(含「9,000 中文字必須放行」的 rune 單位釘)、(c) 超標但變短放行 ×5 面、(d) 等長且超標被擋。四件**分開釘**,但⚠️ **一個 mutant 未必只點名一件**(逐顆實測 2026-07-27,每顆前 `go clean -testcache`):整條閘拿掉 → (a) 6/6 紅 + (d) 5/5 紅,(b)(c) 全綠;`>=` 改 `>` → 只有 (d) 紅,5/5;**rune 改 byte → (b) 4/6 紅、而且 (d) 也紅 3/5**(此處原本記成「中文那條 + (b) 紅」,漏了 (d),已更正)。(d) 會被這顆 mutant 掃到的根因在測試夾具、不在生產碼:`capDoc` 的錨 `«DROP-THIS-SECTION»` 是 19 runes = **21 bytes**,而 (d) 的等長對照物是純 ASCII 的 `strings.Repeat("q", runes(before))`,在 byte 尺下比 `before` **短 2 bytes**,被誤判成合法縮小而放行;(d) 走 patch 面的兩個子測換的是等 byte 數的錨,所以仍綠。**要拿哨兵當單一診斷訊號的人請注意這顆是跨組的**——看到 (b)(d) 同時紅,先懷疑單位而不是 `>=` 邊界。

## 角色誌的第三塊：Insight（T-3809，`api_insight.go` + migration `00047_role_insight.sql`）

owner 2026-07-28 逐字：「其他人不需要知道 Insight，但是 **Insight 跟 Learning 也不應該混在一起**，後者應該是基於環境學習到的一些 Q&A」。角色誌從此是**三塊**，而三塊在儲存上是三個地方、不是一個欄位裡的三段：

- **Duty**（這個角色負責什麼）＝ `role_def.definition_md`，**本票一個位元組都沒動**（也**沒有**任何字數上限——`HandleUpdateRoleApiRolesRolePost` 從 `body.DefinitionMd` 到 SQL 通篇無尺寸守衛，owner 2026-08-01 裁定 2 明文禁止本票替它補上限）。
- **Insight**（這個角色怎麼權衡：反覆用到的取捨與界線）＝ **新表 `role_insight`**，上線時**每個角色都是空的**。
- **Learning**（環境事實 Q&A）＝ `lessons.text`，**本票零改動**。

🔴 **零自動切分**（owner 2026-08-01 `rc-87e850241ef4` 選 ②）：migration `00047` 裡**沒有任何 `UPDATE`、沒有 `INSERT … SELECT`**，今天的內容原地不動，由各角色自己搬。**已知代價，明說**：上線當天 Insight 全空，本票開票的痛點（同一把尺量刪除成本差十倍的東西）**一個字元都沒改善**——交付說明不得含糊帶過。理由是「切錯比切慢貴」：一句判準被誤歸成環境事實，下一次撞上限時會被刪掉，而刪的人不會知道自己刪掉了什麼。

### 三條端點與它們的門檻

| route | MCP tool | `Requires` | 實際閘 |
|---|---|---|---|
| `GET /api/insight/{role_key}` | `get_insight` | `principalMachine` | 無（讀不設限） |
| `POST /api/insight/{role_key}` | `replace_insight` | `principalAgent` | `insightWriteAuthz` |
| `POST /api/insight/{role_key}/patch` | `patch_insight` | `principalAgent` | `insightWriteAuthz` |

- 🔴 **讀取一律不設限，而且要到處講出來。** owner 2026-08-02 於 `rc-dc171587220c` **點選項 ①**，逐字「**包含 Insight：這一輪不關任何讀取（維持我現在的做法）**」⇒ READ 停在 machine 地板，與 Duty（`GET /api/roles` 是 `principalMachine`）、Learning（`routes.go` 逐字 `READ stays on the machine floor`）完全一致。**Insight 是「分開的」，不是「私有的」**——`insight` 這個字本身會讓讀者以為有人承諾過機密性，所以這句交付語必須出現在**每一個讀者到得了的面**（`spec/openapi.json` 三條 description、`seeds/system_interaction.md` §9b、`docs/guide/settings.md` 的 `## 角色誌` 節、`InsightCard` 卡頭）。任何一處把它寫成「私有／別人讀不到」都是**假話**。
- 🔴 **`insightWriteAuthz` 是自己一支，不准改成呼叫 `lessonsWriteAuthz`。** 那支把 `lessons` 這個字**寫死在 403 body 裡**；複用它會讓一次被拒的 insight 寫入回「an agent may only write its own role's lessons」——聽起來對、指錯文件，caller 的下一步（去翻 lessons）整個白費。insight 的 403 逐字是 **`an agent may only write its own role's insight`**。**同一個病也在 anchor-miss 訊息上**，所以 `ApplyDocEdits` 收「要用哪個工具重讀」而不是把 `get_lessons` 烤死。⚠️ 新增一個 authz 站點會讓 `authz_surface_gate_test.go` 的 `TestAuthzOutsideTheRouteTableIsEnumerated` 變紅，直到有人把它連同**理由**寫進清單——這是刻意的，閘會逼實作者具名。
- 身分照憲章 §14 取自 verified `sub` 的名冊列，**不是請求欄位**。admin capability 以上寫任何角色，其餘只寫自己的 `role_key`。**沒有 role 的 caller 是零、不是「只能寫自己的」**——`memberRole == ""` 永遠不等於路徑上永遠非空的 `role_key`，外包 worker 首當其衝。這是要的姿態：worker 沒有角色，沒有自己的 Insight 可整理，也不該去整理別人的。

### 與 lessons 逐項不同的地方（每一項不同都是「共用就會說錯話」的地方）

- **key 只有一個**。lessons 的 key 是 `(role_key, task_type)`，insight 沒有 task_type 軸，`document_history` 的 key 就是**裸 `role_key`**。🔴 **這在 cascade 上是會咬人的**：lessons 的串刪用 `"<role>::"` **前綴**比對（那裡安全，`::` 這個終結符讓 `r-abc::` 不可能打中 `r-abcdef::general`），而單鍵文件**沒有終結符** ⇒ `DeleteInsightForRole` 一律用**精確相等**（`document_key = ?`，跟 `DeleteRoleDef` 同姿勢）。**在這裡用前綴會把 `r-abcdef` 的歷史跟著 `r-abc` 一起刪掉。** 刪角色的串接在 `api_roles.go` 的 delete_role 路徑上。
- 🔴 **seed 有了，而且是 PER-ROLE（T-e1e3 推翻 T-3809 的「沒有 seed 檔」）**。owner 要「所有裝 OffiCraft 的人」的助理一出廠就懂怎麼幫人管 context，而那套知識全部屬於 Insight。所以 `foldInsightDTO` 現在折的是 overlay ⊕ `seeds/insight_<roleKey>.md`（`assets.go seedInsightMD`），**今天只出貨 `insight_assistant.md` 一份**。
  - 🔴 **絕對不可以做成 lessons 那種「全站共用一份」**。lessons 對任何 roleKey 都讀同一個 `lessons.md`；照抄那個形狀會讓**每個角色出廠都拿到助理的判準**，而銀月的判準對測試員、對工程師是錯的。per-role 由**檔名內插 roleKey** 保證，而 `seedRoleName(roleKey) == ""` 那道前置閘（與 `seedRoleDefinitionMD` 同型）順帶由構造排除路徑穿越。
  - **`fs.ErrNotExist` 不是錯誤**：有 Duty seed 但沒有 Insight seed 的 seed 角色，仍然「沒寫過就是真的空的」；其餘 IO 錯誤照樣往上拋（fail-closed）。
  - 🔴 **`is_default` 的語意沒變，蘊含變了**。它一直是「這個角色還沒寫過自己的 Insight」；T-3809 額外倚賴的等價 `is_default == true ⟺ text == ""` **現在只對沒有 seed 的角色成立**。任何拿「空」當「還沒搬」的消費者都要改讀 `is_default`——**座艙首當其衝**：`InsightCard` 從前完全沒讀這個欄位，所以出廠文案會原樣渲染成一份看起來是人寫的文件。現在它掛「預設」徽章（`data-testid="insight-default-badge"`，且 **gated on 非空 text**——沒有 seed 的角色也是 `is_default=true`，把它的空卡標成「預設」等於把「沒有東西」講成「出廠文件」）。
  - **「這個角色還沒搬」仍然答得出來**，只是答案改由 `is_default` 承載，不再由空字串承載。
  - **`api/mock.ts` 同批鏡像**：`INSIGHT_SEEDS` 是一個 **Map**（`{assistant: SEED_INSIGHT_ASSISTANT_MD}`），不是單一常數——mock 若照抄 lessons 形狀，座艙會對著一台「錯得一模一樣」的假 server 變綠。`frontend/src/api/seeds.ts` 以 `?raw` 直讀 `seeds/insight_assistant.md`，零第二份。
  - **`api_document_history.go` 的 restore（`case "insight"`）沒動，但行為隨之改**：cap 檢查的 `before` 現在含 seed，所以 assistant 從沒寫過時，還原一個比 seed 短的超標舊版會放行——**與 `role_definition` 今天的行為一致**，不是新的不對稱。
  - 哨兵 `api_insight_seed_te1e3_test.go`（檔頭寫明**鑑別力由 mutant 承擔**：本票在改動前根本沒有這條路徑，任何「讀得到出廠內容」的斷言只能以編譯失敗這種退化紅成立）。三顆 mutant：per-role 判斷改成共用一份 → 「其他角色仍為空」紅；seed fold 拿掉 → 「全新 DB 讀得到出廠內容」紅；寫過之後仍回 seed → overlay 語意紅。
- **guard 姿態與 lessons 對齊**：整份替換走 `WholeDocWipeBlocked`，patch 走 `LessonsShrinkBlocked`（`allow_shrink` 可放行），兩條都**無條件**再過 `DocCapBlocked(s.insightCap(), before, after)`——cap 取自 `doc.cap_chars.insight` 設定(T-ae38 之前是共用的 `doc.cap_chars`)、request time 讀，與上一節同一把尺；Insight 拿的是**自己一份預算**，不跟 Learning 共用。回條同樣報 `size_chars` / `cap_chars`。
- **歷史快照在交易內重讀**：`insightSnapshotIn` 跟它的 lessons 雙胞胎一樣，**不信 handler 早先折出來的值**——保留的那一版必須是這次寫入所取代的狀態，否則兩個寫者相撞會保留同一個祖先，中間那一版永久救不回來。
- **SSE 是第 13 個 topic**：`hub.go` 的 `sseTopics` 多一格 `"insight"`；三個寫入面（replace／patch／history restore）都發 `topic="insight"`、key 為 `wireOwnerID+"::"+roleKey`。
- 🔴 **`api_document_history.go` 的三個 switch 一個都不能漏**（`documentHistoryAllowed`／`publishDocumentHistoryRestore`／`restoreDocumentHistory`）。**`publishDocumentHistoryRestore` 是本票唯一的靜默面**：它沒有 `default` 分支，漏掉 `case "insight"` 的後果是 **HTTP 200、DB 已改、零錯誤、零紅**，只是沒有任何人的畫面會更新。守它的是 `api_document_history_insight_publish_test.go`（含 lessons 陽性對照——沒有對照的話「沒收到 frame」可能只是夾具沒接好，負斷言會空綠）。
- **隔離證明**：`api_insight_isolation_test.go` 把某角色的 Insight 寫到 `doc.cap_chars.insight` 上限，斷言 Duty 與 Learning 逐位元組不變、且各自都還寫得動。**陽性對照是那支測試的命脈**——先證明「再多一個字元會被 `DocCapBlocked` 擋」，否則「寫到上限」可能根本沒踩到 cap，三條斷言會一起空綠。cap 一律取自產品讀 cap 的同一條路，不准用測試自己編的數字。
- **agent 面**：`seeds/system_interaction.md` 的 **level-2 `## 9b.`** 是唯一會被每一代 agent 開機讀到的招牌（`## 9.` 的**兄弟節**，不是子節——寫成 `### 9b.` 等於在開機分類表裡把 Insight 歸回 Learning，而且**沒有任何東西會紅**）。⚠️ 該節同時被 `worker_sharedcore.go` 的 `workerSharedCoreExclusions` 排除（`Anchor: "## 9b. "`），理由與 §9 那條相同：本文通篇對「有角色的你」說話，送給沒有角色的 worker 只會製造 T-108b 要消滅的那種殘留。**代價寫在這裡不藏**：worker 讀得到別人的 Insight，卻永遠不會被告知它讀得到——與 §9（Learning）今天的狀態一致，不是本票新造的。日後若要讓 worker 知道，正解是**在 worker overlay 加一句 worker 語境的說明**，不是把 exclusion 拿掉。

## 主題頭像池與持久索引(T-cd6f，取代 T-c826)

- `member` 與 `outsource` 圖片屬於 ThemeBundle 的有序 pool；owner／assistant
  仍沿用單圖。每個 pool 最多 12 張，每張沿用 PNG/JPEG/WEBP、magic bytes 與
  decoded 64 KiB 的既有圖片守衛。
- staff 與 outsource 建立時依 active theme 的對應非空 pool 隨機分配
  `member.avatar_index`；空 pool 配 0。索引持久化，換主題、重啟與換手都不改。
- render 契約是 `pool[avatar_index % len(pool)]`；空 pool 使用 glyph。較短 pool
  不觸發資料庫重寫，因此主題切換不會製造 broken image 或隱性 identity mutation。
- `PATCH /api/members/{member_id}/avatar-index` 是 owner-only、MCP-excluded，
  staff 發 `member` delta，outsource 發 `outsource_worker` delta；warden/machine
  目標拒絕。索引只要求非負，不受當前 pool 長度限制。
- migration `00042` 刪除舊 `avatar_attachment_id` 前先刪其專用 `ava-` blobs，
  並退役 personal-avatar PUT/DELETE route、DTO 欄與 editor。Down 只還原空 pointer，
  不假裝能重建已刪除的個人圖片。

## AI 會話清單 = 正職 ∪ 外包,而且每一欄都是自報值(T-e12c,owner 2026-07-31)

owner 兩句裁定:「理想上應該是同一支 API 同時取得所有 AI session(包含外包跟正職)」「成員面板以及監控台,一定要顯示回報回來的狀態,不能顯示設定值」。

- **`GET /api/monitoring` 的 `sessions` 現在同時含成員與 live 外包 worker**(`api_monitoring.go` 的 `monitoringSessionSource`)。外包經既有的 `memberFromWorker` 進同一個迴圈,**沒有 per-kind 的第二條建構路徑**——兩種列不可能各自漂走。**沒有新增任何 DTO 欄位**:`MonitoringSessionDTO` 是 `additionalProperties:false`,加欄就是動凍結 wire;外包列靠 **`ow-` id 前綴**辨識(既有慣例),`role` 誠實留 `""`(worker 的 member row 本來就 `RoleKey: ""`,不編一個假角色名出來)。
- **每一個遙測欄都取該 actor 自己的 telemetry/gauge entry**(key = token sub,外包就是 `ow-` id);沒回報就 null/`""`,**一律不退回名冊設定值**。
- 🔴 **`model` 這一欄在 T-e12c 當時只做了一半,已於本批補齊(owner 2026-07-31 第二次裁定:「正職跟外包都是用回傳值,不存在第二個選項的什麼正職維持看設定值」)**。當時外包取 `wk.ActualModel`(自報)、**正職仍取 `m.Model`(設定)**,所以同一個欄位標題底下是兩種語意;而更糟的是外包那半**結構上取不到值**:`ActualModel` 全庫只有兩個寫入點、都掛在 `report_waking` 上,而 `seeds/worker_context.md` §2 明文把 `report_waking` 從 worker 的開機序列拿掉(worker 的上線訊號是 `get_my_task`,那條路徑完全不碰這個欄位)⇒ **每個外包 worker 的 model 永遠是空字串**,而空字串與「還沒回報」長得一模一樣。現在:
  - **寫入端接在真正活著的那條管道上**——Claude Code statusLine reporter(`ocagent context-report`)與 codex sidecar 各自把 live model 放進 `POST /api/monitoring/telemetry` 的新 `model` 欄(**上行契約**與 `effort` 相同:回報狀態、量不到就省略;spawn 對兩種 kind 不分支,所以 worker 早就在用自己的 `ow-` id 送遙測——`effort` 有值就是這條管道對 `ow-` 通的活證據)。**不是**把 `report_waking` 加回 worker 開機序列,那是刻意的設計決定。
- ✅ **`model` / `runtime` / `effort` 三者現在對稱,都是持久回報欄(T-7f28,owner 於 `rc-3353ce609038` 核可)**。此前只有 `model` 有持久層:`effort` 只活在 in-memory telemetry entry(server 一 re-exec 全機隊抹白),`runtime` 更糟——telemetry **有收**,但每一條讀取路徑都把它改寫成名冊上的**設定值**,所以回報的 runtime 對外零可見度,而面板上那一格一改設定就變,「還沒生效」與「已生效」長得一模一樣。現在三者都走 `stampReportedLaunchFacts`(write-on-change、空值 no-op 不擦既有值、`kind=outsource` 取 `outsourceMu`)落進 `actual_model` / `actual_runtime` / `actual_effort`,讀取端一律讀持久欄、**都不退回設定值**。哨兵:`TestGetMonitoring_ReportedLaunchFactsSurviveAReExec`(三欄一起)與 `TestGetMonitoring_ReportedLaunchFactsNeverFallBackToTheConfiguredValue`。
  - **`stampReportedModel` 把它落庫**(write-on-change,空值 no-op 不擦既有值,`kind=outsource` 取 `outsourceMu`)。遙測是 in-memory,只存那裡的話 server 每次 re-exec 就把全 fleet 的模型欄清空 —— 就是這個 bug 換個計時器重演。
  - **讀取端兩種 kind 同一行 `m.ActualModel`**;`monitoringSessionSource` 的 `model` 覆寫欄**已刪除**(兩臂都寫同一個運算式的欄位不該留著,再分歧就得改碼而不是改一個 call site)。
  - **代價 owner 已知並接受**:從沒回報過的正職在下次開機前顯示空白。那一列的 effort / cost / account / context_pct **本來就全空**,model 是最後一個還在宣稱自己知道的格子。
- **哨兵**:`TestGetMonitoring_SessionModelRoundTrips`(ingest→GET 往返,三列**各斷言具體字串**——改動前該欄是 `""`,所以 `!= nil` / has-key 這類寫法在這裡是恆真,實測過)、`TestGetMonitoring_ReportedModelSurvivesATelemetryWipe`(把 `s.telemetry` 換成空 store 模擬 re-exec,拿掉落庫那一步只有它會紅)、producer 側 `TestContextReportSendsSessionModel`(送 `model.id` 而非 `display_name`——id 是 boot seed 已經教的詞彙,也是唯一帶 `[1m]` 標記的那個)與 `TestReportTokenUsageSendsSessionModel`(codex;空值省略不送 `""`)。
- **`GET /api/outsource-workers` / `outsourceWorkerDTO` 一字未動**:那份 `model`/`effort` 是 owner 的**啟動意圖**,座艙的編輯器讀它、存回去也是它。⚠️ **把它換成自報值會讓 owner 按一次儲存就把自己的設定改掉(未回報時甚至存進空值,被 closed vocabulary 422)** —— 顯示面與編輯面必須分開,這是 T-e12c 唯一刻意保留設定值的地方。
- **released worker 不進 `sessions`**(`RosterStatusRemoved`,與 members 同一個判準),但**仍留在 `actors`** —— 花費不會因為列消失而消失。`actors` 隨著這台站跑過的每一張任務單調成長,現在式的會話表不能長那樣。
- ⚠️ **已知不對稱**:`sessions` 的 `machine` 用 `observedWorkerHost`(與同一份回應的 machines fold 同一個運算式,兩處不可能各說各話),而**外包詳情面板**走 `projectWorker`、會先看 in-memory 的 dispatch target ⇒ 剛派出去還沒連上的 worker,面板顯示目標機器、會話列顯示 `""`。誠實留白 vs 顯示意圖,兩處的問法不同。
- **agent 端的來源見 `cli/CLAUDE.md` 的 session effort 條**:effort 取 statusLine payload 的 `effort.level`(**live**,跟得上中途 `/effort`),**不是** `OC_EFFORT` 那個啟動意圖,而且**沒有 fallback**。
- 哨兵:`TestGetMonitoring_SessionsListStaffAndOutsourceAlike` / `_WorkerSessionReadsItsOwnTelemetry` / `_SilentWorkerSessionShowsHonestBlanks`(設定了 opus/medium 卻沒回報 ⇒ 全空且**仍然列出**)/ `_ReleasedWorkerIsNotASession`(連帶釘住「拿掉它的列不可以拿掉它的錢」)/ `_SessionEffortRoundTrips`(ingest→GET 往返;這條路先前**零覆蓋**,正是 effort 從沒被送過卻沒有任何東西會紅的原因)。
## 收據死線 receipt_missing(T-b36a step 3;`receipt_watch.go`)
warden 的 start/stop 收據(`command_result`)是「這個 op 真的執行了」的**唯一**證據,而
warden 端那個 POST 是 best-effort:失敗的回傳值被 start/stop 呼叫端丟掉,於是**整個失敗事實
不存在於任何地方**。座艙上「op 跑完了、只是我們沒收到回報」與「一切正常」長得一模一樣。
- **只有被欠收據的那一方能觀察到收據缺席**——warden 沒辦法回報「我的回報失敗了」,那條線本身
  就是壞的那條。所以死線在 server:frame **被 warden 接走之後**(`armReceiptWatch`,只在
  enqueue 成功後掛;沒送出去的 frame 由既有 dispatch stamp 自己解釋,不該賴到收據通道頭上)
  起算 `receiptDeadlineSecs`(90s),cadence tick 開頭 `sweepLapsedReceipts` 掃過期,把
  `receipt_missing: <人話>` 寫進 member / worker 的 `last_op*`——**座艙「最近操作」既有的讀端**
  (`mappers.ts` → `MemberDetailPanel` → `AgentDetailPanel` 的 `!lastOpOk && lastOpReason`)。
  🔴 因此 stamp **必須** `last_op_ok=false`,ok=true 的 reason 在那個面板上根本不會被畫出來。
- **90s 的來歷是 warden 自己的預算推導,不是量測**:`claudeProbeBudget` 20s + `nudgeSettle` 1s
  (spawn)或 ~5s kill ladder(stop)+ `commandReportTimeout` 5s + 一個 30s tick 的掃描粒度
  ≈ 56s,90s 再多留一個 tick。**frame→receipt 的實際分佈至今沒有端到端量測**(warden log 只有
  warden 側時鐘,server 側 fold 不留 log),所以這裡寫的是推導、不是數據——要改成量測值請先補量測。
- **解除死線的是「收據**抵達**」,不是「fold 成功寫進去」**:`noteReceiptArrived` 掛在
  `foldCommandResult` **最前面**,在任何 early return 之前。no-op stop 收據(T-9adc)刻意不 fold,
  身分掃描又會把它廣播給每一台 warden——綁在 fold 成功上會讓這些收到且讀過的收據全被判成失蹤。
- 訊息刻意說 **UNKNOWN 而不是 failed**:沒收到收據不代表 op 失敗,可能是 SSE 下行半路死、可能
  op 跑完 POST 被拒。寫成「失敗」會讓 owner 對一個可能已經在跑的 member 再開一槍。
- 每次 dispatch **只 stamp 一次**(lapsed watch 被第一次 sweep 消費掉):否則每 30s 重寫
  `last_op_at` + 噴一個 SSE delta,一次掉收據會變成永久事件流。
- **繼承已知的單槽覆蓋**(`worker_spawn.go` spawnBlockedReasonCodes 上方那段 KNOWN LIMITATION):
  `receipt_missing` 與其他 reason 共用同一組 `last_op*`,新的會蓋掉舊的。`receipt_missing`
  **刻意不列入** `spawnBlockedReasonCodes`——它跟 `wake_timeout` 同類,是 dispatch 層診斷,
  下一次 START 不該把「上一次沒人回話」洗掉。
- **UNINSTALL 不掛死線**:它的收據在 warden 端本來就是硬條件(沒拿到 2xx 就不自殺),而且
  reconcile 會一直重發——它的失敗模式是重試,不是安靜。

## WAL ＋ 讀池／寫池分開(T-dd7a,owner 2026-07-31「尤其是讀寫分離 請優先上」)

**改動前:`SetMaxOpenConns(1)` 且從未開 WAL ⇒ 全行程每個請求在資料庫層排一列。** owner 的 DevTools 截圖坐實:同一個工作階段裡 0.1 kB 的端點花 904 ms、407 kB 的只花 85 ms——大小不能預測耗時,**排隊位置可以**。

- **兩個池,刻意不對稱**(`migrate.go` / `dal.go`):
  - **寫池** `openSQLite`:`journal_mode(WAL)`(**檔案的持久屬性**,不是連線設定)＋**上限 1**(SQLite 單寫者,多開只是把排隊從 Go pool 搬進 SQLite 鎖管理器,那裡等待是 busy-loop、輸了是錯誤)＋`_txlock=immediate` ＋ `busy_timeout(5000)`。
  - **讀池** `openSQLiteReadPool`:8 條、**`mode=ro`**。**必須在寫池 migrate 完之後才開**(`mode=ro` 不建檔、唯讀連線也救不回 WAL)。
- 🔴 **`_txlock=immediate` 到底在防什麼(這裡最容易講錯,而且被實測糾正過)**:**不是**防我們自己的並行寫者——**上限 1 已經讓行程內的鎖升級衝突構造上不可能**,而這**不是推論**:把 `_txlock=immediate` 從 DSN 拿掉,`TestSaveWithDocumentHistoryUnderConcurrentWritersKeepsTheChainContiguous` **仍然綠**(實測 2026-08-01)。它防的是**同一個檔上的第二個 handle**:`ocserverd backup`(`cmdBackup` 自己開一個)、shell `sqlite3`、未來的第二個池。對那些寫者,IMMEDIATE 才是讓 `busy_timeout` **生效**的東西——沒有它,那種寫者不是等,是直接失敗。**鑑別力測試因此是兩個獨立 handle(`TestWritePool_ReadThenWriteNeverHitsBusy`),不是兩個 goroutine。**
  - 機制:DEFERRED 交易先讀再寫 ⇒ 要把讀鎖**升級**成寫鎖 ⇒ 引擎對升級衝突回**立即** SQLITE_BUSY,而 `busy_timeout` **不涵蓋**這種(等下去會讓兩個升級者互鎖,所以引擎不等)。
  - 🔴 **那個危險是 WAL-only,是上面那行 WAL 引進來的,不是 SQLite 本來就有的**(獨立審查實測,本檔原本把它寫成一般性質):**rollback journal ＋ DEFERRED → 8 筆 0 失敗**;**WAL ＋ DEFERRED → 8 筆 2 失敗,而且 SQLITE_BUSY 在 0ms／1ms 就回來**(對比 5000ms 的 busy_timeout,所以「timeout 沒生效」是看得見的);**WAL ＋ IMMEDIATE → 0 失敗**。錯誤碼本身就是證據——`SQLITE_BUSY_SNAPSHOT(517)` **只存在於 WAL 模式**。⇒ 這兩個 DSN 參數是**一個決定、不是兩件順手的整理**:開 WAL 而不加 IMMEDIATE,等於把「排隊」換成「間歇性寫入失敗」。
  - ⚠️ **IMMEDIATE 的代價(實測,寫下來免得下一個人重新發現)**:它把爭用從「第一次寫」搬到 **`BEGIN`**,所以當寫鎖被**別的 handle** 持有時,`Begin` 會**阻塞**而不是立刻回來。實測(同一檔第二個 handle):持有者永不放手 → `Begin` 等 **5.098s**(整個 busy_timeout)然後才失敗 `SQLITE_BUSY`;持有者 300ms 後 commit → `Begin` 等 **345ms** 並**成功**。第二行就是我們要的交換(排隊、不放棄),第一行是價錢——一個卡住的外部寫者會把「立刻報錯」變成「卡 5 秒」。**咬不到我們自己的寫者**(上限 1 已經在 Go 層把它們排好,那裡等待免費且有序),只咬第二個 handle:對著服役中的 serve 跑 `ocserverd migrate`、或一個開著交易沒關的 shell `sqlite3`。
  - ⚠️ **`TestWritePool_ReadThenWriteNeverHitsBusy` 鑑別的不是「改動前 vs 改動後」**——它在**父 commit 上就是綠的**。它鑑別的是「**WAL 之下** IMMEDIATE vs DEFERRED」。也**不是**「證伪簡化版的那條測試」:證伪 `WAL + 上限 8` 的是完整 CI 紅在 `TestSaveWithDocumentHistoryUnderConcurrentWritersKeepsTheChainContiguous`,當時本測試還不存在。(本檔與 commit 訊息原本都把它講寬了。)
- 🔴 **已被證伪的簡化版**:只開 WAL ＋把上限 1→8(分支 `t-dd7a-wal`、commit `25bf66d`)**完整 CI 是紅的**(那條並行寫入測試 SQLITE_BUSY)。`_txlock=immediate` 也是「哪天有人把上限調高」時唯一還撐著的東西。
- **寫入路徑怎麼保證沒漏**:`DAL` 的欄位**不叫 `db`**,叫 `wdb`/`rdb` ⇒ 每個呼叫點都被編譯器逼著指名一個池,**分類由構造窮盡**,不靠一份要記得更新的清單。
  - ⚠️ **編譯器只逼你選,不會告訴你選對了**,而且有**恰好一種**選錯也能通過型別檢查的形狀:`sqlExecer`(寫 seam)與 `sqlQuerier`(讀 seam)都被 `*sql.DB` 滿足,所以 `putChatOn(d.rdb, m)` 編得過,**而且 SQL 在 seam 裡、不在呼叫點**,看呼叫點也看不出來。這個洞由 `TestReadPoolIsNeverHandedToAWriteSeam` 補(AST 兩趟:先找出收 `sqlExecer` 的函式,再看有沒有人餵 `.rdb` 進去)。
  - 🔴 **兩個掃描的語料是「本 package 全部非 `_test.go` 的 `.go` 檔」,不是檔名清單——這一點被獨立審查抓過,而且是最糟的方向**:第一版 glob `dal*.go`,而它自己的註解卻宣稱「derived, not enumerated: any file declaring methods on \*DAL is in scope」。**那句是假的**,而且它會讓下一個維護者相信新增的檔自動在射程內。審查放了一個 `store_review_probe.go`(兩個**真缺陷**:`func (d *DAL)` 裡 `d.rdb.Exec(INSERT…)`、以及 `putChatOn(d.rdb, m)`)⇒ **三道守衛全綠**;`len(out) < 3` 那道下限救不了——當時剛好就是 3 個檔,下限被**正在漏掉缺陷的那個集合**滿足。`dal*.go` 這個前綴從來沒有承重意義(DAL 方法或 `sqlExecer` seam 可以宣告在任何檔名裡,而 seam 掃描的 pass-1 必須看得到全部宣告,否則 pass-2 認不出誤路由)。**下限現在刻意設得遠低於實際檔數**(30 對上數十個)——貼著當前數字的下限本身就是第二份清單。
  - **實測(2026-08-01,兩個方向都跑)**:同一顆探針在**新語料下兩個掃描都紅並各自點名 `store_review_probe.go:8` / `:13`**;把語料改回 `dal*.go`(探針留著)**兩個掃描都綠** ⇒ 差別確實來自這個修正,不是探針恰好被別的東西抓到。
- **執行期自檢(`server.go`,migrate 之後)**:`assertJournalMode` **問資料庫現在是哪個模式**,不是斷言 DSN 字串——**打錯的 pragma 被 SQLite 靜靜忽略**,字串檢查會跟正確的一樣開心地通過。不符就大聲 WARNING,**刻意不 fatal**(慢的工作室勝過起不來的工作室,與 pre-migration 備份鉤同一個取捨)。**實測**:把 pragma 打成 `journl_mode` ⇒ 模式其實是 `delete`、serve 印出 `WARNING: journal_mode is "delete", want "WAL"` 然後**照樣繼續開機**;正確版印 `journal_mode=wal`,`/health` 回 `{"status":"ok"}`,資料目錄下確實有 `officraft.db-wal` 與 `-shm`。
  - 🔴 **呼叫點本身要有測試,不只函式**(獨立審查 M6:把 `server.go` 那整段 `assertJournalMode` 呼叫**刪掉,全套件 0 條紅**)。函式與 `openSQLite` 的 WAL 各有測試,但「serve 真的會問」沒有任何守衛——**而這段自檢是那個不可見缺陷唯一的線上訊號**,可以被無聲移除的自檢不是自檢。`TestServeAsksTheDatabaseWhichJournalModeItIsIn` 用**先占住 port** 的既有樣式(同 `server_test.go` 的孤兒卡開機測試)真的驅動 `cmdServe`:開檔 → 備份鉤 → goose → **這段檢查** → 讀池 → seed,然後在 bind 上 exit 1;**rc==1 本身就是「整條開機路徑真的跑完」的證據**。實測刪掉那段 → **整個 package 恰好 1 條紅**(就是它)。
- 🔴 **WAL 之後資料庫不再是一個檔**:最新 commit 可能還在 `-wal` 裡。所以**不得以單檔方式複製或移動資料庫**——那種備份會「看起來成功」、還原時少掉最近的資料。守衛 `db_singlefile_copy_guard_test.go` 是**一條必須回 0 行的查詢**(不是已知位置清單:清單只知道有人想到過的那些)。Go 面走 AST(註解與字串字面值不是運算式節點,所以掃描不會匹配到自己的說明文字——本 repo 記過這個失效模式);腳本面走文字,而守衛自己是 `.go`,**由構造**不在文字掃描範圍內,不需要任何自我豁免。**`VACUUM INTO`(`backup.go`)不是這條守衛要抓的對象**——那是 SQLite 自己的線上備份、WAL-safe;誤傷它會把下一個人推離唯一正確的機制。運維面的話寫在 `docs/guide/troubleshooting.md`「想自己備份或搬移資料庫」。
  - **誠實邊界**:守衛抓的是「資料庫路徑**直接**交給複製／搬移動詞」。它不做 dataflow,所以 `f,_ := os.Open(dbPath); io.Copy(dst, f)` 繞幾層可以躲過;也看不到我們只是呼叫的外部程式在做什麼。是絆線,不是證明。
- **mutant 實測(2026-08-01,每顆前 `go clean -testcache`,還原一律用 scratchpad 備份、不用 `git checkout --`)**:
  - 寫入接到讀池(直接形狀,`PutAccountAlias` → `d.rdb.Exec`)→ **2 條紅**:`TestNoWriteStatementRunsOnTheReadPool`(點名 `dal.go:1334` 與那句 INSERT)、`TestWritesAllLandThroughTheRealSplitPools`(`attempt to write a readonly database (8)`)。
  - 寫入接到讀池(**seam 形狀**,`putChatOn(d.rdb, m)`)→ **2 條紅**:`TestReadPoolIsNeverHandedToAWriteSeam`、`TestWritesAllLandThroughTheRealSplitPools`。⚠️ **`TestNoWriteStatementRunsOnTheReadPool` 對這顆維持綠**——這正是 seam 掃描存在的理由,別把它當成重複的守衛。
  - 拿掉 `_txlock=immediate` → **1 條紅**:`TestWritePool_ReadThenWriteNeverHitsBusy`(8 筆中 4 筆失敗,`database is locked (517)`)。⚠️ `TestSaveWithDocumentHistoryUnderConcurrentWritersKeepsTheChainContiguous` **維持綠**(上限 1 的緣故,見上)。
  - pragma 名字打錯(`journl_mode`)→ **1 條紅**:`TestOpenSQLite_IsActuallyInWAL`(實際模式 `delete`)。
  - 單檔複製守衛:Go 面注入 `os.Rename(dbPath, dbPath+".handcopy")` ＋腳本面注入 `cp "$DB_PATH" /tmp/handcopy.db` → **兩個半邊各自點名正確位置**(`backup.go:260`、`bin/install.sh:921`)。
  - **掃描語料**(review 項 A):`dal*.go` **之外**的檔放兩個真缺陷 → **2 條紅**(`TestNoWriteStatementRunsOnTheReadPool`、`TestReadPoolIsNeverHandedToAWriteSeam`,各自點名 `store_review_probe.go:8` / `:13`);把語料改回 `dal*.go`、探針留著 → **全綠**(反向對照)。
  - **serve 的自檢接線**(review 項 C):刪掉 `server.go` 那整段呼叫 → **整個 package 恰好 1 條紅** `TestServeAsksTheDatabaseWhichJournalModeItIsIn`;**改動前這顆 mutant 是 0 條紅**。

### 🔴 備份的停等成本:讀者不等了,寫者照樣等(review 項 B 更正)

`backup.go` 曾經在 WAL 上線後寫「a writer no longer waits for the duration either」——**那句可證為假**。獨立審查在 **78 MB 資料庫、WAL 已開**的情況下量到:**vacuum 108ms / writer_wait 86ms / reader_wait 3ms**。

- 原因**與 journal mode 無關**:`VACUUM INTO` 那個 Exec 佔用**寫池唯一那條連線**(`openSQLite` 上限 1),所以 Go 的 pool 層在 SQLite 還沒被問到之前就把其他寫者排住了。
- ⇒ **WAL 只買到「讀者不等」**。任何人要回答「備份對這個工作室的成本是多少」,**一定要報寫入那一側的數字**;引用讀者那個 3ms 去講「備份不影響服務」是錯的。
- 舊的量級參考仍有效:**~0.43s / 340 MB**(WAL 之前量的)。
- **反恆真**:每個掃描都先自證語料非空(Go 檔數、腳本檔數、**以及看到的動詞數**——動詞數為 0 代表偵測器死了,那時「零發現」什麼都不證明);寫入取樣表有下限;所有「寫成功」都以**讀回來**收尾,不以「回 nil」代替。
## 任務清單:問狀態集合,並且自己把 dep 組好(T-a3e4,owner 2026-08-01)

owner 原話三句:「tasks 也一次拿太多了 說真的我們在意的只有 filter 過後的那二十幾個」
「**不是應該以狀態 filter 嗎**」「有相依性的話 **那個相依性為什麼不是後端幫忙處理好組起來**」。
實測基準(正式站 DB,703 張):`GET /api/tasks` = **408,482 B** vs `?open=true` = **17,295 B**。

- **`?statuses=`(可重複)是新的過濾面,`?status=` 一字未改。** 為什麼開第二個參數而不是把
  舊的加寬:舊的 `status=` **會對 `reassigning` 回 400**(`ValidTaskStatus` 不含它,T-9ca5 把它
  搬去 `task.lock` 之後訊息文字沒跟著改,所以那句 400 的內容至今**列了一個它自己會拒絕的值**
  ——本批刻意沒動那句話,免得改到現役 client 讀得到的字),而且它只比對 status 欄。集合面要的
  是**座艙下拉選單的詞彙**:那個選單有「轉派中」一列,而且**預設就勾著**,所以 `statuses` 裡的
  `reassigning` 比對的是 **lock**(`taskStatusSetMatch`)。把這個語意塞進舊參數 = 改掉現役
  client 已經在依賴的答案;不支援它 = 預設視圖每次請求都默默漏掉所有轉派中任務。
  空字串/空重複 = 無條件(與省略同義);不認得的值 **400 並點名**(默默丟掉會在沒人知道的情況
  下縮小答案)。`status` / `statuses` / `open` / `executor` / `type` 全部 **AND**。
- **`TaskListItemDTO.dep_tasks`:每個 dep 的 `task_no`/`title`/`status`,由 handler 自己
  已經做的那一次 `ListTasks()` 建 map 解出來——`newTaskDepRefDTOs` 是純函數、**零額外查詢**
  (這是效能票,別把 payload 修掉換來 N+1)。`byID` 覆蓋**全表**,所以被 filter 排除掉的 dep
  照樣講得出名字,這正是 client 不再需要載入已結案母體的原因。
  🔴 **三種狀態不可壓成兩種**:有 status = 解析到;**status 為空**(TaskNo 仍推導得出)= 那張
  任務不存在了(查無此任務);**整個欄位缺席** = 這個 server 不解 dep(還不知道)。第三種是
  客戶端 `closedLoaded` 旗標退役後留下的那條誠實線,壓掉它就會把一張健康的已結案任務講成
  「查無此任務」。**`TaskDTO`(get_task)刻意沒有這個欄位**:輕量清單才是 payload 熱點,而任務卡
  的 dep 列**讀輕量列**不讀 hydrate 後的 detail,所以展開一張卡不會把 dep 列清空。
- **`OutsourceWorkerDTO` 多四欄** `task_no` / `task_created_ts` / `task_type_key` /
  `task_type_name`:外包面板的排序鍵與列標籤。它以前是**前端 join**——每次 worker/task/chat
  delta 都下載整部 task 歷史 + 整份手冊清單,只為了排五列、翻一個 key。`task_type_name` 由
  `taskTypeDisplayNames()`(一次 `ListTaskManuals`)解;**display_name 為空就留空**,client 照舊
  退回原始 key(填進去等於把已刪手冊藏在一個看起來像人寫的 label 後面)。
- **`TaskCountDTO.total`(未篩選總數)**:清單改成回答狀態集合之後,空清單再也分不出
  「什麼都沒有」與「這幾個狀態裡沒有」,而「目前沒有任務」是對整個工作室的主張。給一個
  grouped COUNT,座艙就不必為了寫一句空狀態文案把清單重新拉寬。
- **`list_tasks` 是 MCP 工具,所以 `spec/mcp-catalog.json` 同批改**(`spec_catalog_conformance_test.go`
  的三方對質會擋:routes 表 ≡ openapi ≡ 凍結 catalog 的參數集合);`catalog_hash` 只由**工具名**
  推導(`catalogHashOf` 讀 route 表),所以加一個參數不動 hash。seeds 同批教(§9c)。
- 哨兵:`api_perf_status_set_test.go`(集合/lock/400 點名/空集合/舊參數逐條不變/dep 解析到被
  filter 排除的那張/`dep_tasks` 恆為陣列/worker 四欄含 fallback/**worker 綁在已結案任務上四欄
  照樣有值**(`TestOutsourceWorkerOnAClosedTaskStillCarriesItsLabels`,done 與 terminated 各一、
  清單面與單筆面各一;mutant:在 `projectWorker` 的 task join 上加 `TaskIsTerminal` 判斷 →
  整個 package 恰好這 1 條紅)/count 的 open≠total),
  conformance `test_tasks.py` 的 `test_status_set_*` / `test_dep_tasks_*` / `test_task_count_*` /
  `test_outsource_worker_carries_*`。
  ✅ **查詢次數現在有守衛了(`api_perf_query_count_test.go`,本票同批補;此前是缺口)**:
  `TestTaskListTaskReadsDoNotGrowWithDepCount` 跑兩次 `?statuses=in_progress`(2 個 dep vs
  25 個 dep),斷言**一次 list 請求打到 `task` 表的讀取次數相等**(實測都是 **1** 次,就是
  `ListTasks`)。量測面是 **database/sql 的 driver seam**——測試檔自己把真 sqlite driver 包一層
  註冊成第二個名字、對 statement 文字比對,**production 一個 byte 都沒動**,而且因為是文字比對
  而不是掛在某個 DAL method 上,`GetTask`／手寫 `SELECT`／任何別的 DAL method 走的都是同一條路。
  **反恆真三道**:兩跑都先斷言每個 dep 真的被解析出來(有 title/status)且數量不同(語料非空)、
  計數器自己必須數到 > 0(seam 死掉時 `0 == 0` 會恆真地過)、次數還要 ≤ 2(「同樣地大」不算)。
  🔴 **但它不是「看得到任何路徑的查詢」——它認的是一份具名的形狀清單,射程外一律漏數,而漏數
  的方向是誤綠。** pattern 吃 `FROM`/`JOIN` ＋ 可選 schema 限定 ＋ 四種 SQLite identifier 引號
  (`"task"`/`'task'`/反引號/`[task]`),排除 `task_step`/`task_dep`/`task_artifact`。**射程與
  兩側邊界由 `TestTaskTableReadPatternKnowsItsOwnBoundary` 逐條對字串斷言**(護欄自己的邊界要被
  釘住,不能只寫在註解裡)。**仍在射程外**:讀取透過 VIEW／CTE 名字／子查詢別名間接發生、
  `FROM /*註解*/ task`、本 package 以外的碼。⚠️ **部分漏數比 seam 全死更危險**——合法的
  `ListTasks` 那一次仍被數到,所以 `> 0` 那道自檢**不會響**。這不是推論:review 實測 M7 用
  `SELECT title, status FROM "task" WHERE id = ?` 做 N+1,**舊 pattern(只認 `\bFROM\s+task\b`)
  下計數器停在 1、測試全綠**;pattern 放寬後同一顆 mutant **整個 package 恰好 1 條紅**。
  🔴 **這個缺口曾經是真的,留著當理由**:review 實測 M6 把 handler 的 join 改寫成**逐 dep
  `s.dal.GetTask(id)`**,整包 `go test` **一條都沒紅**。所以上面那句「釘住『被排除的 dep 也
  解得出來』這個性質」**不能當成 N+1 的替代覆蓋**——那條性質守的是「join 被整個拿掉」與
  「母體被縮小成只有回應那幾列」,**不是查詢次數**;逐 dep 查一樣答得出被排除的 dep,只是慢。
  同一顆 mutant 重跑(2026-08-01,`go clean -testcache`,還原用自備 `.orig` 不用 `git checkout --`):
  **整個 package 恰好 1 條紅**,就是這條新測試(few=3 次 / many=26 次),還原後全綠。
  ✅ 另外一半的靜態保證仍然成立:`newTaskDepRefDTOs` 的簽章裡**既沒有 `*DAL` 也沒有 `*apiServer`**
  (只收 `[]string` 與 `map[string]Task`),所以**那個函式**在編譯期就不可能查資料庫。
  ⚠️ 但那個保證只涵蓋它自己——**handler 仍然可以用逐 dep 查詢去餵那張 map**(M6 做的正是這件事),
  別把函式的保證讀成端點的保證;**端點那一半現在是由上面那條計數測試守的,不是由簽章守的。**

## 備份健康:把「安靜失敗」搬到座艙上(T-da06 → T-5e71,owner 2026-08-02 於 rc-61414359d85a / rc-84cdc40baf36 / rc-5ef6f1319f27 拍板)

`backup.go` 一直有完整的失敗訊號——**全部只走 `log.Printf`**,而這台機器的紀錄檔沒有聚合、座艙讀不到。所以「排程三天前就死了」與「一切正常」在 owner 眼裡**一模一樣**。T-ada9 自己的開票理由就寫著:一個安靜失敗的備份比沒有備份更危險。

- **形狀是 owner 選的,而且他改過一次**:T-da06 原本是右上角常駐燈 + 監控頁備份健康卡(rc-61414359d85a);owner 看到實物後在 **rc-5ef6f1319f27 選①推翻它**——燈與卡都拿掉,備份狀態改放 **設定 › 系統更新與備份**(T-5e71)。**刻意不發通知卡**(owner 在 rc-84cdc40baf36 選①):⚠️ 當時的理由是「燈已經永遠在畫面上」,而**那個前提已經不成立**——現在要 owner 自己走進設定頁才看得到。「要不要在那一列加只在異常時出現的紅點」是另一張卡在問的問題,**尚未裁定**;在他裁定前,不要把「不發通知」讀成「已經確認夠明顯」。
- **`backup_health.go` 是唯一的評估者**,`api_backup_health.go` 只讀持久裁決。畫面一律讀同一支 `GET /api/backup-health`(`principalAdminAgent`、`MCPExclude`),**構造上不可能各說各話**。
- 🔴 **watchdog 是 cmdServe 掛的獨立 goroutine,不是掛在備份流程上**。它要抓的第一名失敗就是「排程根本沒起來」——掛在備份路徑上的檢查在那個情況下永遠不會執行。
- 🔴 **狀態持久化在 `setting` 表**(`backup.watchdog_baseline_ts` 寫一次、`backup.health` 是 JSON 裁決),不是 in-memory。重啟會把「壞了三天」變回綠燈,而**重啟正是升級當下會發生的事**——那是最需要退路的時刻。`since_ts` 跨重啟保留,所以第三天讀到的仍是第三天。
- 🔴 **只有 `scheduled` 備份算證據**(`newestScheduledBackup` 過濾 reason)。manual 與 premigration 落在同一個目錄,算進去的話,一個人手動備份一次、或單純升級一次,就把一個死掉的排程再瞞 12 小時。
- **`unknown` 永遠不折疊成 `healthy`**:沒評估過、狀態讀不出來、剛裝好還沒有任何排程備份——三種都是「不知道有沒有退路」,而這張票的全部意義就是「沒有退路不能長得像有退路」。前端 `lib/backupHealth.ts` 是同一條規則的唯一鏡像(fetch 失敗也落到 unknown,不留著上一次的綠)。
- **時限一律 `backupStaleAfter()` = `backupStaleFactor * backupInterval`**,不另寫 12h;`stale_after_secs` 上 wire,前端不自己重算。
- **失敗要立刻可見**:`backupTick` 呼 `noteScheduledOutcome`。只靠 watchdog 的話,一次失敗要等一整個 stale 窗(12h)才看得出來,那半天 owner 都相信自己有退路。
- **接線本身有測試**:`armBackupHealth` 是 cmdServe **同步**呼叫(baseline 那一列在 serve 走到那裡就已經落庫),所以 `TestServeArmsTheBackupWatchdog` 用「佔住 port 讓 serve exit 1」那套(同 journal-mode 呼叫點測試)證明呼叫點還在——goroutine 只能用競態去斷言,同步寫入不用。
- **mutant 實測(2026-08-02,每顆前 `go clean -testcache`,整個 package 計數)**:失敗不記錄 → **2 條紅**;`never_ran` 分支拿掉 → **4 條紅**;`stale` 分支拿掉 → **1 條紅**(`TestDecideBackupHealth_TheWholeTable`);cmdServe 不掛 watchdog → **1 條紅**(`TestServeArmsTheBackupWatchdog`);manual/premigration 也算證據 → **2 條紅**;`unknown` 落回 `healthy` → **1 條紅**。前端三顆:unknown 落回 healthy → **3 條紅**(跨 lib/App/MonitorPage 三檔)、三種原因收斂成一句 → **1 條紅**、「從來沒跑過」渲染成 dash → **1 條紅**。
- ⚠️ **誠實的觀測底線**:watchdog 自己沒起來或 panic 時,沒有更高層的東西會叫。這是有意識接受的邊界,不是漏掉。
- ⚠️ **裁決列讀不出來時是「重新推導」,不是「保留」**(`evaluate` 明寫):檔案系統對 stale / never ran 是地面真相,下一輪就會重新算對;唯一會掉的是當下那個 `failed` 標記,而且掉了也有界——一直失敗的排程會在 `backupStaleAfter()` 內轉 stale 再叫一次。反過來保留讀不出來的列更糟:沒有任何東西能清掉它,一次壞寫入就把燈永久凍住。
- ⚠️ **獨立審查(2026-08-02)抓到的四件,已全部修掉並補測**:(a) 連續失敗的 `since_ts` 錨點**零覆蓋**——刪掉它整包照樣綠,而那正是生產常態(每 6 小時重試一次),沒有它「壞了三天」會每次重新計時(`TestRepeatedFailuresKeepTheFirstOnesStartTime`,mutant 實測 1 條紅);(b) 測試替身的 `failGet`/`failPut` 從沒被任何測試設過,而註解宣稱它們讓 fail-closed 路徑可達——**不可達的安全路徑不是安全路徑**(`TestAnUnusableSettingsStoreIsNeverGreen`);(c) `load()` 的註解把 `report()` 的性質講成整個模組的性質(見上一條);(d) conformance happy row 的斷言是**恆真**的(對封閉詞彙做成員檢查),已改成斷言「沒有退路的站不得讀成 healthy」。另修:baseline 改用 `strconv.ParseFloat` 嚴格解析(`fmt.Sscanf` 會接受數字**前綴**,`1785600000junk` 也算數,而 baseline 正是「從來沒跑過」的計時起點)、`report()` 拿掉一個永遠不可能獨立決定任何事的 `err != nil ||`。

## 已知邊界(誠實列,別當成熟功能用)
- **config 預設路徑是 CWD-relative `oc.toml`**(binary 沒有 source-path 可錨 repo root);部署正解走 `$OC_CONFIG`。
- **`-wal` 的大小:常態有界,但被一個長命讀者就變無界(T-dd7a 上線後的新運維面;實測 2026-08-01,只查事實、未改任何設定)。**
  - **repo 裡沒有任何地方設 `wal_autocheckpoint` 或 `journal_size_limit`**(grep 回 0 行),所以吃的是驅動預設:`wal_autocheckpoint=1000` 頁、`page_size=4096` ⇒ **約 4 MB 的 checkpoint 門檻**;`journal_size_limit=-1`(checkpoint 後**不截短**檔案,空間是重用的)。
  - **常態有界**:連續寫入約 80 MB 的資料(主檔長到 91 MB),`-wal` **全程停在 ~4.1 MB**。
  - 🔴 **一個開著的讀取交易就會讓它無界**:auto-checkpoint 需要沒有讀者卡著舊 snapshot,所以在**單一個** read tx 開著的期間連續寫入,`-wal` 從 4 MB 長到 **67 MB → 196 MB**(還在長)。讀者放手後**檔案不會縮回去**(`journal_size_limit=-1`),那個高水位留在磁碟上被重用。
  - ✅ **「碰巧」已經變成「被守住」**:立案時 `rdb.Begin`/`BeginTx` 在非測試碼裡一次都沒有,但**沒有任何東西在守它**——加一條讀取交易就會踩到,而且零訊號。現在由 `TestNoTransactionIsOpenedOnTheReadPool`(**一條必須回 0 行的查詢**,與單檔複製守衛同一個語料、同一個做法)擋住,理由也一樣:WAL 引進的新危險 ＋ 壞掉時零訊號 ＋ 今天沒有違規者。**掃得到三種形狀**:(a) `<x>.rdb.Begin/BeginTx`、(b) 函式內 `q := d.rdb` 之後 `q.Begin()`(一跳的別名,直接掃會被繞過的那條)、(c) 把 `.rdb` 餵進**任何本 package 收 `*sql.DB` 且在其 body 對該參數 `Begin` 的函式**(AST 兩趟)。**拒絕訊息講後果不只講規則**(pin snapshot ⇒ `-wal` 無界成長且不縮回、零訊號),並指出正解方向(一條 statement / 真要多筆一致就走 `d.wdb` 的 `inTx`,那個池上限 1、交易本來就短),**刻意不提供任何旁路**。
    - 🔴 **別名判定與 callee 比對是三個掃描共用的(`readPoolAliases` / `isReadPoolHandle` / `calleeSimpleName`),這是刻意的**:第二輪獨立審查跑了 10 個繞過形狀,抓到的三處全是「**描述涵蓋範圍比實際寬**」——與第一輪 A 同一種病。逐一修掉並共用之後,「一個掃描有守、另一個沒守」不可能再發生:
      - **`var q *sql.DB = d.rdb; q.Begin()`** 原本整條逃掉(只收 `*ast.AssignStmt`),而註解已宣稱擋「local aliases of rdb」⇒ 補 `*ast.ValueSpec`。
      - **方法形式 `h.f(d.rdb)`** 原本配不到(pass-2 只比對 `*ast.Ident` callee),**seam 守衛也一樣逃掉**(那是第一輪就有的),而 CLAUDE.md 用「任何…函式」描述它 ⇒ pass-2 改用 `calleeSimpleName` 同時吃 `Ident` 與 `SelectorExpr`,seam 守衛的 pass-1 也不再過濾 `fn.Recv`(方法宣告一併登記)。**名字比對是粗的**(同名的 method 與 func 會被混為一談),那是**安全方向**——只會多給人看、不會少抓。
      - **寫入 SQL 掃描沒有別名規則**(`q := d.rdb; q.Exec(INSERT…)` 逃掉)⇒ 改成 per-function 走訪並吃同一份別名判定。**選「修」不選「揭露」**:成本只有幾行,而「一個掃描有守、另一個沒守」本身就是下一個人的陷阱。
    - ⚠️ **掃不到的(誠實邊界,已按上面的修正更新過——別留一份「說掃不到、其實掃得到」的反向謊)**:**兩跳以上的別名**(`a := d.rdb; b := a`)、**存進 struct 欄位或閉包再取用**、reflection、本 package 以外的碼;以及 🔴 **另一種同樣會 pin snapshot 的形狀——`*sql.Rows` 開著很久**(`Query` 之後在 loop 裡做慢事、或忘了 `Close`)。後者**靜態上無法判定「很久」**,所以守衛刻意不碰;審查實測它的危害與開著讀交易**逐位元組相同**(`-wal` 4.1 → 61.9 MB、主檔凍在 5.0 MB、`Close()` 後也不縮回),**而且全程沒有任何 `Begin`** ⇒ 它是這條規則現在最可能的實際破法。`migrate.go` 的註解已明確分成「ENFORCED(沒有顯式 Begin)」與「NOT ENFORCED(Rows 要立刻消耗完,只是紀律)」兩段——原本那句 `that is ENFORCED` 把後者包進去了,是假的。
    - **反恆真**:掃描先自證「看得到 `Begin`/`BeginTx` 這個構造」(寫池的 `inTx`/`HardDeleteMember` 就是活證據);計數為 0 = 偵測器死了,那時「零發現」什麼都不證明。
    - **mutant 實測(2026-08-01;每顆前 `go clean -testcache`,還原一律用 scratchpad 備份)**:
      - (a) 直接 `d.rdb.BeginTx` → 紅、點名 `dal.go:203 BeginTx on d.rdb`。
      - (b) `q := d.rdb; q.Begin()` → 紅、點名 `Begin on q (a local alias of the read pool)`。
      - (c) 裸函式 `probeSnapshotRead(d.rdb)`(該函式對參數 `Begin`)→ 紅。
      - **(R1) `var q *sql.DB = d.rdb; q.Begin()` → 紅**、點名 `Begin on q (a local alias of the read pool)`(**修正前這顆是綠的**)。
      - **(R2) 方法形式 `probeBox{}.snapshotRead(d.rdb)` → 紅**、點名 `snapshotRead(…the read pool…)`(**修正前綠**)。
      - **(R2 seam) 方法形式 `seamBox{}.writeVia(d.rdb, …)`(參數型別 `sqlExecer`)→ `TestReadPoolIsNeverHandedToAWriteSeam` 紅**、點名 `writeVia(…the read pool…)`(**修正前綠,且那是第一輪就存在的洞**)。
      - **(R4) `q := d.rdb; q.Exec(INSERT…)` → `TestNoWriteStatementRunsOnTheReadPool` 紅**、點名該句 INSERT(**修正前綠**)。
      - 全部還原後四道守衛回綠。
    - ⚠️ **`TestServeAsksTheDatabaseWhichJournalModeItIsIn` 的失敗訊息分三種原因,不共用一句**:印出 `WARNING: journal_mode` = **接線是好的、WAL 沒開**(去查 `openSQLite` 的 pragma);完全沒印 `journal_mode=` = **呼叫點不見了**;印了但不是 wal = 第三種。舊版把前兩種都講成「the self-check is not wired in」,**把人指去錯的檔**——正是這張票一直在修的那類缺陷。
  - **乾淨關閉會清掉**:最後一條連線關閉時 WAL 被 checkpoint 並移除(實測:serve 正常退出後資料目錄只剩 `officraft.db`)⇒ 高水位不跨重啟。**當機不會**——那時 `-wal` 還在,而它裡面就是最需要的那幾筆(這也是單檔複製守衛存在的理由)。
  - **要處理的話有兩個旋鈕**(`journal_size_limit` 讓檔案 checkpoint 後真的截短、或縮小 `wal_autocheckpoint`)——**本次刻意沒動**,等裁定。

## 慣例
- 改 Go → fresh build 驗證，**不 commit binary**(root §13)。需要可單檔 boot/裝機的 build 前，先跑 `bash bin/build-seedsdist && bash bin/build-bindist` 再 `go build -ldflags="-s -w"`——asset 已 embed-only，漏 staging 會默默丟掉單檔 boot/裝機能力；本機產物是 gitignored，部署 binary 由發版流程 fresh build，故此步靠人工紀律。
- 有別於 cli/ 的 stdlib-only:本模組帶第三方依賴(BurntSushi/toml、pressly/goose/v3、modernc.org/sqlite)——server 端 schema/config 需求正當化;新增依賴前先想能不能 stdlib。
- 新端點 = RouteSpec 表加一行(`routes.go`),不散寫 mount;boot assertion 缺 requires 即拒起。

## 取消喚醒 / 復活死掉的 session：兩個「問錯問題」的守衛（T-7526）

兩個缺陷形狀相同：**守衛問的是「有沒有人下過指令」（INTENT），該問的是「它現在是什麼狀態」（LIVENESS）。**

- **取消喚醒（deactivate 打在 waking 成員身上）原本一個指令都沒發出去。** owner 親自遇到、以為自己記錯。`decideDown` 第一個分支是 `if !obs.Online { converged offline }`，而 **waking 依定義就是 `!online`**（`deriveLiveness` 只在 `!Online` 時算成 waking），所以整個 waking 窗內 cadence 什麼都不派：先前 START 已經放上機器的那個 process 照常開完機、連上、變綠，然後才以「online + desired offline」的身分進 `decideDown`，開始它的 120s 寬限。owner 看到的就是「按了沒反應，兩分鐘後才停」。
  - 修在 **handler**（`HandleDeactivateMember…`），不在 reconcile 核：presence 是 waking 就 `dispatchRobustStopNow`。**`cancellingWake` 必須在 `StoppingSince` 被蓋之前算**——蓋那一筆本身就會終結 waking 的判定。
  - **刻意只給 waking 這一格**：線上成員的停止**保留 120s 寬限**（它手上有東西要收），已離線的成員**什麼都不派**（沒有 session、也沒有進行中的喚醒）。三格各有自己的哨兵，`_OnlineMemberKeepsTheGracefulGrace` 就是防「把取消寬限化成每次停止都 force-stop」的那道。
  - **沒有寬限可失去**：還沒連上的成員沒領過任何工作，它進不去的那個窗買不到東西。
  - **`reconcileMemberNow` 照舊呼叫**：raw dispatch 不碰 reconcile store，cadence 的 STOP 臂仍是冪等 backstop。
  - 🔴 **治理面沒有變寬**：deactivate 與 force-stop **本來就同在 `principalAdminAgent`**（唯一的非座艙入口是 MCP 工具 `deactivate_member`；**CLI 沒有這個入口**——`cli/officraft` 只是 launchd shim），所以這裡沒有讓任何人取得原本取得不到的能力。`TestDeactivateMember_StaysAdminGatedAfterTheCancelDispatch` 兩件事一起釘：一般 agent 打 deactivate 是 403，**而且**兩列的 `Requires` 必須相等——哪天有人把 deactivate 調低，那顆 mutant 就會紅。
- **`report_waking` 會抹掉取消留下的唯一痕跡。** 它無條件 `StoppingSince = 0`，於是那個「已經在開機、取消才追到」的 agent 一連上就畫出一片新的綠，蓋在一個仍然是 offline 的意圖上面。改成**只有 `desired_state == online` 才清**：清掉陳舊錨點對**普通**開機是對的（否則每個停過的成員回來都像還在收尾——`_ClearsTheStopTraceOnAnOrdinaryBoot` 釘住這半），但那份意圖本身才是「這次開機到底要不要」的權威。
- **外包 restart 的守衛同一個病**：`desired_state != offline → 409` 問的是「有沒有人按過停止」。**session 自己死掉時 `desired_state` 還是 online**，所以唯一能把它叫回來的端點回 409；而座艙的重啟入口是看 presence 的（此時 offline），畫出來的是「停止」。兩邊合起來 = owner 對一個死掉的 worker **完全沒有任何復活途徑**。改成 `desired_state != offline && hub.IsOnline(id)`：**兩半都是承重的**——真的活著仍然 409，restart 不會變成偷偷的雙重 spawn（`_ClearsAndRedispatches` 的第一段就是那道，`B1b` mutant 會紅）。
  - **座艙同批**（`WorkerDetailPanel.tsx` 的 `noLiveSession = stopped || offline`）與 **mock 同批**（mock↔http parity；不改的話 FE 測試會對著一個仍然壞掉的假 server 變綠）。
  - **舊文「409 if not stopped」在新守衛下是假的（不是過時），所以要同批清乾淨**（§13 spec-first、§9c 動到 agent 互動面就要同批教）。
    🔴 **不要照抄清單去信任地跳過** —— 第一版這裡就列了 5 個位置、實際有 9 個，漏掉的其中一個還在被改的那個函式自己的 godoc 裡（`mock.ts` 更慘：同一個函式裡新舊註解互相矛盾）。**判準不是清單，是這條 grep 必須回 0 行**：
    ```
    grep -rn "409 if not stopped\|not stopped (nothing to restart)" \
      --exclude-dir=node_modules --exclude-dir=.git . | grep -v "seedsdist\|docsdist"
    ```
    同理，409 的 envelope 訊息本身也從「worker is not stopped」改成「worker is still online」（server 與 mock **成對**改，mock↔http parity）。
- ⚠️ **從乾淨 worktree 跑 `go test` 前要先 staging embed assets**（asset 是 embed-only，見上方 `assets.go` 條）——`bin/ci.sh` 會做，手跑不會：
  ```
  bash bin/build-seedsdist && bash bin/build-docsdist
  ```
  沒 staging 時會紅一大片，而且它們長得很像你剛改壞的東西（**都不是回歸**，在 `origin/main` 上同樣紅）：
  - 缺 `seedsdist` → `TestRestartWorker_ClearsAndRedispatches`、`TestRestartWorker_NoKillTarget_StillAttemptsStart`（`boot_context_failed: open system_interaction.md`）
  - 缺 `docsdist` → `TestDocsMcpToolsCallableByAssistantAgent`、`TestGetDocHandlerServesStagedEmbed`、`TestDocsdistEmbedsExactlyGuide`
  🔴 **這個列表同樣是「清單型」的東西**：第一版只揭露了前兩條，第二版寫「至少 5 條」，**兩版都低估**。實測（兩個 dist 都清空、其餘不動）：**132 條紅**，含整批 `TestContextDocCap_*`、`TestPatchLessons*`、`TestDocumentHistory*`、`TestLessonsWrite*`——沒有一條的名字看得出跟 embed staging 有關。判準是**先 staging 再判紅**，不是背這張表。
  ✅ **`bin/build-bindist` 不是 `go test` 的前置**（實測：只清空 bindist、`go test ./...` 仍 0 紅）。它是**單檔 boot/裝機**能力的前置，見上方 `assets.go` 條；跑測試只要 seedsdist + docsdist。

- **mutant 驗證紀錄**：`docs/design/worker-panel-parity-mutants.md`（13 支，含 3 支第一輪存活、補測試後才紅的）。

## 正職與外包的刻意不對稱（T-072b）

- `reconcileDecide` 只共用 desired×observed 的純決策。member 的觀測是完整的 member projection；worker 在進核前有 worker lifecycle 的遮罩與狀態換算，不能為了形式對齊而合併 producer 或抹平這些輸入差異。
- 外包仍有三個專屬行為：(1) `outsource_sched.go::outsourceDecide` 依任務指派並由 `domain.go::DeriveCodename` 配發 task-minted codename；(2) 任務終態由 `dal_tasks.go::ReleaseWorkersForTask` 釋放，close-out 再由 `worker_spawn.go::dismissOutsourceWorkersForTask` 收回 session；(3) `DeriveCodename` 與 `ListOutsourceWorkers` 的完整集合 fold 保證已用 codename 永不重用。是否有第四項專屬行為必須先由 owner 裁定，不能在實作時自行擴張。
